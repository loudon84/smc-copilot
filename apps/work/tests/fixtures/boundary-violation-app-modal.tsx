/** Fixture only — must not live under src/renderer/src/components. */
import { Button } from "@/components/ui/button";

export function ProbeModal(): JSX.Element {
  return <Button>probe</Button>;
}
