# NSIS EnVar Plugin

This directory should contain the EnVar plugin for NSIS.

Download the Unicode build from: https://github.com/GsNSIS/EnVar/releases

Place `EnVar.dll` in `build/nsis/Plugins/x86-unicode/EnVar.dll`

The build script (`scripts/ensure-nsis-plugins.ts`) will automatically download it if missing.
