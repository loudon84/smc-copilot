/**
 * Compatibility re-export — upstream File Platform historically imported
 * `@shared/files` / `../../shared/files`. Production contract lives in
 * `shared/chat-files`.
 */
export * from "../chat-files";
