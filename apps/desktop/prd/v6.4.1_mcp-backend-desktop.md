依据当前源码约束：nodeskclaw 认证入口在 `/api/v1/auth` 下，`account-login` 支持账号统一登录；`/auth/me` 返回 `current_org_id / org_role / portal_org_role`，可用于 Desktop member 校验；Desktop 现有 AuthEndpointConfig 已包含 `backendUrl / authPrefix / aiosHomeUrl`，适合作为登录、Proxy、MCP Gateway 验证的唯一配置源。  

下面是可直接交给 Cursor 执行的 PRD。

# PRD：v6.4.1_mcp-backend-desktop

## 1. 版本信息

```text
版本号：v6.4.1_mcp-backend-desktop
项目：ai-os-desktop
目标后端：nodeskclaw
后端地址：http://192.168.0.118:4510
目标模块：
  - Desktop 用户登录
  - AuthEndpointConfig
  - MCP Skill Gateway Local Proxy
  - Hermes MCP Server 注册
  - HermesMcpGatewayPage 状态一致性
实施工具：Cursor
```

---

## 2. 背景

当前 Desktop 已完成 MCP Gateway 页面初步接入，页面包含：

```text
1. Login 状态
2. Local Proxy 状态
3. Hermes registration 状态
4. Register / Unregister 操作
5. Test proxy / Test remote MCP 操作
```

当前页面仍显示旧后端：

```text
http://192.168.0.110:8000
```

现需要统一切换到 nodeskclaw backend：

```text
http://192.168.0.118:4510
```

并保证以下链路完全一致：

```text
Desktop Login
  → nodeskclaw member 登录

Desktop AuthEndpointConfig.backendUrl
  → MCP Proxy 远程转发目标

MCP Proxy health
  → 显示当前 nodeskclaw backend 与 remote MCP URL

Test remote MCP
  → 必须通过 Local Proxy 转发

Hermes config.yaml
  → 只注册 Local Proxy URL，不写远程 backend，不写 token
```

---

## 3. 核心目标

### 3.1 登录目标

Desktop 用户登录从原独立 serve 登录迁移为 nodeskclaw member 登录。

登录接口：

```text
POST http://192.168.0.118:4510/api/v1/auth/account-login
```

登录后校验接口：

```text
GET http://192.168.0.118:4510/api/v1/auth/me
```

登录成功标准：

```text
1. access_token 存在
2. refresh_token 存在
3. user.current_org_id 存在
4. user.portal_org_role 存在
5. user.is_active = true
```

### 3.2 MCP Backend 统一目标

统一配置源：

```text
AuthEndpointConfig.backendUrl
```

该配置必须同时用于：

```text
1. 登录请求
2. refresh 请求
3. /auth/me 请求
4. MCP Proxy 转发目标
5. Test remote MCP 目标
6. HermesMcpGatewayPage 展示 Backend URL
```

### 3.3 Local Proxy 目标

本地 MCP Proxy 地址：

```text
http://127.0.0.1:48742/mcp
```

远程 MCP Gateway 地址：

```text
http://192.168.0.118:4510/api/v1/hermes/mcp
```

Hermes Agent 只访问本地 Proxy：

```yaml
mcp_servers:
  mcp_skill_gateway:
    enabled: true
    type: http
    url: http://127.0.0.1:48742/mcp
```

---

## 4. 非目标

本版本不做以下内容：

```text
1. 不做 Desktop 侧 Tool 调用中心
2. 不做 Desktop 侧 Task Center
3. 不做 Desktop 侧 Artifact Center
4. 不直接在 Renderer 请求 nodeskclaw backend
5. 不把 accessToken 写入 Hermes config.yaml
6. 不把 remote backend URL 写入 Hermes config.yaml 的 mcp server
7. 不改 nodeskclaw 后端认证接口
8. 不改 Hermes Agent MCP 调度逻辑
```

---

## 5. 统一链路设计

### 5.1 登录链路

```text
用户输入 account/password
  ↓
Renderer 调用 window.desktopAuth.login
  ↓
Preload 转发 auth:login IPC
  ↓
Main Process AuthClient
  ↓
POST /api/v1/auth/account-login
  ↓
解析 ApiResponse.data
  ↓
GET /api/v1/auth/me
  ↓
校验 member 权限
  ↓
writeStoredSession
  ↓
updateTokenInjectionPolicy
  ↓
启动或刷新 MCP Local Proxy
  ↓
刷新 HermesMcpGatewayPage
```

### 5.2 MCP Proxy 链路

```text
Hermes Agent
  ↓
http://127.0.0.1:48742/mcp
  ↓
Desktop Main Process Local Proxy
  ↓
读取 token-store accessToken
  ↓
POST http://192.168.0.118:4510/api/v1/hermes/mcp
  ↓
nodeskclaw mcp-skill-gateway
```

### 5.3 Test remote MCP 链路

```text
HermesMcpGatewayPage
  ↓
window.mcpSkillGatewayRuntime.testRemoteMcp()
  ↓
Main IPC
  ↓
POST http://127.0.0.1:48742/mcp
  ↓
Local Proxy 注入 Authorization
  ↓
POST http://192.168.0.118:4510/api/v1/hermes/mcp
  ↓
tools/list
```

禁止路径：

```text
Renderer 直接请求 http://192.168.0.118:4510/api/v1/hermes/mcp
```

---

## 6. 数据契约

### 6.1 AuthEndpointConfig

文件：

```text
src/shared/auth/auth-contract.ts
```

目标结构：

```ts
export interface AuthEndpointConfig {
  backendUrl: string;
  authPrefix: string;
  aiosHomeUrl: string;
}

export interface LoginInput {
  endpointConfig: AuthEndpointConfig;
  account: string;
  password: string;

  /**
   * Legacy compatibility.
   * Do not use in new UI.
   */
  email?: string;
}
```

默认配置：

```ts
export function getDefaultAuthEndpointConfig(): AuthEndpointConfig {
  return {
    backendUrl: "http://192.168.0.118:4510",
    authPrefix: "/api/v1/auth",
    aiosHomeUrl: "http://192.168.0.118:4517",
  };
}
```

如当前 portal frontend 暂未部署在 `4517`，允许临时保留原 `aiosHomeUrl`，但 `backendUrl` 必须为：

```text
http://192.168.0.118:4510
```

### 6.2 DesktopAuthUser

文件：

```text
src/shared/auth/auth-contract.ts
```

目标结构：

```ts
export interface DesktopAuthUser {
  id: string;
  username: string;
  displayName?: string;

  email?: string;
  phone?: string;
  avatarUrl?: string;

  currentOrgId?: string;
  orgRole?: string | null;
  portalOrgRole?: string | null;

  isSuperAdmin?: boolean;
  mustChangePassword?: boolean;
}
```

映射规则：

```text
nodeskclaw user.id                  → id
nodeskclaw user.username/email      → username
nodeskclaw user.name                → displayName
nodeskclaw user.email               → email
nodeskclaw user.phone               → phone
nodeskclaw user.avatar_url          → avatarUrl
nodeskclaw user.current_org_id      → currentOrgId
nodeskclaw user.org_role            → orgRole
nodeskclaw user.portal_org_role     → portalOrgRole
nodeskclaw user.is_super_admin      → isSuperAdmin
nodeskclaw user.must_change_password → mustChangePassword
```

### 6.3 NodeDeskClawApiResponse

新增文件：

```text
src/main/auth/nodeskclaw-auth-response.ts
```

内容：

```ts
export interface NodeDeskClawApiResponse<T> {
  code: number;
  error_code?: number | null;
  message_key?: string | null;
  message: string;
  data: T | null;
}

export function unwrapNodeDeskClawResponse<T>(body: unknown): T {
  if (!body || typeof body !== "object") {
    throw new Error("Invalid response body");
  }

  const resp = body as Partial<NodeDeskClawApiResponse<T>>;

  if (typeof resp.code === "number") {
    if (resp.code !== 0) {
      throw new Error(resp.message || resp.message_key || "Request failed");
    }

    if (resp.data == null) {
      throw new Error("Invalid response: missing data");
    }

    return resp.data;
  }

  return body as T;
}
```

---

## 7. AuthClient 改造

### 7.1 文件

```text
src/main/auth/auth-client.ts
```

### 7.2 登录接口

旧逻辑：

```text
POST /login
body: { email, password }
```

新逻辑：

```text
POST /account-login
body: { account, password }
```

实现要求：

```ts
const account = input.account?.trim() || input.email?.trim();

if (!account) {
  throw new Error("Account is required");
}

const url = buildAuthUrl(input.endpointConfig, "account-login");

const res = await fetch(url, {
  method: "POST",
  headers: { "Content-Type": "application/json" },
  body: JSON.stringify({
    account,
    password: input.password,
  }),
});
```

### 7.3 登录响应解析

nodeskclaw 登录返回：

```ts
{
  code: 0,
  message: "success",
  data: {
    access_token: string,
    refresh_token: string,
    token_type: "bearer",
    expires_in: number,
    user: NodeDeskClawUserInfo
  }
}
```

解析后必须调用 `/auth/me` 再校验 member：

```ts
const loginData = unwrapNodeDeskClawResponse<NodeDeskClawLoginResponse>(body);
const me = await fetchMe(input.endpointConfig, loginData.access_token);
assertMember(me);
return mapNodeDeskClawSession(loginData, me);
```

### 7.4 `/auth/me` 实现

新增：

```ts
async function fetchMe(
  endpointConfig: AuthEndpointConfig,
  accessToken: string,
): Promise<NodeDeskClawUserInfo> {
  const res = await fetch(buildAuthUrl(endpointConfig, "me"), {
    method: "GET",
    headers: {
      Authorization: `Bearer ${accessToken}`,
    },
  });

  if (!res.ok) {
    const text = await res.text().catch(() => "");
    throw new Error(formatAuthError(res.status, text));
  }

  const body = await res.json();
  return unwrapNodeDeskClawResponse<NodeDeskClawUserInfo>(body);
}
```

### 7.5 member 校验

新增：

```ts
function assertMember(user: NodeDeskClawUserInfo): void {
  if (user.is_active === false) {
    throw new Error("当前账号已被禁用");
  }

  if (!user.current_org_id) {
    throw new Error("当前账号未加入任何组织，无法登录 Desktop");
  }

  if (!user.portal_org_role) {
    throw new Error("当前账号不是组织成员，无法登录 Desktop");
  }
}
```

### 7.6 refresh 改造

旧逻辑问题：

```text
refresh 接口返回被当作完整 LoginResponse 解析。
```

nodeskclaw refresh 只返回 token，不返回 user。新流程：

```text
POST /api/v1/auth/refresh
  ↓
unwrap TokenResponse
  ↓
GET /api/v1/auth/me with new access_token
  ↓
assertMember
  ↓
map session
```

实现要求：

```ts
async refresh(
  endpointConfig: AuthEndpointConfig,
  refreshToken: string,
): Promise<StoredAuthSession> {
  const res = await fetch(buildAuthUrl(endpointConfig, "refresh"), {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ refresh_token: refreshToken }),
  });

  if (!res.ok) {
    const text = await res.text().catch(() => "");
    throw new Error(formatAuthError(res.status, text));
  }

  const body = await res.json();
  const token = unwrapNodeDeskClawResponse<NodeDeskClawTokenResponse>(body);
  const me = await fetchMe(endpointConfig, token.access_token);
  assertMember(me);

  return mapNodeDeskClawSession(
    {
      access_token: token.access_token,
      refresh_token: token.refresh_token,
      token_type: token.token_type,
      expires_in: token.expires_in,
      user: me,
    },
    me,
  );
}
```

---

## 8. Auth URL 改造

### 8.1 文件

```text
src/shared/auth/auth-url.ts
```

### 8.2 AuthUrlPath

修改：

```ts
export type AuthUrlPath =
  | "login"
  | "account-login"
  | "refresh"
  | "me"
  | "logout";
```

### 8.3 buildAuthUrl

保留：

```ts
export function buildAuthUrl(config: AuthEndpointConfig, path: AuthUrlPath): string {
  return `${normalizeBaseUrl(config.backendUrl)}${normalizePrefix(config.authPrefix)}/${path}`;
}
```

### 8.4 normalizeBackendBaseUrl

必须支持用户误填以下地址时自动归一：

```text
http://192.168.0.118:4510/api/v1
http://192.168.0.118:4510/api/v1/auth
http://192.168.0.118:4510/api/v1/desktop
```

统一归一为：

```text
http://192.168.0.118:4510
```

---

## 9. Login UI 改造

### 9.1 输入字段

旧字段：

```text
Email
Password
```

新字段：

```text
Account
Password
```

文案：

```text
账号
密码
```

Placeholder：

```text
邮箱 / 手机号 / 用户名
```

### 9.2 调用

旧调用：

```ts
desktopAuth.login({
  endpointConfig,
  email,
  password,
});
```

新调用：

```ts
desktopAuth.login({
  endpointConfig,
  account,
  password,
});
```

### 9.3 兼容

如果旧页面仍传 `email`：

```ts
account = input.account || input.email
```

不得中断旧流程。

---

## 10. MCP Gateway Runtime 改造

### 10.1 统一 backend 来源

文件：

```text
src/main/mcp-skill-gateway-runtime/mcp-skill-gateway-config.ts
src/main/mcp-skill-gateway-runtime/mcp-skill-gateway-proxy.ts
src/main/mcp-skill-gateway-runtime/mcp-skill-gateway-ipc.ts
```

规则：

```text
MCP Runtime 不再维护独立 backendBaseUrl。
必须从 readAuthEndpointConfig() 读取 backendUrl。
```

远程 MCP URL 统一计算：

```ts
const backendBaseUrl = normalizeBackendBaseUrl(endpointConfig.backendUrl);
const remoteMcpUrl = `${backendBaseUrl}/api/v1/hermes/mcp`;
```

本地 Proxy URL 统一计算：

```ts
const localMcpUrl = `http://127.0.0.1:${localProxyPort}/mcp`;
```

### 10.2 Runtime Config

保留字段：

```ts
export interface McpSkillGatewayRuntimeConfig {
  enabled: boolean;
  localProxyHost: "127.0.0.1";
  localProxyPort: number;
  autoStartProxy: boolean;
  autoRegisterToHermes: boolean;
  autoRestartHermesGateway: boolean;
  registeredProfiles: string[];
  updatedAt: string;
}
```

废弃字段：

```ts
backendBaseUrl
```

如已有历史配置包含 `backendBaseUrl`，读取时忽略。

---

## 11. Local MCP Proxy 改造

### 11.1 文件

```text
src/main/mcp-skill-gateway-runtime/mcp-skill-gateway-proxy.ts
```

### 11.2 GET /health

返回：

```ts
{
  ok: true,
  service: "mcp-skill-gateway-proxy",
  status: "running",
  loggedIn: true,
  backendBaseUrl: "http://192.168.0.118:4510",
  remoteMcpUrl: "http://192.168.0.118:4510/api/v1/hermes/mcp",
  localMcpUrl: "http://127.0.0.1:48742/mcp"
}
```

未登录：

```ts
{
  ok: false,
  service: "mcp-skill-gateway-proxy",
  status: "running",
  loggedIn: false,
  backendBaseUrl: "http://192.168.0.118:4510",
  remoteMcpUrl: "http://192.168.0.118:4510/api/v1/hermes/mcp",
  localMcpUrl: "http://127.0.0.1:48742/mcp",
  errorCode: "MCP_GATEWAY_NOT_LOGGED_IN"
}
```

### 11.3 POST /mcp

处理规则：

```text
1. 只允许 POST /mcp
2. 请求体最大 2MB
3. 读取 Desktop accessToken
4. 未登录返回 JSON-RPC error -32010
5. 读取 AuthEndpointConfig.backendUrl
6. 转发到 {backendBaseUrl}/api/v1/hermes/mcp
7. 注入 Authorization: Bearer <accessToken>
8. 返回远端 JSON-RPC 原始响应
```

未登录返回：

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "error": {
    "code": -32010,
    "message": "Desktop login required"
  }
}
```

远程未授权返回：

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "error": {
    "code": -32013,
    "message": "Desktop session expired"
  }
}
```

### 11.4 日志要求

日志路径：

```text
<userData>/logs/mcp-skill-gateway-proxy.log
```

允许记录：

```text
time
jsonrpc_id
method
remote_status
duration_ms
error_code
```

禁止记录：

```text
Authorization
accessToken
refreshToken
password
api_key
secret
cookie
```

---

## 12. Test Remote MCP 改造

### 12.1 文件

```text
src/main/mcp-skill-gateway-runtime/mcp-skill-gateway-ipc.ts
```

### 12.2 IPC

```text
mcp-skill-gateway-runtime:test-remote-mcp
```

### 12.3 实现规则

必须通过本地 Proxy：

```ts
const localProxyUrl = `http://127.0.0.1:${port}/mcp`;

const res = await fetch(localProxyUrl, {
  method: "POST",
  headers: { "Content-Type": "application/json" },
  body: JSON.stringify({
    jsonrpc: "2.0",
    id: "desktop-test-tools-list",
    method: "tools/list",
    params: {},
  }),
});
```

返回结构：

```ts
export interface McpGatewayRemoteTestResult {
  ok: boolean;
  localProxyUrl: string;
  backendBaseUrl: string;
  remoteMcpUrl: string;
  toolCount?: number;
  jsonrpcErrorCode?: number;
  error?: string;
}
```

成功：

```ts
{
  ok: true,
  localProxyUrl: "http://127.0.0.1:48742/mcp",
  backendBaseUrl: "http://192.168.0.118:4510",
  remoteMcpUrl: "http://192.168.0.118:4510/api/v1/hermes/mcp",
  toolCount: 3
}
```

权限不足但链路正常：

```ts
{
  ok: false,
  localProxyUrl,
  backendBaseUrl,
  remoteMcpUrl,
  jsonrpcErrorCode: -32003,
  error: "permission denied"
}
```

---

## 13. Hermes MCP 注册一致性

### 13.1 写入规则

Hermes config.yaml 只写本地 Proxy URL：

```yaml
mcp_servers:
  mcp_skill_gateway:
    enabled: true
    type: http
    url: http://127.0.0.1:48742/mcp
```

禁止写入：

```yaml
headers:
  Authorization: Bearer xxx
```

禁止写入：

```text
http://192.168.0.118:4510/api/v1/hermes/mcp
```

### 13.2 注册前校验

注册前执行：

```text
1. 检查用户已登录
2. 检查 member 校验通过
3. 检查 Proxy 已启动
4. 检查 Proxy health.backendBaseUrl = AuthEndpointConfig.backendUrl
5. 检查 Proxy health.localMcpUrl = 即将写入 config.yaml 的 URL
```

### 13.3 注册后校验

注册后读取 config.yaml：

```text
1. mcp_servers.mcp_skill_gateway 存在
2. enabled = true
3. type = http
4. url = http://127.0.0.1:48742/mcp
```

返回：

```ts
export interface McpGatewayProfileRegistration {
  profile: string;
  configPath: string;
  registered: boolean;
  enabled: boolean;
  url: string | null;
  expectedUrl: string;
  urlMatched: boolean;
  backendMatched: boolean;
  ready: boolean;
}
```

ready 判定：

```ts
ready =
  registered &&
  enabled &&
  urlMatched &&
  backendMatched;
```

---

## 14. HermesMcpGatewayPage 改造

### 14.1 文件

```text
src/renderer/src/screens/Hermes/pages/McpGateway/HermesMcpGatewayPage.tsx
```

### 14.2 页面必须展示

#### Login

```text
Login status:
  Logged in / Not logged in

User:
  admin@portal.local

Backend URL:
  http://192.168.0.118:4510

Member:
  verified / failed

Portal org role:
  member / admin / viewer / empty
```

#### Local Proxy

```text
Proxy status:
  RUNNING / STOPPED / FAILED

Local URL:
  http://127.0.0.1:48742/mcp

Remote MCP URL:
  http://192.168.0.118:4510/api/v1/hermes/mcp
```

#### Hermes Registration

表格字段：

```text
Profile
Config path
Registered
Enabled
URL
Expected URL
URL matched
Backend matched
Ready
Action
```

#### Consistency

新增状态块：

```text
Consistency check

Auth backend:
  http://192.168.0.118:4510

Proxy backend:
  http://192.168.0.118:4510

Remote MCP:
  http://192.168.0.118:4510/api/v1/hermes/mcp

Hermes MCP URL:
  http://127.0.0.1:48742/mcp

Status:
  Ready / Mismatch
```

### 14.3 不一致提示

当 `ready=false`：

```text
当前 MCP Gateway 配置不一致：
- 登录后端：{authBackendUrl}
- Proxy 后端：{proxyBackendUrl}
- Hermes 注册 URL：{registeredUrl}
- 期望注册 URL：{expectedLocalProxyUrl}

请点击“重新注册 default profile”。
```

### 14.4 按钮行为

```text
Start:
  启动 Local Proxy

Stop:
  停止 Local Proxy

Restart:
  重启 Local Proxy

Test proxy:
  GET /health

Test remote MCP:
  通过 Local Proxy 调 tools/list

Register default profile:
  写入 default profile config.yaml

Unregister:
  设置 mcp_skill_gateway.enabled=false
```

---

## 15. IPC 契约

### 15.1 Preload API

文件：

```text
src/preload/mcp-skill-gateway-runtime-api.ts
```

接口：

```ts
export interface McpSkillGatewayRuntimeAPI {
  getStatus(): Promise<McpSkillGatewayRuntimeStatus>;
  getConfig(): Promise<McpSkillGatewayRuntimeConfig>;
  saveConfig(input: Partial<McpSkillGatewayRuntimeConfig>): Promise<McpSkillGatewayRuntimeConfig>;

  startProxy(): Promise<McpSkillGatewayActionResult>;
  stopProxy(): Promise<McpSkillGatewayActionResult>;
  restartProxy(): Promise<McpSkillGatewayActionResult>;

  testProxy(): Promise<McpGatewayProxyHealthResult>;
  testRemoteMcp(): Promise<McpGatewayRemoteTestResult>;

  registerToProfile(profile: string): Promise<McpGatewayProfileRegistration>;
  unregisterFromProfile(profile: string): Promise<McpGatewayProfileRegistration>;
  listProfileRegistrations(): Promise<McpGatewayProfileRegistration[]>;

  readProxyLogs(lines?: number): Promise<string>;
}
```

### 15.2 IPC Channels

```text
mcp-skill-gateway-runtime:get-status
mcp-skill-gateway-runtime:get-config
mcp-skill-gateway-runtime:save-config

mcp-skill-gateway-runtime:start-proxy
mcp-skill-gateway-runtime:stop-proxy
mcp-skill-gateway-runtime:restart-proxy

mcp-skill-gateway-runtime:test-proxy
mcp-skill-gateway-runtime:test-remote-mcp

mcp-skill-gateway-runtime:register-to-profile
mcp-skill-gateway-runtime:unregister-from-profile
mcp-skill-gateway-runtime:list-profile-registrations

mcp-skill-gateway-runtime:read-proxy-logs
```

---

## 16. 生命周期要求

### 16.1 App Ready

```text
1. hydrateTokenStore
2. readAuthEndpointConfig
3. 如果已登录且 mcp runtime enabled=true：
   - 启动 Local Proxy
4. 如果 autoRegisterToHermes=true：
   - 检查 default profile 注册状态
   - 若缺失则注册
```

### 16.2 登录成功

```text
1. writeStoredSession
2. updateTokenInjectionPolicy
3. 启动 Local Proxy
4. testProxy
5. testRemoteMcp
6. 如果 autoRegisterToHermes=true：
   - register default profile
7. 刷新 HermesMcpGatewayPage 状态
```

### 16.3 Logout

```text
1. POST /api/v1/auth/logout
2. clearStoredSession
3. clearPortalSession
4. 停止 Local Proxy
5. 保留 Hermes config.yaml
6. 页面显示 Not logged in
```

### 16.4 App Quit

```text
1. 停止 Local Proxy
2. 不修改 Hermes config.yaml
```

---

## 17. 错误码

```ts
export type McpBackendDesktopErrorCode =
  | "AUTH_BACKEND_NOT_CONFIGURED"
  | "AUTH_ACCOUNT_REQUIRED"
  | "AUTH_INVALID_RESPONSE"
  | "AUTH_MEMBER_REQUIRED"
  | "AUTH_ORG_REQUIRED"
  | "AUTH_ACCOUNT_DISABLED"
  | "MCP_GATEWAY_NOT_LOGGED_IN"
  | "MCP_GATEWAY_PROXY_PORT_IN_USE"
  | "MCP_GATEWAY_PROXY_START_FAILED"
  | "MCP_GATEWAY_PROXY_NOT_RUNNING"
  | "MCP_GATEWAY_BACKEND_MISMATCH"
  | "MCP_GATEWAY_REMOTE_UNREACHABLE"
  | "MCP_GATEWAY_REMOTE_UNAUTHORIZED"
  | "MCP_GATEWAY_REMOTE_FORBIDDEN"
  | "MCP_GATEWAY_CONFIG_WRITE_FAILED"
  | "MCP_GATEWAY_CONFIG_MISMATCH";
```

错误文案：

```text
AUTH_MEMBER_REQUIRED:
  当前账号不是组织成员，无法登录 Desktop。

AUTH_ORG_REQUIRED:
  当前账号未加入任何组织，无法登录 Desktop。

MCP_GATEWAY_BACKEND_MISMATCH:
  Proxy 后端与当前登录后端不一致，请重启 Proxy。

MCP_GATEWAY_CONFIG_MISMATCH:
  Hermes MCP 注册配置与当前 Proxy 不一致，请重新注册当前 Profile。
```

---

## 18. Cursor 实施任务

### Task 1：修改 Auth Contract

文件：

```text
src/shared/auth/auth-contract.ts
```

任务：

```text
1. LoginInput 增加 account
2. email 改为兼容字段
3. DesktopAuthUser 增加 nodeskclaw member 字段
4. toPublicState 保持兼容
```

验收：

```text
pnpm typecheck 通过
旧 email 登录调用不报 TS 错
```

---

### Task 2：修改 Auth URL

文件：

```text
src/shared/auth/auth-url.ts
```

任务：

```text
1. AuthUrlPath 增加 account-login
2. 默认 backendUrl 改为 http://192.168.0.118:4510
3. authPrefix 保持 /api/v1/auth
4. normalizeBackendBaseUrl 支持剥离 /api/v1/auth
```

---

### Task 3：新增 nodeskclaw Auth Response Parser

文件：

```text
src/main/auth/nodeskclaw-auth-response.ts
```

任务：

```text
1. 定义 NodeDeskClawApiResponse<T>
2. 定义 unwrapNodeDeskClawResponse<T>
3. 处理 code != 0
4. 处理 data 缺失
```

---

### Task 4：重写 HttpAuthClient

文件：

```text
src/main/auth/auth-client.ts
```

任务：

```text
1. login 改用 account-login
2. body 使用 { account, password }
3. 登录后调用 /me
4. 校验 current_org_id 与 portal_org_role
5. refresh 改为 refresh → me → assertMember
6. logout 继续调用 /logout
7. mapLoginResponse 改为 mapNodeDeskClawSession
```

---

### Task 5：修改 Login UI

搜索：

```text
desktopAuth.login
email
password
```

任务：

```text
1. 页面字段 email 改 account
2. placeholder 改为 邮箱 / 手机号 / 用户名
3. 调用 desktopAuth.login 时传 account
4. 错误提示按 nodeskclaw error_code 映射
```

---

### Task 6：MCP Runtime 去除独立 backendBaseUrl

文件：

```text
src/main/mcp-skill-gateway-runtime/mcp-skill-gateway-config.ts
```

任务：

```text
1. Runtime config 删除 backendBaseUrl
2. 读取历史配置时忽略 backendBaseUrl
3. 统一从 AuthEndpointConfig.backendUrl 计算 backendBaseUrl
4. 统一计算 remoteMcpUrl
```

---

### Task 7：修改 MCP Proxy

文件：

```text
src/main/mcp-skill-gateway-runtime/mcp-skill-gateway-proxy.ts
```

任务：

```text
1. GET /health 返回 backendBaseUrl / remoteMcpUrl / localMcpUrl
2. POST /mcp 从 token-store 读取 accessToken
3. POST /mcp 转发到 AuthEndpointConfig.backendUrl + /api/v1/hermes/mcp
4. 不记录 token
5. 远程 401 映射 -32013
6. 未登录映射 -32010
```

---

### Task 8：修改 Test Remote MCP

文件：

```text
src/main/mcp-skill-gateway-runtime/mcp-skill-gateway-ipc.ts
```

任务：

```text
1. test-remote-mcp 必须 POST 本地 Local Proxy URL
2. JSON-RPC method = tools/list
3. 返回 localProxyUrl / backendBaseUrl / remoteMcpUrl / toolCount
4. 不允许 Renderer 直连 backend
```

---

### Task 9：修改 Hermes 注册器

文件：

```text
src/main/mcp-skill-gateway-runtime/mcp-skill-gateway-register.ts
```

任务：

```text
1. 注册前检查 proxy health
2. backendMatched 必须为 true
3. config.yaml 只写 localMcpUrl
4. 注册后重新读取 config.yaml
5. 返回 urlMatched / backendMatched / ready
6. unregister 设置 enabled=false
```

---

### Task 10：修改 HermesMcpGatewayPage

文件：

```text
src/renderer/src/screens/Hermes/pages/McpGateway/HermesMcpGatewayPage.tsx
```

任务：

```text
1. Login 区显示 Backend URL = http://192.168.0.118:4510
2. 显示 memberVerified / portalOrgRole
3. Local Proxy 区显示 Remote MCP URL
4. Hermes registration 表格增加 Expected URL / URL matched / Backend matched / Ready
5. 增加 Consistency check 区
6. ready=false 时显示重新注册提示
```

---

### Task 11：生命周期接入

文件：

```text
src/main/index.ts
src/main/auth/auth-ipc.ts
```

任务：

```text
1. app ready 后恢复 token 并启动 proxy
2. 登录成功后启动 proxy
3. 登录成功后可自动注册 default profile
4. logout 后停止 proxy
5. before-quit 停止 proxy
```

---

### Task 12：测试

新增测试目录：

```text
src/main/auth/__tests__/
src/main/mcp-skill-gateway-runtime/__tests__/
```

测试用例：

```text
1. account-login 请求路径正确
2. nodeskclaw ApiResponse unwrap 正确
3. login 后 /me 校验 current_org_id
4. portal_org_role 缺失时拒绝登录
5. refresh 后会重新调用 /me
6. proxy health 返回 backendBaseUrl / remoteMcpUrl
7. testRemoteMcp 通过 local proxy 调 tools/list
8. register 写入 config.yaml 只包含 localMcpUrl
9. config.yaml 不包含 Authorization
10. backend mismatch 时 ready=false
```

---

## 19. 验收标准

### 19.1 登录验收

```text
1. Desktop 登录请求：
   POST http://192.168.0.118:4510/api/v1/auth/account-login

2. 登录成功后请求：
   GET http://192.168.0.118:4510/api/v1/auth/me

3. Login 页面显示：
   Backend URL = http://192.168.0.118:4510

4. 非组织 member 不能进入 Desktop。

5. refresh 后仍保持 member 校验。
```

### 19.2 MCP Proxy 验收

```text
1. Local Proxy 地址：
   http://127.0.0.1:48742/mcp

2. Proxy health 返回：
   backendBaseUrl = http://192.168.0.118:4510
   remoteMcpUrl = http://192.168.0.118:4510/api/v1/hermes/mcp

3. Test remote MCP 必须通过 Local Proxy。

4. 未登录时 /mcp 返回 JSON-RPC -32010。

5. Token 过期时 /mcp 返回 JSON-RPC -32013。
```

### 19.3 Hermes 注册验收

```text
1. default config.yaml 中写入：
   http://127.0.0.1:48742/mcp

2. config.yaml 不包含：
   Authorization
   Bearer
   accessToken
   refreshToken
   http://192.168.0.118:4510/api/v1/hermes/mcp

3. 页面显示：
   Registered = yes
   Enabled = yes
   URL matched = yes
   Backend matched = yes
   Ready = yes
```

### 19.4 一致性验收

```text
Auth backend:
  http://192.168.0.118:4510

Proxy backend:
  http://192.168.0.118:4510

Remote MCP:
  http://192.168.0.118:4510/api/v1/hermes/mcp

Hermes config URL:
  http://127.0.0.1:48742/mcp

最终状态：
  Ready
```

### 19.5 构建验收

```text
pnpm lint
pnpm typecheck
pnpm test
pnpm build
```

全部通过。

---

## 20. Cursor 执行提示

```text
请按 v6.4.1_mcp-backend-desktop PRD 实施。

关键目标：
1. Desktop 登录切换到 nodeskclaw member。
2. 当前 nodeskclaw backend 固定为 http://192.168.0.118:4510。
3. 登录使用 /api/v1/auth/account-login。
4. 登录后必须调用 /api/v1/auth/me 校验 current_org_id 和 portal_org_role。
5. AuthEndpointConfig.backendUrl 是唯一远程 backend 配置源。
6. MCP Proxy 远程转发目标必须从 AuthEndpointConfig.backendUrl 计算。
7. Test remote MCP 必须通过 Local Proxy 调 tools/list。
8. Hermes config.yaml 只能注册 http://127.0.0.1:48742/mcp。
9. Hermes config.yaml 不允许写 Authorization、Bearer、token。
10. HermesMcpGatewayPage 必须展示 Auth backend、Proxy backend、Remote MCP URL、Hermes config URL 的一致性状态。

实施顺序：
1. Auth Contract
2. Auth URL
3. nodeskclaw response parser
4. AuthClient account-login + /me 校验
5. Login UI
6. MCP Runtime config
7. MCP Proxy
8. Test remote MCP
9. Hermes register
10. HermesMcpGatewayPage
11. 生命周期
12. 测试与构建修复

验收命令：
pnpm lint
pnpm typecheck
pnpm test
pnpm build
```
