下面是 **第三阶段 Cursor 执行稿**。执行前提：第二阶段已完成 `MainPage + ShellView IPC 扩展 + WebOperatorScreen 使用 WebContentsHost`。

第三阶段目标：

```txt id="t5gvem"
Phase 3.1：ShellBrowserViewAdapter 替代 BrowserViewManager
Phase 3.2：browser.open 自动激活 MainPage 的 Web Operator tab
Phase 3.3：支持 external-browser 多 tab
Phase 3.4：Tab close / restore / reload
Phase 3.5：Tab DnD 排序
```

当前代码里 Web Operator 初始化仍然创建 `BrowserViewManager`，再传给 `BrowserController` 和 `BrowserIPC`。
`BrowserIPC` 仍通过 `browser.update_bounds` 调用 `viewManager.updateBounds()`。
第三阶段核心就是把这个单 View 管理器替换为 `ShellViewManager` 适配器。

---

# 一、第三阶段提交拆分

```txt id="txllss"
Commit 1：抽象 BrowserViewPort 接口
Commit 2：新增 ShellBrowserViewAdapter
Commit 3：Web Operator 初始化改用 ShellBrowserViewAdapter
Commit 4：browser.open 触发 MainPage 自动切换到 Web Operator
Commit 5：external-browser 多 tab
Commit 6：tab close / reload / restore
Commit 7：tab DnD 排序
```

---

# 二、Commit 1：抽象 BrowserViewPort 接口

## 新增文件

`src/main/browser/browser-viewport.ts`

```ts id="lr13ly"
import type { BrowserViewBounds } from "../../shared/browser/browser-contract";

export interface BrowserViewPort {
  createView(url: string): Promise<void>;
  navigate(url: string): Promise<void>;
  destroyView(): void;
  updateBounds(bounds: BrowserViewBounds): void;
  getExternalWebContents(): Electron.WebContents | null;
  isReady(): boolean;
  onBoundsUpdate?(callback: (bounds: BrowserViewBounds) => void): void;
}
```

## 修改 BrowserController

`src/main/browser/browser-controller.ts`

当前 `BrowserController` 直接 import `BrowserViewManager`。

替换：

```ts id="thksq5"
import { BrowserViewManager } from "./browser-view-manager";
```

为：

```ts id="h78epy"
import type { BrowserViewPort } from "./browser-viewport";
```

替换字段和 constructor：

```ts id="ecblfo"
private viewManager: BrowserViewPort;

constructor(
  viewManager: BrowserViewPort,
  securityGuard: BrowserSecurityGuard,
  auditLogger: BrowserAuditLogger,
) {
  this.viewManager = viewManager;
  this.securityGuard = securityGuard;
  this.auditLogger = auditLogger;
}
```

不改 `BrowserController` 其他业务逻辑。它当前只依赖：

```txt id="j7n9gk"
isReady()
getExternalWebContents()
createView()
navigate()
destroyView()
updateBounds()
```

这些方法全部由接口覆盖。

## 修改 BrowserIPC

`src/main/browser/browser-ipc.ts`

替换：

```ts id="q6m0oj"
import { BrowserViewManager } from "./browser-view-manager";
```

为：

```ts id="wmxqtu"
import type { BrowserViewPort } from "./browser-viewport";
```

替换字段：

```ts id="bj9zzj"
private viewManager: BrowserViewPort;

constructor(controller: BrowserController, viewManager: BrowserViewPort) {
  this.controller = controller;
  this.viewManager = viewManager;
}
```

保留所有旧 IPC channel，不重命名：

```txt id="lp8bad"
browser.open
browser.back
browser.forward
browser.reload
browser.get_state
browser.screenshot
browser.click
browser.type
browser.extract_table
browser.get_audit_log
browser.confirm_action
browser.reject_action
browser.update_bounds
```

---

# 三、Commit 2：新增 ShellBrowserViewAdapter

## 新增文件

`src/main/browser/shell-browser-view-adapter.ts`

```ts id="r6oknb"
import type { BrowserViewBounds } from "../../shared/browser/browser-contract";
import type { BrowserViewPort } from "./browser-viewport";
import type { ShellViewManager } from "../shell/views/shell-view-manager";

export const WEB_OPERATOR_LAYER_ID = "web-operator";

export class ShellBrowserViewAdapter implements BrowserViewPort {
  constructor(private readonly shellViewManager: ShellViewManager) {}

  async createView(url: string): Promise<void> {
    const existing = this.shellViewManager.getView(WEB_OPERATOR_LAYER_ID);

    if (!existing) {
      await this.shellViewManager.createView(
        WEB_OPERATOR_LAYER_ID,
        "web-operator",
        url,
        {
          layer: "content",
          partition: "persist:browser",
          sandbox: true,
          contextIsolation: true,
          nodeIntegration: false,
        },
      );
      return;
    }

    await existing.load(url);
  }

  async navigate(url: string): Promise<void> {
    await this.createView(url);
  }

  destroyView(): void {
    this.shellViewManager.destroyView(WEB_OPERATOR_LAYER_ID);
  }

  updateBounds(bounds: BrowserViewBounds): void {
    const existing = this.shellViewManager.getView(WEB_OPERATOR_LAYER_ID);
    if (!existing) return;

    if (bounds.width < 1 || bounds.height < 1) {
      this.shellViewManager.deactivateView(WEB_OPERATOR_LAYER_ID);
      return;
    }

    this.shellViewManager.activateView(WEB_OPERATOR_LAYER_ID, bounds);
  }

  getExternalWebContents(): Electron.WebContents | null {
    return (
      this.shellViewManager
        .getView(WEB_OPERATOR_LAYER_ID)
        ?.getWebContents() ?? null
    );
  }

  isReady(): boolean {
    return this.shellViewManager.getView(WEB_OPERATOR_LAYER_ID)?.isReady() ?? false;
  }
}
```

说明：

```txt id="q084g7"
1. layerId 固定为 web-operator
2. browser.open 默认操作 Web Operator 这个 View
3. WebContentsHost 继续负责 DOM bounds → shellView.setBounds
4. browser.update_bounds 仍然兼容，但底层转发到 ShellViewManager
```

`ShellViewManager` 已提供 `createView / activateView / destroyView / getView`。
`ManagedWebContentsView` 已提供 `load / focus / getWebContents / getState / getBounds / isActive`。

---

# 四、Commit 3：Web Operator 初始化改用 ShellBrowserViewAdapter

## 修改 main/index.ts import

当前 import：

```ts id="ehbftf"
import { BrowserViewManager } from "./browser/browser-view-manager";
```

替换为：

```ts id="awx9m6"
import { ShellBrowserViewAdapter } from "./browser/shell-browser-view-adapter";
```

保留旧文件 `browser-view-manager.ts`，暂不删除，避免回滚困难。

## 调整 shellViewManager 作用域

当前 `shellViewManager` 在 `if (mainWindow)` 内部局部声明，后面的 Web Operator 初始化段访问不到。

改为在初始化主流程上方声明：

```ts id="x9gcb4"
let shellViewManager: ShellViewManager | null = null;
```

然后 ShellView 初始化段改为：

```ts id="r6hjl0"
if (mainWindow) {
  try {
    shellViewManager = new ShellViewManager(mainWindow);
    console.log("[SHELL] ShellViewManager initialized");
  } catch (err) {
    console.error("[SHELL] Failed to initialize ShellViewManager:", err);
  }
}
```

## 修改 Web Operator 初始化段

原逻辑：

```ts id="jhy6kf"
const viewManager = new BrowserViewManager(mainWindow);
const securityGuard = new BrowserSecurityGuard(configPath);
const auditLogger = new BrowserAuditLogger(logDir);
const controller = new BrowserController(viewManager, securityGuard, auditLogger);
```

改为：

```ts id="x7oc62"
if (!shellViewManager) {
  throw new Error("ShellViewManager is required by Web Operator");
}

const viewManager = new ShellBrowserViewAdapter(shellViewManager);
const securityGuard = new BrowserSecurityGuard(configPath);
const auditLogger = new BrowserAuditLogger(logDir);
const controller = new BrowserController(viewManager, securityGuard, auditLogger);
controller.setMainWindow(mainWindow);

browserIPC = new BrowserIPC(controller, viewManager);
browserIPC.register();
```

保留：

```ts id="im13lf"
BrowserToolBridge
BrowserToolServer
auditLogger.onLog(...)
registerBrowserToolServerStop(...)
```

当前 `BrowserToolServer` 只依赖 `BrowserController`，不用改。

---

# 五、Commit 4：browser.open 自动激活 Web Operator tab

当前 renderer 的 `BrowserToolbar` 通过 `window.aiosBrowser.open({ url, source: "user" })` 打开网页。
`useBrowserActions` 也全部走 `window.aiosBrowser`。

需要让 `browser.open` 成功后通知 renderer 切换到 `web-operator` tab。

## 修改 shared contract

`src/shared/browser/browser-contract.ts`

新增：

```ts id="fo5nee"
export const BrowserEvents = {
  OPENED: "browser.opened",
} as const;

export interface BrowserOpenedEvent {
  url: string;
  layerId: "web-operator";
  source: BrowserActionSource;
}
```

`BrowserOpenRequest` 保持兼容，只增加可选字段：

```ts id="wejfda"
export interface BrowserOpenRequest {
  url: string;
  profile?: string;
  source: BrowserActionSource;
  activateTab?: boolean;
}
```

当前 `BrowserOpenRequest` 只有 `url / profile / source`，扩展为可选字段不会破坏旧调用。

## 修改 BrowserController

`src/main/browser/browser-controller.ts`

新增 import：

```ts id="ims49t"
import { BrowserEvents, type BrowserOpenedEvent } from "../../shared/browser/browser-contract";
import { WEB_OPERATOR_LAYER_ID } from "./shell-browser-view-adapter";
```

新增方法：

```ts id="s5o8k5"
private emitBrowserOpened(url: string, source: BrowserActionSource): void {
  if (!this.mainWindow || this.mainWindow.isDestroyed()) return;

  const event: BrowserOpenedEvent = {
    url,
    layerId: WEB_OPERATOR_LAYER_ID,
    source,
  };

  this.mainWindow.webContents.send(BrowserEvents.OPENED, event);
}
```

在 `openExternalUrl()` 成功后增加：

```ts id="rjqmq8"
this.emitBrowserOpened(request.url, request.source);
```

完整成功段应变成：

```ts id="tm2ayq"
await this.viewManager.createView(request.url);
this.auditLogger.log({
  source: request.source,
  action: "browser.open",
  url: request.url,
  status: "success",
});
this.emitBrowserOpened(request.url, request.source);
return { ok: true, url: request.url };
```

## 修改 preload/browser-api.ts

当前 preload API 已暴露 `open / back / forward / reload / getState / screenshot / click / type / extractTable / updateBounds` 等。

新增 import：

```ts id="celag0"
import type { BrowserOpenedEvent } from "../shared/browser/browser-contract";
import { BrowserEvents } from "../shared/browser/browser-contract";
```

接口新增：

```ts id="bmkr90"
onOpened(callback: (event: BrowserOpenedEvent) => void): () => void;
```

实现新增：

```ts id="ng6vfa"
onOpened: (callback) => {
  const handler = (_event: Electron.IpcRendererEvent, payload: BrowserOpenedEvent): void => {
    callback(payload);
  };
  ipcRenderer.on(BrowserEvents.OPENED, handler);
  return () => ipcRenderer.removeListener(BrowserEvents.OPENED, handler);
},
```

## 修改 Layout.tsx

在 `Layout` 内监听打开事件：

```tsx id="nm38cz"
useEffect(() => {
  try {
    return window.aiosBrowser.onOpened(() => {
      navigation.navigateToView("web-operator");
    });
  } catch {
    return undefined;
  }
}, [navigation.navigateToView]);
```

结果：

```txt id="pik9gl"
Hermes tool 调 browser.open
  → BrowserController.openExternalUrl
  → ShellBrowserViewAdapter.createView
  → BrowserController emit browser.opened
  → renderer Layout 收到事件
  → MainPage 切换到 web-operator tab
  → WebContentsHost 设置 bounds
  → ShellViewManager 激活 web-operator WebContentsView
```

---

# 六、Commit 5：external-browser 多 tab

## 修改 MainPage tab 类型

`src/renderer/src/screens/MainPage/main-page-types.ts`

新增：

```ts id="stmcwj"
export interface ExternalBrowserTab {
  id: `external-browser:${string}`;
  title: string;
  url: string;
  createdAt: number;
  updatedAt: number;
}
```

## 新增 hook

`src/renderer/src/screens/MainPage/useExternalBrowserTabs.ts`

```ts id="8kqwf9"
import { useCallback, useState } from "react";
import type { View } from "../../types/desktop-shell";
import type { ExternalBrowserTab } from "./main-page-types";

function createTabId(): `external-browser:${string}` {
  return `external-browser:${crypto.randomUUID()}`;
}

export function useExternalBrowserTabs() {
  const [tabs, setTabs] = useState<ExternalBrowserTab[]>([]);

  const openExternalTab = useCallback(async (url: string): Promise<View> => {
    const id = createTabId();
    const now = Date.now();

    const tab: ExternalBrowserTab = {
      id,
      title: new URL(url).hostname || "External",
      url,
      createdAt: now,
      updatedAt: now,
    };

    await window.shellView.create(id, "external-browser", url, {
      layer: "content",
      sandbox: true,
      contextIsolation: true,
      nodeIntegration: false,
    });

    setTabs((prev) => [...prev, tab]);
    return id;
  }, []);

  const closeExternalTab = useCallback(async (id: `external-browser:${string}`) => {
    await window.shellView.destroy(id);
    setTabs((prev) => prev.filter((tab) => tab.id !== id));
  }, []);

  const reloadExternalTab = useCallback(async (tab: ExternalBrowserTab) => {
    await window.shellView.loadUrl(tab.id, tab.url);
    setTabs((prev) =>
      prev.map((item) =>
        item.id === tab.id ? { ...item, updatedAt: Date.now() } : item,
      ),
    );
  }, []);

  return {
    externalTabs: tabs,
    openExternalTab,
    closeExternalTab,
    reloadExternalTab,
  };
}
```

## 修改 MainViewTabs

增加 `externalTabs` props：

```tsx id="pfc45v"
externalTabs: ExternalBrowserTab[];
onCloseTab: (id: View) => void;
onReloadTab: (id: View) => void;
```

渲染时合并：

```tsx id="okd3yz"
const tabs = [
  ...buildMainWorkspaceTabs(profileEntries),
  ...externalTabs.map((tab) => ({
    id: tab.id as View,
    title: tab.title,
    closeable: true,
    source: "external" as const,
  })),
];
```

tab item 增加关闭按钮：

```tsx id="jpvwll"
{tab.closeable ? (
  <button
    type="button"
    className="MainViewTabs__close"
    onClick={(event) => {
      event.stopPropagation();
      onCloseTab(tab.id);
    }}
  >
    ×
  </button>
) : null}
```

## 修改 WorkspaceOutlet

对 `external-browser:*` view 增加渲染：

```tsx id="czxlxl"
if (typeof view === "string" && view.startsWith("external-browser:")) {
  return (
    <WebContentsHost
      layerId={view}
      className="h-full w-full min-h-0"
    />
  );
}
```

`WebContentsHost` 已经能按 layerId 同步 bounds 到 `window.shellView.setBounds`。

---

# 七、Commit 6：Tab close / reload / restore

## MainTopBar actions 增加当前 tab 操作

`src/renderer/src/screens/MainPage/MainTopBar.tsx`

增加 props：

```ts id="g0k2v8"
onReloadActiveTab: () => void;
onCloseActiveTab: () => void;
canCloseActiveTab: boolean;
```

增加按钮：

```tsx id="mr6mg5"
<button type="button" title="Reload tab" onClick={onReloadActiveTab}>
  <RotateCw size={15} />
</button>

{canCloseActiveTab ? (
  <button type="button" title="Close tab" onClick={onCloseActiveTab}>
    <X size={15} />
  </button>
) : null}
```

## Layout.tsx 中实现

```tsx id="x21xgc"
const handleReloadActiveTab = useCallback(() => {
  const active = navigation.view;

  if (active === "web-operator") {
    void window.aiosBrowser.reload();
    return;
  }

  if (typeof active === "string" && active.startsWith("external-browser:")) {
    const tab = externalTabs.find((item) => item.id === active);
    if (tab) void reloadExternalTab(tab);
    return;
  }

  void window.shellView.loadUrl(active, "");
}, [navigation.view, externalTabs, reloadExternalTab]);
```

注意：`aios-home` 不建议直接 reload 空 URL，应专门使用 `shellView.getState()` 获取 url 再 reload。更稳的方式是第四阶段给 `shellView.reload(layerId)` 增加 IPC。

第三阶段先只支持：

```txt id="mni5ux"
web-operator reload
external-browser reload
```

## Close active tab

```tsx id="ne5wns"
const handleCloseActiveTab = useCallback(() => {
  const active = navigation.view;

  if (typeof active === "string" && active.startsWith("external-browser:")) {
    void closeExternalTab(active);
    navigation.navigateToView("aios-home");
  }
}, [navigation.view, closeExternalTab, navigation.navigateToView]);
```

---

# 八、Commit 7：Tab DnD 排序

当前项目没有 DnD 依赖。
第三阶段建议引入 `@dnd-kit`，不要引入 `react-beautiful-dnd`，原因是 React 19 兼容性和维护状态风险。

## 安装依赖

```bash id="v6llag"
npm install @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities
```

## 新增 tab order state

`MainPage` 或 `useExternalBrowserTabs` 内维护：

```ts id="m3h2tt"
const [tabOrder, setTabOrder] = useState<string[]>([]);
```

排序逻辑：

```ts id="m9j2ia"
function sortTabsByOrder<T extends { id: string }>(tabs: T[], order: string[]): T[] {
  const index = new Map(order.map((id, i) => [id, i]));
  return [...tabs].sort((a, b) => {
    const ai = index.get(a.id) ?? Number.MAX_SAFE_INTEGER;
    const bi = index.get(b.id) ?? Number.MAX_SAFE_INTEGER;
    return ai - bi;
  });
}
```

## DnD 边界

```txt id="w15x4d"
1. 只允许 external-browser tab 和 profile-workspace tab 排序
2. Portal / Workspace / Web Operator 固定在左侧
3. close button 必须 stopPropagation
4. drag handle 区域不能影响 window drag
5. .MainViewTabs 必须 no-drag
```

---

# 九、第三阶段验收标准

```txt id="b2zui3"
1. npm run typecheck 通过
2. npm run lint 通过
3. Web Operator 不再创建 BrowserViewManager
4. BrowserController 构造参数变为 BrowserViewPort 接口
5. browser.open 能通过 ShellBrowserViewAdapter 创建/加载 web-operator View
6. Hermes tool 调 browser.open 后，MainPage 自动切换到 Web Operator tab
7. BrowserToolbar 的 open/back/forward/reload/get_state/screenshot/click/type 仍可用
8. browser.update_bounds 旧 channel 保持可用
9. external-browser:* tab 可创建、切换、关闭
10. 关闭 external-browser tab 后，对应 ShellView 被 destroy
11. Web Operator tab 与 external-browser tab 不互相覆盖
12. DnD 排序只影响 tab 顺序，不影响 ShellView layerId
```

---

# 十、第三阶段禁止项

```txt id="b9fecz"
1. 禁止重命名 browser.* IPC channel
2. 禁止删除 BrowserViewManager 文件
3. 禁止让 BrowserController 直接依赖 ShellViewManager
4. 禁止把 external-browser tab 接入 Hermes browser tool 默认目标
5. 禁止一次性改动 BrowserSecurityGuard 策略
6. 禁止在 WebContentsView 中关闭 sandbox / contextIsolation
```

第三阶段完成后，主窗口结构进入目标状态：

```txt id="d6vwwx"
MainPage
  ├─ MainTopBar
  │   ├─ workspace tabs
  │   ├─ profile workspace tabs
  │   ├─ external-browser tabs
  │   └─ tab actions
  ├─ Sidebar 二级导航
  ├─ WorkspaceOutlet
  │   ├─ AIOSHomeScreen -> ShellViewManager: aios-home
  │   ├─ WebOperatorScreen -> ShellViewManager: web-operator
  │   ├─ ProfileWorkspace -> ShellViewManager: profile-page
  │   └─ ExternalBrowser -> ShellViewManager: external-browser:*
  └─ StatusBar
```

第三阶段的关键结果：**Browser Tool、Web Operator UI、多 WebView tabs 全部统一到 ShellViewManager，不再出现 BrowserViewManager 与 ShellViewManager 双轨运行。**
