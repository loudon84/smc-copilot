## v3.3 方案结论

采用 **组件与样式迁移 + 页面改造 + Portal WebContentsView 嵌入** 的混合方案可以落地，但 v3.3 的核心不是先改样式，而是先补齐 **Desktop Auth Endpoint Config + Main 侧 Token Vault + AIOS Home 专用 Header Injection**。

当前 `ai-os-desktop` 已经具备基础接入点：`preload/index.ts` 已暴露 `desktopAuth`、`desktopUserConfig`、`shellView` 等桥接对象；Main 侧已注册 `registerAuthIpc()`、`installTokenHeaderInjector()`，并在窗口创建后注册 `registerUserConfigIpc(mainWindow)` 与 `ShellViewManager`。这说明 v3.3 不需要推翻现有 IPC 架构，只需要补强 auth 配置、token 隔离、WebContentsView partition 策略。  

`ai-os-full/frontend` 是 Next 14 前端工程，依赖中已有 `next-auth`、`axios`、`react-hook-form`、`zod` 等可复用登录页相关基础能力；`tsconfig` 显示其使用 `app/[lang]` 多语言 App Router 结构。因此登录迁移应按“抽 UI 组件 + 适配 Desktop Auth Bridge”的方式做，不把 Web 端 token 存储逻辑原样迁入 Desktop Renderer。 

---

# Portal Desktop v3.3：Auth Config + Token Injection + AIOS Home Embed

## 1. 目标边界

### 1.1 本阶段目标

```text
v3.3 目标：
1. login 阶段支持配置 Portal backend URL / authPrefix / aiosHomeUrl。
2. 登录 token 只保存在 Electron Main 侧。
3. token 使用 safeStorage 或 keytar 加密落盘。
4. aios-home WebContentsView 加载时通过 session.webRequest 注入 request header。
5. token 只注入白名单 origin。
6. external-browser / web-operator / office / aios-workspace 不继承 aios-home token。
7. ai-os-full/frontend 登录 UI 迁移为 Desktop 登录组件，但不迁移其本地 token 存储模式。
```

### 1.2 不做内容

```text
v3.3 不做：
1. 不把 token 暴露给 Renderer。
2. 不使用 localStorage / sessionStorage / cookie 保存 token。
3. 不让 web-operator 共享 aios-home partition。
4. 不让 external-browser:* 自动带 Authorization。
5. 不改 Hermes Runtime 为页面路由。
6. 不引入 BFF / server proxy。
```

---

## 2. 启动阶段 Screen 流程

保留当前启动枚举，但 login 阶段升级为 Endpoint Config + Login 二合一页面。

```ts
type Screen =
  | "splash"
  | "login"
  | "welcome"
  | "installing"
  | "setup"
  | "main";
```

### 2.1 推荐流程

```text
App 启动
  ↓
splash
  - 读取本地 endpoint config
  - 读取 Main 侧 auth state
  - 读取 install / runtime 状态
  ↓
未配置 endpoint 或未登录
  → login

已登录但首次 bootstrap 未完成
  → welcome / installing / setup

已登录且 bootstrap 完成
  → main
```

### 2.2 login 阶段必须包含三个配置项

```ts
interface AuthEndpointConfig {
  backendUrl: string;   // 例：http://127.0.0.1:8000
  authPrefix: string;   // 例：/api/auth
  aiosHomeUrl: string;  // 例：http://127.0.0.1:3000
}
```

UI 表单字段：

```text
Portal backend URL
  - 必填
  - 必须是 http:// 或 https://
  - 默认：http://127.0.0.1:8000

authPrefix
  - 必填
  - 必须以 / 开头
  - 默认：/api/auth

aiosHomeUrl
  - 必填
  - 必须是 http:// 或 https://
  - 默认：http://127.0.0.1:3000
```

---

## 3. Token 保存与注入方案

### 3.1 Token 存储原则

```text
token 存储规则：
1. accessToken / refreshToken 只进入 Main Process。
2. Renderer 不允许读取 token。
3. Preload 不提供 getToken API。
4. TokenStore 使用 keytar 优先，safeStorage 兜底。
5. safeStorage 不可用时禁止明文落盘，只允许内存 session。
6. logout 必须清空加密 token、内存 token、注入缓存。
```

### 3.2 Renderer 可见的 AuthState

Renderer 只能拿到非敏感状态。

```ts
interface DesktopAuthState {
  authenticated: boolean;
  user?: {
    id: string;
    username: string;
    displayName?: string;
    tenantId?: string;
  };
  endpointConfig: AuthEndpointConfig | null;
  expiresAt?: string;
}
```

禁止返回：

```text
accessToken
refreshToken
idToken
apiKey
Authorization header value
```

---

## 4. Main 侧 Auth API 设计

### 4.1 修改文件

```text
src/shared/auth/auth-contract.ts
src/preload/auth-api.ts
src/main/auth/auth-ipc.ts
src/main/auth/auth-client.ts
src/main/auth/token-store.ts
src/main/auth/token-header-injector.ts
src/main/auth/auth-endpoint-config-store.ts     # 新增
src/main/auth/auth-url.ts                       # 新增
src/renderer/src/modules/auth/LoginScreen.tsx
src/renderer/src/modules/auth/AuthProvider.tsx
```

### 4.2 contract 扩展

```ts
export interface AuthEndpointConfig {
  backendUrl: string;
  authPrefix: string;
  aiosHomeUrl: string;
}

export interface LoginInput {
  endpointConfig: AuthEndpointConfig;
  username: string;
  password: string;
}

export interface DesktopAuthState {
  authenticated: boolean;
  endpointConfig: AuthEndpointConfig | null;
  user: {
    id: string;
    username: string;
    displayName?: string;
    tenantId?: string;
  } | null;
  expiresAt: string | null;
}

export interface DesktopAuthAPI {
  getState(): Promise<DesktopAuthState>;
  saveEndpointConfig(config: AuthEndpointConfig): Promise<AuthEndpointConfig>;
  login(input: LoginInput): Promise<DesktopAuthState>;
  logout(): Promise<DesktopAuthState>;
  refresh(): Promise<DesktopAuthState>;
}
```

### 4.3 preload 暴露限制

`src/preload/auth-api.ts` 只允许暴露：

```ts
export const authApi: DesktopAuthAPI = {
  getState: () => ipcRenderer.invoke("auth:get-state"),
  saveEndpointConfig: (config) =>
    ipcRenderer.invoke("auth:save-endpoint-config", config),
  login: (input) => ipcRenderer.invoke("auth:login", input),
  logout: () => ipcRenderer.invoke("auth:logout"),
  refresh: () => ipcRenderer.invoke("auth:refresh"),
};
```

不得新增：

```ts
getToken()
getAccessToken()
getRefreshToken()
getAuthorizationHeader()
```

---

## 5. AuthEndpointConfig 持久化

### 5.1 新增 `auth-endpoint-config-store.ts`

配置本身不是 token，可以明文保存，但必须集中管理，不能散落在 Renderer。

```ts
export interface StoredAuthEndpointConfig {
  backendUrl: string;
  authPrefix: string;
  aiosHomeUrl: string;
  updatedAt: string;
}

export function readAuthEndpointConfig(): StoredAuthEndpointConfig | null;

export function writeAuthEndpointConfig(
  input: AuthEndpointConfig,
): StoredAuthEndpointConfig;

export function getDefaultAuthEndpointConfig(): AuthEndpointConfig {
  return {
    backendUrl: "http://127.0.0.1:8000",
    authPrefix: "/api/auth",
    aiosHomeUrl: "http://127.0.0.1:3000",
  };
}
```

保存位置建议：

```text
app.getPath("userData")/auth-endpoint-config.json
```

---

## 6. Login URL 规范

新增 `src/main/auth/auth-url.ts`。

```ts
export function normalizeBaseUrl(url: string): string {
  return url.replace(/\/+$/, "");
}

export function normalizePrefix(prefix: string): string {
  const value = prefix.trim();
  if (!value.startsWith("/")) return `/${value}`;
  return value.replace(/\/+$/, "");
}

export function buildAuthUrl(
  config: AuthEndpointConfig,
  path: "login" | "refresh" | "me",
): string {
  return `${normalizeBaseUrl(config.backendUrl)}${normalizePrefix(
    config.authPrefix,
  )}/${path}`;
}
```

默认请求：

```text
POST {backendUrl}{authPrefix}/login
POST {backendUrl}{authPrefix}/refresh
GET  {backendUrl}{authPrefix}/me
```

Portal backend 需要提供兼容接口：

```ts
// POST /api/auth/login
interface LoginRequest {
  username: string;
  password: string;
}

interface LoginResponse {
  accessToken: string;
  refreshToken?: string;
  expiresAt?: string;
  tokenType?: "Bearer";
  user: {
    id: string;
    username: string;
    displayName?: string;
    tenantId?: string;
  };
}
```

---

## 7. User Config Contract 扩展

当前 `DesktopBootstrapConfig` 已包含 `features`、`hermes`、`aios.frontendUrl`、`aios.backendUrl` 等字段。v3.3 需要补齐 `authPrefix` 与 `aiosHomeUrl`，并保持旧字段兼容。

### 7.1 修改 `DesktopBootstrapConfig`

```ts
export interface DesktopBootstrapConfig {
  schemaVersion: 2;
  configVersion: string;
  configHash: string;

  user: {
    userId: string;
    username: string;
    displayName: string;
    tenantId: string;
    tenantName?: string;
  };

  features: {
    aiosHome: boolean;
    aiosWorkspace: boolean;
    webOperator: boolean;
    office: boolean;
    hermesRuntimeDrawer: boolean;
  };

  aios: {
    backendUrl: string;

    /**
     * v3.3 新字段
     */
    authPrefix: string;
    aiosHomeUrl: string;

    /**
     * 兼容旧字段。
     * 读取时：aiosHomeUrl ?? frontendUrl
     * 写入时：同时写 frontendUrl = aiosHomeUrl
     */
    frontendUrl?: string;

    workspacePath?: string;
    autoStart: boolean;
    env?: Record<string, string>;
    source?: {
      type: "git" | "zip" | "bundled";
      url?: string;
      branch?: string;
      localPath?: string;
    };
  };

  hermes: {
    activeProfile: string;
    profiles: Array<{
      name: string;
      enabled: boolean;
      soul?: string;
      user?: string;
      memory?: string;
      env?: Record<string, string>;
      config?: Record<string, unknown>;
    }>;
    models: Array<{
      name: string;
      provider: string;
      model: string;
      baseUrl: string;
      apiKeyRef?: string;
    }>;
    toolsets?: Record<string, boolean>;
    platforms?: Record<string, boolean>;
  };
}
```

### 7.2 兼容规则

```ts
function normalizeBootstrapConfig(
  config: DesktopBootstrapConfigV1 | DesktopBootstrapConfig,
): DesktopBootstrapConfig {
  return {
    ...config,
    schemaVersion: 2,
    aios: {
      ...config.aios,
      authPrefix: config.aios.authPrefix ?? "/api/auth",
      aiosHomeUrl: config.aios.aiosHomeUrl ?? config.aios.frontendUrl,
      frontendUrl: config.aios.aiosHomeUrl ?? config.aios.frontendUrl,
    },
  };
}
```

---

## 8. WebContentsView Partition 隔离

### 8.1 Partition 常量

修改：

```text
src/shared/shell/browser-partitions.ts
```

建议统一定义：

```ts
export const SHELL_PARTITIONS = {
  AIOS_HOME: "persist:aios-home",
  AIOS_WORKSPACE: "persist:aios-workspace",
  WEB_OPERATOR: "persist:web-operator",
  OFFICE: "persist:office",
  EXTERNAL_BROWSER_PREFIX: "persist:external-browser:",
} as const;

export function getExternalBrowserPartition(tabId: string): string {
  return `${SHELL_PARTITIONS.EXTERNAL_BROWSER_PREFIX}${tabId}`;
}
```

### 8.2 View 类型隔离

```text
aios-home
  partition = persist:aios-home
  可注入 token

aios-workspace
  partition = persist:aios-workspace
  不注入 token，除非后续单独定义 workspace auth

web-operator
  partition = persist:web-operator
  不注入 token

office
  partition = persist:office
  不注入 token

external-browser:*
  partition = persist:external-browser:<tabId>
  不注入 token
```

---

## 9. Token Header Injector

### 9.1 只绑定 aios-home session

修改：

```text
src/main/auth/token-header-injector.ts
```

核心规则：

```ts
import { session } from "electron";
import { SHELL_PARTITIONS } from "../../shared/shell/browser-partitions";

export function installTokenHeaderInjector(): void {
  const aiosHomeSession = session.fromPartition(SHELL_PARTITIONS.AIOS_HOME);

  aiosHomeSession.webRequest.onBeforeSendHeaders(
    { urls: ["http://*/*", "https://*/*"] },
    (details, callback) => {
      const policy = getCurrentInjectionPolicy();

      if (!policy.enabled) {
        callback({ requestHeaders: details.requestHeaders });
        return;
      }

      if (!isAllowedUrl(details.url, policy.allowedOrigins)) {
        callback({ requestHeaders: details.requestHeaders });
        return;
      }

      const token = getCachedAccessToken();
      if (!token) {
        callback({ requestHeaders: details.requestHeaders });
        return;
      }

      callback({
        requestHeaders: {
          ...details.requestHeaders,
          Authorization: `Bearer ${token}`,
        },
      });
    },
  );
}
```

### 9.2 白名单生成

```ts
export function buildAllowedOrigins(config: AuthEndpointConfig): string[] {
  const origins = new Set<string>();

  origins.add(new URL(config.aiosHomeUrl).origin);
  origins.add(new URL(config.backendUrl).origin);

  return Array.from(origins);
}
```

### 9.3 URL 判断

```ts
export function isAllowedUrl(url: string, allowedOrigins: string[]): boolean {
  try {
    const origin = new URL(url).origin;
    return allowedOrigins.includes(origin);
  } catch {
    return false;
  }
}
```

### 9.4 external-browser / web-operator 隔离验收

必须通过以下测试：

```text
1. 打开 aios-home：
   http://127.0.0.1:3000/*
   request header 包含 Authorization。

2. 打开 external-browser:http://127.0.0.1:3000：
   request header 不包含 Authorization。

3. 打开 web-operator 并访问 http://127.0.0.1:3000：
   request header 不包含 Authorization。

4. logout 后刷新 aios-home：
   request header 不包含 Authorization。

5. 修改 aiosHomeUrl 后：
   旧 origin 不再注入，新 origin 才注入。
```

---

## 10. AIOS Home 加载策略

### 10.1 aios-home 不再硬编码 3000

修改：

```text
src/main/aios/aios-config.ts
src/main/shell/views/view-registry.ts
src/main/shell/views/managed-webcontents-view.ts
src/renderer/src/screens/AIOSHome/AIOSHomeScreen.tsx
```

加载 URL 来源优先级：

```text
1. 本地已应用 DesktopBootstrapConfig.aios.aiosHomeUrl
2. AuthEndpointConfig.aiosHomeUrl
3. 默认 http://127.0.0.1:3000
```

### 10.2 加载前刷新注入策略

```ts
export async function beforeLoadAiosHome(): Promise<void> {
  const endpointConfig = readAuthEndpointConfig();
  const token = await tokenStore.readAccessToken();

  updateTokenInjectionPolicy({
    enabled: Boolean(endpointConfig && token),
    allowedOrigins: endpointConfig ? buildAllowedOrigins(endpointConfig) : [],
  });
}
```

ShellView 创建 `aios-home` 前执行：

```ts
await beforeLoadAiosHome();
await view.webContents.loadURL(aiosHomeUrl);
```

---

## 11. Login UI 迁移方案

### 11.1 从 ai-os-full 迁移的内容

迁移：

```text
1. 登录页布局
2. 输入框样式
3. 表单校验方式
4. loading / error 状态
5. logo / brand 区域
```

不迁移：

```text
1. next-auth session 写入逻辑
2. localStorage token
3. cookie token
4. Web 浏览器跳转逻辑
5. 服务端 Next route handler 登录逻辑
```

### 11.2 Desktop 登录页结构

```text
src/renderer/src/modules/auth/
  AuthProvider.tsx
  LoginGate.tsx
  LoginScreen.tsx
  components/
    EndpointConfigPanel.tsx
    LoginForm.tsx
    LoginBrandPanel.tsx
  styles/
    login.css
```

### 11.3 LoginScreen 行为

```text
LoginScreen
  ├─ EndpointConfigPanel
  │   ├─ backendUrl
  │   ├─ authPrefix
  │   └─ aiosHomeUrl
  ├─ LoginForm
  │   ├─ username
  │   └─ password
  └─ submit
      └─ window.desktopAuth.login({
           endpointConfig,
           username,
           password
         })
```

登录成功后：

```text
desktopAuth.login()
  ↓
Main AuthClient 请求 Portal backend
  ↓
TokenStore 加密保存 token
  ↓
updateTokenInjectionPolicy()
  ↓
desktopUserConfig.bootstrap()
  ↓
根据 bootstrap result 进入 welcome / installing / setup / main
```

---

## 12. Cursor 执行任务

### Task 1：补充 Auth Contract

```text
修改：
- src/shared/auth/auth-contract.ts

目标：
1. 增加 AuthEndpointConfig。
2. 增加 LoginInput。
3. 增加 DesktopAuthState。
4. 禁止 contract 中出现 token 返回字段。
```

验收：

```bash
pnpm typecheck
```

---

### Task 2：实现 AuthEndpointConfig Store

```text
新增：
- src/main/auth/auth-endpoint-config-store.ts
- src/main/auth/auth-url.ts

目标：
1. backendUrl / authPrefix / aiosHomeUrl 可保存。
2. 支持默认值。
3. 支持 URL normalize。
4. 支持 auth URL 拼接。
```

验收：

```text
1. authPrefix 输入 api/auth，保存为 /api/auth。
2. backendUrl 输入 http://127.0.0.1:8000/，保存为 http://127.0.0.1:8000。
3. aiosHomeUrl 输入 http://127.0.0.1:3000/，保存为 http://127.0.0.1:3000。
```

---

### Task 3：重构 TokenStore

```text
修改：
- src/main/auth/token-store.ts

目标：
1. keytar 优先。
2. safeStorage 兜底。
3. 不可加密时不明文落盘。
4. 提供内存缓存。
5. logout 清空全部 token。
```

必要接口：

```ts
export interface StoredAuthSession {
  accessToken: string;
  refreshToken?: string;
  expiresAt?: string;
  tokenType: "Bearer";
  user: {
    id: string;
    username: string;
    displayName?: string;
    tenantId?: string;
  };
}

export interface TokenStore {
  read(): Promise<StoredAuthSession | null>;
  write(session: StoredAuthSession): Promise<void>;
  clear(): Promise<void>;
  getCachedAccessToken(): string | null;
}
```

---

### Task 4：实现 AuthClient 登录

```text
修改：
- src/main/auth/auth-client.ts

目标：
1. 使用 endpointConfig 构建 login URL。
2. POST {backendUrl}{authPrefix}/login。
3. 成功后写入 TokenStore。
4. 返回 DesktopAuthState。
5. 不返回 token。
```

---

### Task 5：扩展 Auth IPC

```text
修改：
- src/main/auth/auth-ipc.ts
- src/preload/auth-api.ts

目标：
1. auth:get-state
2. auth:save-endpoint-config
3. auth:login
4. auth:logout
5. auth:refresh
```

验收：

```text
window.desktopAuth.getState() 返回 authenticated / user / endpointConfig。
返回对象中不能包含 accessToken / refreshToken。
```

---

### Task 6：重构 token-header-injector

```text
修改：
- src/main/auth/token-header-injector.ts
- src/shared/shell/browser-partitions.ts

目标：
1. 只监听 persist:aios-home。
2. 只对白名单 origin 注入 Authorization。
3. external-browser / web-operator 不注入。
4. logout 后立即停止注入。
```

---

### Task 7：改造 aios-home URL 来源

```text
修改：
- src/main/aios/aios-config.ts
- src/main/shell/views/view-registry.ts
- src/main/shell/views/managed-webcontents-view.ts
- src/renderer/src/screens/AIOSHome/AIOSHomeScreen.tsx

目标：
1. aios-home URL 从 auth/user config 读取。
2. 不再硬编码 http://127.0.0.1:3000。
3. loadURL 前刷新 token injection policy。
```

---

### Task 8：扩展 User Config Contract

```text
修改：
- src/shared/user-config/user-config-contract.ts
- src/main/user-config/user-config-client.ts
- src/main/user-config/user-config-bootstrap.ts
- src/main/user-config/user-config-applier.ts

目标：
1. DesktopBootstrapConfig.schemaVersion 升级到 2。
2. aios 增加 authPrefix / aiosHomeUrl。
3. 兼容旧 frontendUrl。
4. 首次登录允许远程配置覆盖。
5. 后续登录必须 diff 确认。
```

---

### Task 9：迁移 Login UI

```text
修改：
- src/renderer/src/modules/auth/LoginScreen.tsx
- src/renderer/src/modules/auth/AuthProvider.tsx

新增：
- src/renderer/src/modules/auth/components/EndpointConfigPanel.tsx
- src/renderer/src/modules/auth/components/LoginForm.tsx
- src/renderer/src/modules/auth/components/LoginBrandPanel.tsx

目标：
1. 合并 endpoint config 与 login 表单。
2. 登录调用 desktopAuth.login。
3. 成功后调用 desktopUserConfig.bootstrap。
4. 根据 bootstrap result 切 screen。
```

---

## 13. 验收清单

### 13.1 登录配置验收

```text
1. 首次启动进入 login。
2. login 页面可配置 backendUrl / authPrefix / aiosHomeUrl。
3. 配置保存后重启仍存在。
4. 登录成功后进入 main。
5. 登录失败只展示错误，不保存 token。
```

### 13.2 Token 安全验收

```text
1. Renderer DevTools 执行 window.desktopAuth.getState() 看不到 token。
2. localStorage / sessionStorage / document.cookie 中没有 token。
3. app userData 目录中没有明文 token。
4. logout 后加密 token 被清除。
5. safeStorage/keytar 不可用时不允许明文持久化 token。
```

### 13.3 Header 注入验收

```text
1. aios-home 请求带 Authorization: Bearer xxx。
2. external-browser 请求同一个 URL 不带 Authorization。
3. web-operator 请求同一个 URL 不带 Authorization。
4. office 不带 Authorization。
5. aiosHomeUrl 切换后旧 origin 不再注入。
```

### 13.4 Bootstrap 验收

```text
1. 首次登录后拉取 Portal backend 用户配置。
2. 首次配置允许覆盖本地。
3. 后续远程配置变化必须展示 diff。
4. 用户确认后才应用 diff。
5. 拒绝 diff 后继续使用本地配置。
```

---

## 14. 最终落地顺序

```text
1. Auth contract
2. Endpoint config store
3. TokenStore 加密
4. AuthClient 登录
5. Auth IPC / preload
6. token-header-injector partition 隔离
7. aios-home URL 动态加载
8. user-config schema v2
9. Login UI 迁移
10. 验收测试
```

v3.3 完成后，`aios-home` 可以作为受控的 Portal WebContentsView 嵌入；登录态由 Desktop Main 侧持有，Portal 页面通过请求头获得身份信息，且不会污染 `external-browser` 与 `web-operator`。
