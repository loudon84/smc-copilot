## 结论

现有 `ai-os-desktop` 已经具备 multi profiles 的基础骨架：SQLite 表、Profile Runtime Manager、Hermes Local Adapter、IPC、Profile Entry、Capability、Skill、Delegation、Session Share 都已存在。下一阶段不要重做模块，应在现有 `profile-runtime` 体系上补齐 **角色库导入、Profile 预设、角色文件生成、Hermes Gateway 真隔离启动、UI 管理入口**。

核心改造点：

1. 保留现有 `profiles / runtime_instances / profile_entries / profile_capabilities / profile_skills` 结构。
2. 新增 `profile_role_specs` 表，记录角色来源、源文件、角色版本、checksum。
3. 新增 `resources/profile-presets/hermes-expert-profiles.v1.yaml`，一次性创建 6 个专家 profile。
4. 新增 `role-library-sync / role-compiler`，从 `agency-agents-zh` 拉取角色源并生成每个 profile 的 `SOUL.md / MEMORY.md / profile-role.json`。
5. 修正 `hermes-local-adapter`，启动时必须让每个 Hermes Gateway 读取自己的 profile home，否则多端口不等于多角色隔离。
6. Settings Drawer 增加 Multi Profiles 管理页，支持安装预设、查看角色来源、启动/停止、同步角色、查看日志。

---

## 1. 当前 ai-os-desktop multi profiles 源码判断

当前项目已经有 SQLite profile runtime 体系。`profile-runtime-db.ts` 中已经定义了 `profiles`、`runtime_instances`、`profile_entries`、`profile_capabilities`、`profile_skills`、`skill_sync_events`、`shared_contexts`、`delegation_events`、`audit_events` 等表，适合作为 multi profiles 的持久层基础。

现有 Runtime Manager 已实现 profile 状态机、端口占用检查、启动超时、启动/停止/重启/批量启动能力，下一阶段只需要增强角色创建和启动隔离，不需要推翻。

现有 `config-importer.ts` 已经支持从 YAML 导入 profiles，校验 profile 名称、端口、runtimeType、capabilities，并自动创建 profile 目录、`skills / memories / desktop / desktop/shared-context` 子目录和 `SOUL.md`。

现有 IPC 已暴露 `importConfig`、`listProfiles`、`startProfile`、`stopProfile`、`restartProfile`、`startAll`、`stopAll`、`delegate`、`copySkill`、`shareSessionContext`、`getGatewayLogs` 等接口，足够支撑 Settings Drawer 的 multi profiles UI。

主要风险在 `hermes-local-adapter.ts`：当前会向每个 profile home 写 `config.yaml` 中的 `api_server` 端口，并读取 profile home 下的 `.env`，但启动命令仍是 `spawn(HERMES_SCRIPT, ["gateway"], { cwd: HERMES_REPO, env })`，没有显式设置 `HERMES_HOME=profileHome` 或 Hermes profile 参数。这样会导致 Gateway 可能仍读取默认 Hermes 配置，无法保证每个 profile 的 `SOUL.md / config.yaml / skills` 被真实隔离加载。

此外，`config-importer.ts` 的 overwrite 分支调用 `require("./profile-runtime-db").getDb()`，但 `profile-runtime-db.ts` 中 `getDb()` 是内部函数，没有导出。后续如果开启覆盖导入，会有运行时风险。

---

## 2. agency-agents-zh 角色库取用方式

`agency-agents-zh` 是一个中文 AI 角色库，README 中说明其覆盖工程、设计、营销、金融等部门，并且不是普通提示词模板，而是带有身份定义、工作流程和交付物的角色定义。([GitHub][1])

该仓库明确支持安装到 Hermes Agent，工具列表中 Hermes Agent 的安装位置为 `~/.hermes/skills/`，并提示部分工具需要先执行格式转换。([GitHub][1])

对本项目的正确用法不是直接把整个仓库复制进 Hermes，而是：

```text
agency-agents-zh 原始角色源
        ↓
role-library-sync 拉取 / 更新
        ↓
role-compiler 选取指定目录或文件
        ↓
生成每个 Hermes profile 的：
  - SOUL.md
  - MEMORY.md
  - profile-role.json
  - skills/role-source/*.md
        ↓
profile-runtime-db 记录 role source / checksum / version
```

---

## 3. 目标 Profile 清单

按用户给定 profile 名称保留，不自动纠正 `hurman-9621`，避免后续配置、端口、目录、任务分派不一致。

| Profile         | Port | Display Name | Role Source               | 角色定位                 |
| --------------- | ---: | ------------ | ------------------------- | -------------------- |
| `writer-9601`   | 9601 | 写作生文专家       | `marketing`               | 模板创作、一键生文、多平台内容改写    |
| `engineer-9612` | 9612 | 智能体工程专家      | `engineering`             | skill、代码、工具、集成方案生产   |
| `research-9602` | 9602 | 数据研究专家       | `strategy` + `paid-media` | 市场情报、数据研究、搜索词/投放数据分析 |
| `hurman-9621`   | 9621 | 招聘专家         | `hr`                      | JD、简历筛选、面试流、招聘数据看板   |
| `finance-9631`  | 9631 | 财经专家         | `finance`                 | 财务分析、模型、预算、经营分析      |
| `sales-9641`    | 9641 | 销售专家         | `sales`                   | 客户拓展、QBR、客户健康、销售增长   |

角色源选型依据：

* `writer-9601` 使用 `marketing-content-creator.md`，该角色覆盖内容策略、多平台创作、内容运营、内容日历和内容模板，符合“模板创作、一键生文”。
* `research-9602` 的 `strategy/playbooks/phase-0-discovery.md` 包含趋势研究员、反馈分析师、UX 研究员、数据分析师、合规扫描、工具评估师等发现阶段角色编排，适合作为数据研究 profile 的战略研究底座。
* `research-9602` 叠加 `paid-media-search-query-analyst.md`，该角色覆盖搜索词报告、N-gram、意图分类、浪费识别、机会挖掘，适合投放和市场数据分析。
* `hurman-9621` 使用 `hr-recruiter.md`，覆盖招聘需求分析、渠道运营、简历筛选、面试协调、Offer 与入职闭环。
* `finance-9631` 使用 `finance-financial-analyst.md`，覆盖财务建模、预测、场景分析、差异分析、KPI 仪表盘。
* `sales-9641` 使用 `sales-account-strategist.md`，覆盖 Land-and-Expand、QBR、干系人关系图谱、客户健康和 NRR。

---

# Multi Profiles 优化方案 v1.0

## 4. 新增目录结构

```text
resources/
  profile-presets/
    hermes-expert-profiles.v1.yaml

src/main/profile-roles/
  role-library-sync.ts
  role-compiler.ts
  role-preset-installer.ts
  role-spec-db.ts
  role-file-writer.ts

src/shared/profile-roles/
  profile-role-contract.ts

src/renderer/features/settings/multi-profiles/
  MultiProfilesPanel.tsx
  ProfilePresetInstallCard.tsx
  ProfileRoleSourceView.tsx
  ProfileRuntimeActions.tsx
  ProfileLogViewer.tsx
```

---

## 5. 新增数据库表

新增 migration version：`CURRENT_SCHEMA_VERSION = 3`

```sql
CREATE TABLE IF NOT EXISTS profile_role_specs (
  id TEXT PRIMARY KEY,
  profile_id TEXT NOT NULL,
  role_key TEXT NOT NULL,
  role_name TEXT NOT NULL,
  role_source_repo TEXT NOT NULL,
  role_source_paths_json TEXT NOT NULL,
  role_summary TEXT,
  role_manifest_path TEXT NOT NULL,
  soul_path TEXT NOT NULL,
  memory_path TEXT,
  source_checksum TEXT NOT NULL,
  installed_at TEXT NOT NULL,
  updated_at TEXT NOT NULL,
  FOREIGN KEY(profile_id) REFERENCES profiles(id) ON DELETE CASCADE,
  UNIQUE(profile_id, role_key)
);

CREATE INDEX IF NOT EXISTS idx_profile_role_specs_profile
ON profile_role_specs(profile_id);

CREATE INDEX IF NOT EXISTS idx_profile_role_specs_key
ON profile_role_specs(role_key);
```

不建议把这些字段塞进 `profiles.description` 或 `config_json`，原因是角色源、checksum、同步状态属于可审计数据，应独立持久化。

---

## 6. Profile Preset YAML

新增文件：

```text
resources/profile-presets/hermes-expert-profiles.v1.yaml
```

内容：

```yaml
version: 1

runtime:
  defaultAdapter: hermes-local

gateway:
  host: 127.0.0.1
  healthPath: /health

roleLibrary:
  provider: github
  repo: https://github.com/jnMetaCode/agency-agents-zh.git
  branch: main
  localDir: agency-agents-zh

profiles:
  writer-9601:
    displayName: 写作生文专家
    role: 模板创作与一键生文专家
    runtimeType: hermes-local
    enabled: true
    autoStart: false
    port: 9601
    model: default
    entry:
      type: specialist-workspace
      route: profile/writer-9601
      title: 写作生文
      icon: pen-tool
    capabilities:
      - delegation
      - skill-sync
      - session-share
      - gateway-supervisor
    roleSpec:
      roleKey: writer
      roleName: 写作生文专家
      sourceRepo: https://github.com/jnMetaCode/agency-agents-zh
      sourcePaths:
        - marketing/marketing-content-creator.md
      outputMode: soul-memory-skill

  engineer-9612:
    displayName: 智能体工程专家
    role: Skill、代码与工具生产专家
    runtimeType: hermes-local
    enabled: true
    autoStart: false
    port: 9612
    model: default
    entry:
      type: specialist-workspace
      route: profile/engineer-9612
      title: 智能体工程
      icon: code-2
    capabilities:
      - delegation
      - skill-sync
      - session-share
      - gateway-supervisor
    roleSpec:
      roleKey: engineer
      roleName: 智能体工程专家
      sourceRepo: https://github.com/jnMetaCode/agency-agents-zh
      sourcePaths:
        - engineering/engineering-ai-engineer.md
        - engineering/engineering-software-architect.md
      outputMode: soul-memory-skill

  research-9602:
    displayName: 数据研究专家
    role: 市场情报、数据研究与投放分析专家
    runtimeType: hermes-local
    enabled: true
    autoStart: false
    port: 9602
    model: default
    entry:
      type: specialist-workspace
      route: profile/research-9602
      title: 数据研究
      icon: chart-line
    capabilities:
      - delegation
      - skill-sync
      - session-share
      - gateway-supervisor
    roleSpec:
      roleKey: research
      roleName: 数据研究专家
      sourceRepo: https://github.com/jnMetaCode/agency-agents-zh
      sourcePaths:
        - strategy/playbooks/phase-0-discovery.md
        - paid-media/paid-media-search-query-analyst.md
      outputMode: soul-memory-skill

  hurman-9621:
    displayName: 招聘专家
    role: 招聘全流程与人才获取专家
    runtimeType: hermes-local
    enabled: true
    autoStart: false
    port: 9621
    model: default
    entry:
      type: specialist-workspace
      route: profile/hurman-9621
      title: 招聘专家
      icon: users
    capabilities:
      - delegation
      - skill-sync
      - session-share
      - gateway-supervisor
    roleSpec:
      roleKey: hurman
      roleName: 招聘专家
      sourceRepo: https://github.com/jnMetaCode/agency-agents-zh
      sourcePaths:
        - hr/hr-recruiter.md
      outputMode: soul-memory-skill

  finance-9631:
    displayName: 财经专家
    role: 财务分析、经营模型与预算预测专家
    runtimeType: hermes-local
    enabled: true
    autoStart: false
    port: 9631
    model: default
    entry:
      type: specialist-workspace
      route: profile/finance-9631
      title: 财经专家
      icon: banknote
    capabilities:
      - delegation
      - skill-sync
      - session-share
      - gateway-supervisor
    roleSpec:
      roleKey: finance
      roleName: 财经专家
      sourceRepo: https://github.com/jnMetaCode/agency-agents-zh
      sourcePaths:
        - finance/finance-financial-analyst.md
      outputMode: soul-memory-skill

  sales-9641:
    displayName: 销售专家
    role: 客户拓展、QBR 与销售增长专家
    runtimeType: hermes-local
    enabled: true
    autoStart: false
    port: 9641
    model: default
    entry:
      type: specialist-workspace
      route: profile/sales-9641
      title: 销售专家
      icon: handshake
    capabilities:
      - delegation
      - skill-sync
      - session-share
      - gateway-supervisor
    roleSpec:
      roleKey: sales
      roleName: 销售专家
      sourceRepo: https://github.com/jnMetaCode/agency-agents-zh
      sourcePaths:
        - sales/sales-account-strategist.md
      outputMode: soul-memory-skill
```

---

## 7. 角色文件生成规则

每个 profile 安装完成后目录结构：

```text
~/.hermes/profiles/{profileName}/
  .env
  config.yaml
  SOUL.md
  MEMORY.md
  profile-role.json
  skills/
    role-source/
      agency-agents-zh/
        *.md
  memories/
  desktop/
    shared-context/
```

`SOUL.md` 不直接原样塞入全部角色库内容，而是生成一个压缩后的 profile 指令：

```md
# 写作生文专家

## 身份
你是 Hermes Desktop 中的写作生文专家，负责模板创作、一键生文、多平台内容改写、内容复用矩阵和发布复盘。

## 角色来源
- agency-agents-zh/marketing/marketing-content-creator.md

## 工作边界
- 只处理写作、生文、模板、选题、内容复用、文案优化类任务。
- 涉及代码实现时，应委派给 engineer-9612。
- 涉及市场数据研究时，应委派给 research-9602。
- 涉及财务测算时，应委派给 finance-9631。
- 涉及客户拓展时，应委派给 sales-9641。

## 默认交付物
- 内容模板
- 生文提示词
- 长文大纲
- 多平台改写版本
- 内容日历
- 数据复盘清单
```

`profile-role.json` 示例：

```json
{
  "profile": "writer-9601",
  "port": 9601,
  "roleKey": "writer",
  "roleName": "写作生文专家",
  "sourceRepo": "https://github.com/jnMetaCode/agency-agents-zh",
  "sourcePaths": ["marketing/marketing-content-creator.md"],
  "generatedFiles": ["SOUL.md", "MEMORY.md", "skills/role-source/agency-agents-zh/marketing-content-creator.md"],
  "checksum": "<sha256>",
  "installedAt": "<iso-date>"
}
```

---

## 8. Hermes Local Adapter 必须修正

文件：

```text
src/main/hermes-local-adapter.ts
```

当前启动代码需要补充 profile home 环境隔离。

目标修改：

```ts
const env = { ...process.env } as Record<string, string>;

env.HERMES_HOME = home;
env.HERMES_PROFILE = profile.name;
env.HERMES_PROFILE_HOME = home;
env.HERMES_GATEWAY_HOST = instance.host;
env.HERMES_GATEWAY_PORT = String(instance.port);
```

同时 stop 阶段必须使用当前 profile home：

```ts
const pidPath = join(profileHome(profile.name), "gateway.pid");
```

而不是：

```ts
const pidPath = join(profileHome(), "gateway.pid");
```

否则停止非 default profile 时可能误读 default pid 文件。

---

## 9. Config Importer 改造

文件：

```text
src/main/config-importer.ts
```

新增 `roleSpec` 解析：

```ts
interface ParsedRoleSpec {
  roleKey: string;
  roleName: string;
  sourceRepo: string;
  sourcePaths: string[];
  outputMode?: "soul-memory-skill";
}

interface ParsedProfile {
  displayName: string;
  role?: string;
  runtimeType: string;
  enabled?: boolean;
  autoStart?: boolean;
  port: number;
  model?: string;
  entry?: ParsedProfileEntry;
  capabilities?: string[];
  soulPrompt?: string;
  roleSpec?: ParsedRoleSpec;
}
```

导入流程调整为：

```text
parse YAML
  ↓
validate profile
  ↓
create profile/runtime/entry/capability
  ↓
if roleSpec exists:
  sync role library
  compile role files
  write SOUL.md / MEMORY.md / profile-role.json
  copy source md into skills/role-source
  insert profile_role_specs
  insert profile_skills
  insert audit_events
```

必须删除 overwrite 分支中对未导出 `getDb()` 的调用。替代方案：

```ts
export function deleteProfileCascade(id: string): boolean
```

在 `profile-runtime-db.ts` 中明确导出，而不是通过 `require().getDb()` 访问内部函数。

---

## 10. 新增 Role Sync 模块

### `src/main/profile-roles/role-library-sync.ts`

职责：

```text
- 检查本地 role library 是否存在
- 不存在则 git clone
- 存在则 git fetch + reset 或 git pull
- 返回本地 repo 路径
- 记录 sync audit event
```

本地路径：

```text
{HERMES_HOME}/desktop/role-library/agency-agents-zh
```

最小接口：

```ts
export interface RoleLibraryRef {
  repo: string;
  branch?: string;
  localDir?: string;
}

export interface SyncRoleLibraryResult {
  ok: boolean;
  localPath: string;
  commit?: string;
  error?: string;
}

export async function syncRoleLibrary(input: RoleLibraryRef): Promise<SyncRoleLibraryResult>;
```

---

## 11. 新增 Role Compiler

### `src/main/profile-roles/role-compiler.ts`

职责：

```text
- 读取 roleSpec.sourcePaths
- 计算 checksum
- 生成 SOUL.md
- 生成 MEMORY.md
- 生成 profile-role.json
- 输出 source markdown 到 skills/role-source
```

最小接口：

```ts
export interface CompileProfileRoleInput {
  profileName: string;
  displayName: string;
  port: number;
  profileHome: string;
  roleKey: string;
  roleName: string;
  sourceRepo: string;
  sourceRoot: string;
  sourcePaths: string[];
}

export interface CompileProfileRoleResult {
  soulPath: string;
  memoryPath: string;
  manifestPath: string;
  checksum: string;
  copiedSourceFiles: string[];
}

export function compileProfileRole(input: CompileProfileRoleInput): CompileProfileRoleResult;
```

---

## 12. Settings Drawer UI 改造

新增入口：

```text
TopBar Settings Drawer
  └─ Hermes Runtime
      └─ Multi Profiles
```

页面结构：

```text
MultiProfilesPanel
  ├─ ProfilePresetInstallCard
  │   ├─ 安装 6 个专家 Profiles
  │   ├─ 覆盖安装开关
  │   └─ 检查端口冲突
  │
  ├─ ProfileRuntimeActions
  │   ├─ Start
  │   ├─ Stop
  │   ├─ Restart
  │   ├─ Start All
  │   └─ Stop All
  │
  ├─ ProfileRoleSourceView
  │   ├─ profileName
  │   ├─ roleName
  │   ├─ sourceRepo
  │   ├─ sourcePaths
  │   ├─ checksum
  │   └─ sync/update action
  │
  └─ ProfileLogViewer
      ├─ gateway logs
      ├─ audit events
      └─ role sync events
```

IPC 增补：

```ts
profile-role:syncLibrary
profile-role:installPreset
profile-role:getSpec
profile-role:listSpecs
profile-role:recompile
```

---

## 13. Cursor 执行任务清单

### Task 1：数据库 migration v3

修改：

```text
src/main/profile-runtime-db.ts
src/shared/profile-roles/profile-role-contract.ts
```

验收：

```text
- profile-runtime.db 自动迁移到 version 3
- 新增 profile_role_specs 表
- list / insert / update / getByProfileId 方法可用
- 不破坏 version 1/2 表
```

---

### Task 2：修正 Hermes Local Adapter 隔离启动

修改：

```text
src/main/hermes-local-adapter.ts
```

验收：

```text
- 启动 writer-9601 时读取 ~/.hermes/profiles/writer-9601/config.yaml
- 启动 finance-9631 时读取 ~/.hermes/profiles/finance-9631/config.yaml
- stopProfile 不再读取 default gateway.pid
- 6 个 profile 可分别占用 9601/9612/9602/9621/9631/9641
```

---

### Task 3：新增 role-library-sync

新增：

```text
src/main/profile-roles/role-library-sync.ts
```

验收：

```text
- 首次安装自动 clone agency-agents-zh
- 已存在时可以更新
- sync 失败时返回明确错误，不影响已有 profile 运行
```

---

### Task 4：新增 role-compiler

新增：

```text
src/main/profile-roles/role-compiler.ts
src/main/profile-roles/role-file-writer.ts
```

验收：

```text
- 每个 profile 生成 SOUL.md
- 每个 profile 生成 MEMORY.md
- 每个 profile 生成 profile-role.json
- source markdown 被复制到 skills/role-source/agency-agents-zh
- checksum 可复算
```

---

### Task 5：扩展 config-importer

修改：

```text
src/main/config-importer.ts
resources/profile-presets/hermes-expert-profiles.v1.yaml
```

验收：

```text
- importConfigContent 可以导入 6 个 profile
- 端口冲突时明确返回 PROFILE_PORT_CONFLICT
- 已存在 profile 且 overwrite=false 时不覆盖
- overwrite=true 时走显式 deleteProfileCascade，不访问未导出的 getDb
```

---

### Task 6：新增 profile-role IPC

新增：

```text
src/main/profile-role-ipc.ts
```

修改：

```text
src/main/index.ts
src/preload/index.ts
src/shared/profile-roles/profile-role-contract.ts
```

验收：

```text
- renderer 可调用 installPreset
- renderer 可查看每个 profile 的 roleSpec
- renderer 可触发 recompile
- renderer 不直接访问文件系统
```

---

### Task 7：Settings Drawer UI

新增：

```text
src/renderer/features/settings/multi-profiles/MultiProfilesPanel.tsx
src/renderer/features/settings/multi-profiles/ProfilePresetInstallCard.tsx
src/renderer/features/settings/multi-profiles/ProfileRoleSourceView.tsx
src/renderer/features/settings/multi-profiles/ProfileRuntimeActions.tsx
src/renderer/features/settings/multi-profiles/ProfileLogViewer.tsx
```

验收：

```text
- Settings Drawer 中出现 Multi Profiles 页面
- 可安装 6 个专家 profiles
- 可 Start / Stop / Restart 单个 profile
- 可 Start All / Stop All
- 可查看 role source、checksum、日志
```

---

## 14. 最终验收命令

```bash
npm run typecheck
npm run lint
npm run build
npm run test
```

手工验收：

```bash
# 启动应用后，在 Settings Drawer 安装预设
# 然后检查端口
curl http://127.0.0.1:9601/health
curl http://127.0.0.1:9612/health
curl http://127.0.0.1:9602/health
curl http://127.0.0.1:9621/health
curl http://127.0.0.1:9631/health
curl http://127.0.0.1:9641/health
```

文件验收：

```text
~/.hermes/profiles/writer-9601/SOUL.md
~/.hermes/profiles/engineer-9612/SOUL.md
~/.hermes/profiles/research-9602/SOUL.md
~/.hermes/profiles/hurman-9621/SOUL.md
~/.hermes/profiles/finance-9631/SOUL.md
~/.hermes/profiles/sales-9641/SOUL.md
```

DB 验收：

```sql
SELECT name, port, status
FROM profiles p
JOIN runtime_instances r ON r.profile_id = p.id
ORDER BY port;

SELECT profile_id, role_key, role_name, source_checksum
FROM profile_role_specs;
```

---

## 15. 这一阶段不做的内容

本阶段不做跨设备团队协作任务分派，不做远程 Hermes profile，不做 Portal backend profile 下发，不做角色市场。当前目标只限定为：

```text
本地 ai-os-desktop
  + 本地 Hermes multi profiles
  + agency-agents-zh 角色源
  + profile runtime 管理
  + Settings Drawer 可视化管理
```

这样可以先把本地专家 profile 跑稳，再进入团队作战和任务分派阶段。

[1]: https://github.com/jnMetaCode/agency-agents-zh "GitHub - jnMetaCode/agency-agents-zh:  211 个即插即用的 AI 专家角色 — 支持 Hermes Agent/Claude Code/Cursor/Copilot 等 16 种工具，覆盖工程/设计/营销/金融等 18 个部门。含 46 个中国市场原创智能体（小红书/抖音/微信/飞书/钉钉等） · GitHub"
