# PRD：v5.7.6_crm-host-bridge

## 1. 版本定义

版本号：`v5.7.6_crm-host-bridge`

目标模块：

```txt
src/shared/crm-bridge/**
src/main/crm-bridge/**
src/main/browser/browser-tool-bridge.ts
src/shared/browser/browser-tool-schema.ts
src/preload/crm-bridge-preload.ts
resources/crm-bridge/hermes-crm-bridge-sdk.js
src/renderer/src/screens/WebOperator/CrmEventPanel.tsx
src/renderer/src/components/hermes/constants.ts
```

明确不修改：

```txt
src/renderer/src/screens/WebOperator/HermesTaskPanel.tsx
src/renderer/src/screens/WebOperator/context/WebOperatorPageContext.tsx
src/renderer/src/screens/WebOperator/WebOperatorTaskStartDialogHost.tsx
src/renderer/src/screens/WebOperator/HermesTaskStartDialog.tsx
src/main/web-operator-task-session-store.ts
src/main/web-operator-task-session-ipc.ts
```

## 2. 功能目标

实现 Hermes 通过 Desktop Host Bridge 操作 CRM：

```txt
1. Hermes 主动跳转到 CRM URL。
2. Desktop 在跳转前保存 JSON handoff。
3. CRM 页面加载完成后，CRM JSSDK 主动上报 crm.page.ready。
4. Desktop 匹配 pending handoff。
5. Desktop 将 JSON 推送给 CRM 页面。
6. CRM JSSDK 把 JSON 保存到页面 JS 临时 store。
7. Desktop 触发 CRM actionKey。
8. CRM JSSDK 打开 Form。
9. CRM Form 从临时 store 读取 JSON 并填充录入字段。
10. CRM JSSDK 返回 command ack，Hermes 获得工具执行结果。
```

## 3. 交互边界

### 3.1 页面内容分析链路保持不变

```txt
PageStructurePanel
  → requestHermesAnalysis
  → HermesTaskPanel
  → task_session
  → HermesTaskStartDialog
  → WebOperatorHermesChatPanel
```

该链路继续用于 `[分析内容]`。

### 3.2 CRM Host Bridge 新链路

```txt
Hermes Chat
  → crm.open_form_with_json
  → BrowserToolBridge
  → CrmHandoffStore
  → BrowserController.open
  → CRM 页面加载
  → CRM JSSDK emitReady
  → CrmHandoffOrchestrator
  → desktop.crm.pushJson
  → desktop.crm.runAction
  → CRM Form 填充字段
```

该链路不得触发：

```txt
analysisRequest
taskStartDialog
setTaskStartDialog
setTaskStartDialogHandlers
requestHermesAnalysis
HermesTaskPanel.currentTask
task_session
```

## 4. 总体架构

```txt
Hermes Agent
  │
  │ tool call: crm.open_form_with_json
  ▼
BrowserToolServer
  │
  ▼
BrowserToolBridge
  │
  ├─ save PendingCrmHandoff
  └─ BrowserController.open(targetUrl)
        │
        ▼
WebOperator WebContentsView
  │
  ▼
CRM Page + crm-bridge-preload
  │
  ▼
CRM JSSDK
  │
  ├─ emit crm.page.ready
  ├─ receive desktop.crm.pushJson
  ├─ save handoff JSON
  ├─ receive desktop.crm.runAction
  ├─ open Form
  ├─ fill fields
  └─ command result ack
```

## 5. 数据协议

### 5.1 CRM Event Type 扩展

修改文件：

```txt
src/shared/crm-bridge/crm-bridge-contract.ts
```

扩展：

```ts
export type CrmBridgeEventType =
  | "crm.context.submit"
  | "crm.customer.open-ai-panel"
  | "crm.quote.create-assist"
  | "crm.order.risk-check"
  | "crm.page.snapshot-request"
  | "crm.page.ready";
```

### 5.2 CRM Desktop Command Type 扩展

```ts
export type CrmDesktopCommandType =
  | "desktop.crm.showToast"
  | "desktop.crm.highlightField"
  | "desktop.crm.focusField"
  | "desktop.crm.fillField"
  | "desktop.crm.scrollToSection"
  | "desktop.crm.requestContext"
  | "desktop.crm.clickButton"
  | "desktop.crm.runAction"
  | "desktop.crm.pushJson";
```

### 5.3 CrmDesktopCommand 扩展

```ts
export interface CrmDesktopCommand {
  commandId: string;
  type: CrmDesktopCommandType;
  target?: {
    selector?: string;
    fieldName?: string;
    entityId?: string;
    frameId?: string;
    actionKey?: string;
  };
  payload?: Record<string, unknown>;
  createdAt: string;

  /**
   * true：等待 CRM JSSDK ack。
   * false：保持旧命令行为，派发成功即返回。
   */
  expectAck?: boolean;

  /**
   * 默认 8000ms。
   */
  timeoutMs?: number;
}
```

### 5.4 Command Ack

```ts
export interface CrmDesktopCommandAck {
  commandId: string;
  ok: boolean;
  type: CrmDesktopCommandType;
  action?: string;
  message?: string;
  data?: Record<string, unknown>;
  errorCode?: string;
  receivedAt: string;
  completedAt: string;
}
```

### 5.5 Bridge Events 常量

```ts
export const CrmBridgeEvents = {
  ON_EVENT: "crm-bridge:on-event",
  COMMAND: "crm-bridge:command",
  COMMAND_RESULT: "crm-bridge:command-result",
} as const;
```

## 6. Handoff 数据结构

新增文件：

```txt
src/main/crm-bridge/crm-handoff-store.ts
```

类型：

```ts
export type PendingCrmHandoffStatus =
  | "pending"
  | "navigating"
  | "ready"
  | "delivering"
  | "delivered"
  | "failed"
  | "expired";

export type PendingCrmHandoff = {
  handoffId: string;
  targetUrl: string;
  normalizedUrl: string;
  actionKey: string;
  schema: string;
  entityType?: string;
  entityId?: string;
  data: Record<string, unknown>;
  status: PendingCrmHandoffStatus;
  createdAt: string;
  updatedAt: string;
  expiresAt: string;
  lastError?: string;
};
```

存储：

```ts
const handoffs = new Map<string, PendingCrmHandoff>();
```

索引：

```ts
const handoffIdsByNormalizedUrl = new Map<string, Set<string>>();
```

TTL：

```txt
默认 60 秒。
每次 match 前清理 expired handoff。
```

导出函数：

```ts
export function createCrmHandoff(input: {
  targetUrl: string;
  actionKey: string;
  schema: string;
  entityType?: string;
  entityId?: string;
  data: Record<string, unknown>;
  ttlMs?: number;
}): PendingCrmHandoff;

export function findPendingCrmHandoffByUrl(url: string): PendingCrmHandoff | null;

export function markCrmHandoffStatus(
  handoffId: string,
  status: PendingCrmHandoffStatus,
  lastError?: string,
): PendingCrmHandoff | null;

export function removeCrmHandoff(handoffId: string): void;

export function normalizeCrmUrl(rawUrl: string): string;
```

URL 归一化规则：

```txt
1. trim
2. 使用 new URL(rawUrl)
3. 保留 origin + pathname + search
4. 默认忽略 hash
5. pathname 去掉尾部 /
6. URL 解析失败时使用原始字符串 trim
```

## 7. Command Ack Store

新增文件：

```txt
src/main/crm-bridge/crm-command-result-store.ts
```

职责：

```txt
1. 保存等待 ack 的 command。
2. 接收 CRM JSSDK command result。
3. 按 commandId resolve。
4. 超时返回 COMMAND_ACK_TIMEOUT。
```

类型：

```ts
type PendingCommandResult = {
  commandId: string;
  resolve: (result: CrmBridgeResult & { data?: unknown }) => void;
  timer: NodeJS.Timeout;
  createdAt: string;
};
```

导出函数：

```ts
export function waitForCrmCommandResult(
  commandId: string,
  type: CrmDesktopCommandType,
  timeoutMs?: number,
): Promise<CrmBridgeResult & { data?: unknown }>;

export function resolveCrmCommandResult(ack: CrmDesktopCommandAck): void;

export function rejectCrmCommandResult(commandId: string, message: string): void;
```

超时返回：

```ts
{
  ok: false,
  requestId: commandId,
  errorCode: "COMMAND_ACK_TIMEOUT",
  message: "CRM command ack timeout"
}
```

同步扩展 `CrmBridgeErrorCode`：

```ts
| "COMMAND_ACK_TIMEOUT"
| "COMMAND_ACK_INVALID"
```

## 8. CrmCommandDispatcher 改造

修改文件：

```txt
src/main/crm-bridge/crm-command-dispatcher.ts
```

新增判断：

```ts
function shouldWaitForAck(command: CrmDesktopCommand): boolean {
  if (command.expectAck === true) return true;
  if (command.expectAck === false) return false;

  return (
    command.type === "desktop.crm.clickButton" ||
    command.type === "desktop.crm.runAction" ||
    command.type === "desktop.crm.pushJson"
  );
}
```

派发流程：

```ts
export async function dispatchCrmCommand(
  commandInput: unknown,
  viewManager: BrowserViewPort,
): Promise<CrmBridgeResult & { data?: unknown }> {
  const command = normalizeCommand(commandInput);

  if (!command) {
    return {
      ok: false,
      requestId: "",
      errorCode: "INVALID_SCHEMA",
      message: "Invalid CRM command",
    };
  }

  const wc = viewManager.getExternalWebContents();
  if (!wc || wc.isDestroyed()) {
    return {
      ok: false,
      requestId: command.commandId,
      errorCode: "WEB_OPERATOR_NOT_READY",
      message: "WebOperator view is not ready",
    };
  }

  wc.send(CrmBridgeEvents.COMMAND, command);

  if (!shouldWaitForAck(command)) {
    return {
      ok: true,
      requestId: command.commandId,
      action: "crm.command.sent",
      message: "Command dispatched to CRM page",
    };
  }

  return waitForCrmCommandResult(
    command.commandId,
    command.type,
    command.timeoutMs ?? 8000,
  );
}
```

`normalizeCommand` 必须保留：

```ts
expectAck
timeoutMs
target.actionKey
```

## 9. CrmBridgeIPC 改造

修改文件：

```txt
src/main/crm-bridge/crm-bridge-ipc.ts
```

### 9.1 send-command 改为 async

```ts
ipcMain.handle("crm-bridge:send-command", async (_event, command) => {
  const result = await dispatchCrmCommand(command, this.viewManager);
  logCrmBridgeAudit({
    requestId: result.requestId,
    action: result.ok ? "crm.command.completed" : "crm.command.failed",
    status: result.ok ? "success" : "failed",
    errorCode: result.errorCode,
    message: result.message,
  });
  return result;
});
```

### 9.2 新增 command-result IPC

```ts
ipcMain.handle("crm-bridge:command-result", async (event, ack) => {
  const wc = this.viewManager.getExternalWebContents();

  if (!wc || wc.isDestroyed() || wc.id !== event.sender.id) {
    return {
      ok: false,
      requestId: ack?.commandId ?? "",
      errorCode: "ORIGIN_NOT_ALLOWED",
      message: "CRM command result sender is not active WebOperator view",
    };
  }

  resolveCrmCommandResult(ack);
  return { ok: true };
});
```

### 9.3 unregister 增加 channel

```ts
"crm-bridge:command-result"
```

## 10. CRM Handoff Orchestrator

新增文件：

```txt
src/main/crm-bridge/crm-handoff-orchestrator.ts
```

职责：

```txt
1. 处理 crm.page.ready。
2. 按 ready page URL 匹配 PendingCrmHandoff。
3. 先下发 desktop.crm.pushJson。
4. pushJson 成功后下发 desktop.crm.runAction。
5. 更新 handoff 状态。
6. 返回执行结果。
```

接口：

```ts
export async function handleCrmPageReadyForHandoff(input: {
  readyEvent: CrmBridgeStoredEvent;
  viewManager: BrowserViewPort;
}): Promise<CrmBridgeResult & { handoffId?: string; data?: unknown }>;
```

执行逻辑：

```txt
1. normalizedUrl = normalizeCrmUrl(readyEvent.page.url)
2. handoff = findPendingCrmHandoffByUrl(normalizedUrl)
3. 未命中：返回 ok=true, action="crm.handoff.none"
4. 命中：mark status delivering
5. 发送 desktop.crm.pushJson，expectAck=true
6. pushJson 失败：mark failed，返回失败
7. 发送 desktop.crm.runAction，expectAck=true
8. runAction 成功：mark delivered
9. runAction 失败：mark failed
```

pushJson command：

```ts
{
  commandId: `crm_push_${handoff.handoffId}`,
  type: "desktop.crm.pushJson",
  payload: {
    handoffId: handoff.handoffId,
    schema: handoff.schema,
    entityType: handoff.entityType,
    entityId: handoff.entityId,
    data: handoff.data,
  },
  expectAck: true,
  timeoutMs: 8000,
  createdAt: new Date().toISOString(),
}
```

runAction command：

```ts
{
  commandId: `crm_action_${handoff.handoffId}`,
  type: "desktop.crm.runAction",
  target: {
    actionKey: handoff.actionKey,
    entityId: handoff.entityId,
  },
  payload: {
    handoffId: handoff.handoffId,
    schema: handoff.schema,
    entityType: handoff.entityType,
    entityId: handoff.entityId,
  },
  expectAck: true,
  timeoutMs: 12000,
  createdAt: new Date().toISOString(),
}
```

## 11. crm.page.ready 路由接入

修改文件：

```txt
src/main/crm-bridge/crm-bridge-ipc.ts
```

在 `crm-bridge:emit` 成功存储事件后，新增：

```ts
if (bridgeEvent.type === "crm.page.ready") {
  void handleCrmPageReadyForHandoff({
    readyEvent: stored,
    viewManager: this.viewManager,
  }).then((handoffResult) => {
    logCrmBridgeAudit({
      requestId: handoffResult.requestId,
      eventType: bridgeEvent.type,
      action: handoffResult.action ?? "crm.handoff.ready",
      status: handoffResult.ok ? "success" : "failed",
      errorCode: handoffResult.errorCode,
      message: handoffResult.message,
    });
  });
}
```

注意：该逻辑不修改 `routeCrmBridgeEvent` 原有行为。

## 12. crm-bridge-preload 改造

修改文件：

```txt
src/preload/crm-bridge-preload.ts
```

### 12.1 常量

```ts
const SDK_SOURCE = "copilot-crm-jssdk";
const DESKTOP_SOURCE = "copilot-desktop";
const COMMAND_CHANNEL = "crm.desktop.command";
const COMMAND_RESULT_CHANNEL = "crm.desktop.command.result";
const READY_CHANNEL = "crm.desktop.ready";
```

### 12.2 Desktop command 转发给页面

```ts
ipcRenderer.on(CrmBridgeEvents.COMMAND, (_event, command) => {
  window.postMessage(
    {
      source: DESKTOP_SOURCE,
      channel: COMMAND_CHANNEL,
      version: BRIDGE_VERSION,
      command,
      replyRequired: Boolean(command.expectAck),
    },
    window.location.origin,
  );
});
```

### 12.3 CRM JSSDK command result 转 IPC

```ts
window.addEventListener("message", (messageEvent) => {
  if (messageEvent.origin !== window.location.origin) return;

  const data = messageEvent.data as {
    source?: string;
    channel?: string;
    result?: unknown;
  } | null;

  if (!data || data.source !== SDK_SOURCE) return;
  if (data.channel !== COMMAND_RESULT_CHANNEL) return;
  if (!data.result) return;

  void ipcRenderer.invoke("crm-bridge:command-result", data.result);
});
```

### 12.4 Desktop ready 通知页面

```ts
window.postMessage(
  {
    source: DESKTOP_SOURCE,
    channel: READY_CHANNEL,
    version: BRIDGE_VERSION,
  },
  window.location.origin,
);
```

## 13. WebOperator WebContentsView 挂载 crm preload

修改文件：

```txt
src/main/browser/shell-browser-view-adapter.ts
src/main/index.ts
```

`ShellBrowserViewAdapter` 增加参数：

```ts
constructor(
  private readonly shellViewManager: ShellViewManager,
  private readonly crmBridgePreloadPath?: string,
) {}
```

创建 WebOperator view 时传：

```ts
preload: this.crmBridgePreloadPath,
```

`src/main/index.ts` 初始化：

```ts
const crmBridgePreloadPath = join(__dirname, "../preload/crm-bridge-preload.js");

const viewManager = new ShellBrowserViewAdapter(
  shellViewManager,
  crmBridgePreloadPath,
);
```

验收：

```js
window.CopilotDesktopCRM?.isAvailable?.()
```

返回：

```js
true
```

## 14. BrowserToolBridge 扩展

修改文件：

```txt
src/main/browser/browser-tool-bridge.ts
src/shared/browser/browser-tool-schema.ts
```

新增工具：

```txt
crm.get_context
crm.click_button
crm.run_action
crm.push_json
crm.open_form_with_json
```

### 14.1 工具：crm.open_form_with_json

入参 schema：

```ts
{
  url: string;
  actionKey: string;
  schema: string;
  entityType?: string;
  entityId?: string;
  data: Record<string, unknown>;
  ttlMs?: number;
}
```

执行：

```txt
1. 校验 url/actionKey/schema/data。
2. 创建 PendingCrmHandoff。
3. 调用 BrowserController.open({ url, source: "agent" })。
4. 返回 handoffId + status=navigating。
5. 后续 CRM ready 事件继续交付 JSON 和执行 action。
```

返回：

```ts
{
  ok: true,
  action: "crm.handoff.created",
  handoffId,
  targetUrl,
  normalizedUrl,
  expiresAt,
}
```

### 14.2 工具：crm.push_json

直接推送给当前 CRM 页面。

入参：

```ts
{
  schema: string;
  entityType?: string;
  entityId?: string;
  data: Record<string, unknown>;
  handoffId?: string;
}
```

派发：

```ts
{
  type: "desktop.crm.pushJson",
  payload: {
    handoffId,
    schema,
    entityType,
    entityId,
    data,
  },
  expectAck: true,
}
```

### 14.3 工具：crm.run_action

入参：

```ts
{
  actionKey: string;
  entityId?: string;
  payload?: Record<string, unknown>;
}
```

派发：

```ts
{
  type: "desktop.crm.runAction",
  target: {
    actionKey,
    entityId,
  },
  payload,
  expectAck: true,
}
```

### 14.4 工具：crm.click_button

入参：

```ts
{
  actionKey?: string;
  selector?: string;
  entityId?: string;
  payload?: Record<string, unknown>;
}
```

执行要求：

```txt
actionKey 和 selector 至少提供一个。
优先 actionKey。
selector 仅作为兜底。
```

### 14.5 工具：crm.get_context

返回最近一次 CRM event：

```ts
getLastCrmBridgeEvent()
```

如果没有事件：

```ts
{
  ok: false,
  message: "No CRM context event received"
}
```

## 15. CRM JSSDK 升级

修改文件：

```txt
resources/crm-bridge/hermes-crm-bridge-sdk.js
```

全局对象：

```ts
window.CopilotCrm
```

### 15.1 init

```ts
CopilotCrm.init({
  app: "crm",
  sdkVersion: "0.3.0",
  collectContext: () => ({
    app: "crm",
    entityType: "lead",
    entityId: window.currentLeadId,
    entityName: window.currentLeadName,
    url: location.href,
    title: document.title,
    fields: {},
    safeText: document.body.innerText.slice(0, 3000),
  }),
});
```

### 15.2 emitReady

页面 ready 后执行：

```ts
CopilotCrm.emitReady({
  capabilities: [
    "pushJson",
    "runAction",
    "clickButton",
    "openForm"
  ],
});
```

产生事件：

```ts
{
  type: "crm.page.ready",
  page: collectContext(),
  payload: {
    capabilities
  }
}
```

### 15.3 registerButton

```ts
CopilotCrm.registerButton(
  "lead.openAiForm",
  '[data-ai-action="lead.openAiForm"]',
);
```

### 15.4 registerAction

```ts
CopilotCrm.registerAction("lead.openAiForm", async ({ payload }) => {
  const handoff = CopilotCrm.getHandoffJson(payload.handoffId);

  window.crmForm.open("lead-ai-form");
  await window.crmForm.ready("lead-ai-form");

  window.crmForm.setValues("lead-ai-form", {
    riskLevel: handoff.data.riskLevel,
    summary: handoff.data.summary,
    nextAction: handoff.data.nextAction,
  });

  return {
    formId: "lead-ai-form",
    loaded: true,
  };
});
```

### 15.5 onJson

```ts
CopilotCrm.onJson(async ({ handoffId, schema, entityType, entityId, data }) => {
  if (schema === "lead.ai_analysis") {
    window.pendingAiAnalysis = data;
  }
});
```

### 15.6 内部 handoff store

```ts
const handoffStore = new Map();

function saveHandoffJson(input) {
  handoffStore.set(input.handoffId, input);

  window.dispatchEvent(
    new CustomEvent("copilot-crm-json-ready", {
      detail: input,
    }),
  );
}

function getHandoffJson(handoffId) {
  return handoffStore.get(handoffId) || null;
}
```

导出：

```ts
CopilotCrm.getHandoffJson = getHandoffJson;
```

### 15.7 Command Listener

监听 Desktop command：

```ts
window.addEventListener("message", async (event) => {
  if (event.origin !== window.location.origin) return;

  const message = event.data;
  if (!message || message.source !== "copilot-desktop") return;
  if (message.channel !== "crm.desktop.command") return;

  const command = message.command;
  const ack = await executeCommand(command);
  postCommandResult(ack);
});
```

### 15.8 executeCommand

支持：

```txt
desktop.crm.pushJson
desktop.crm.runAction
desktop.crm.clickButton
desktop.crm.showToast
desktop.crm.highlightField
desktop.crm.focusField
desktop.crm.fillField
desktop.crm.scrollToSection
desktop.crm.requestContext
```

`pushJson`：

```txt
1. 保存到 handoffStore。
2. 触发 onJson handlers。
3. dispatch copilot-crm-json-ready。
4. 返回 ack。
```

`runAction`：

```txt
1. 根据 actionKey 查找注册 action。
2. 执行 handler。
3. handler 结果作为 ack.data。
```

`clickButton`：

```txt
1. actionKey 查 registeredButton。
2. 查不到时用 [data-ai-action="${actionKey}"]。
3. 仍查不到时用 selector。
4. HTMLElement.click()。
```

## 16. CRM 页面标准接入示例

```html
<script src="/static/hermes-crm-bridge-sdk.js"></script>
<script>
  const copilotCrm = window.CopilotCrm.init({
    app: "crm",
    sdkVersion: "0.3.0",
    collectContext: () => ({
      app: "crm",
      entityType: "lead",
      entityId: window.currentLeadId,
      entityName: window.currentLeadName,
      url: location.href,
      title: document.title,
      fields: {
        status: window.currentLeadStatus
      },
      safeText: document.body.innerText.slice(0, 3000)
    })
  });

  copilotCrm.registerButton(
    "lead.openAiForm",
    '[data-ai-action="lead.openAiForm"]'
  );

  copilotCrm.registerAction("lead.openAiForm", async ({ payload }) => {
    const handoff = copilotCrm.getHandoffJson(payload.handoffId);

    if (!handoff) {
      throw new Error("handoff json not found");
    }

    window.crmForm.open("lead-ai-form");
    await window.crmForm.ready("lead-ai-form");

    window.crmForm.setValues("lead-ai-form", {
      riskLevel: handoff.data.riskLevel,
      summary: handoff.data.summary,
      nextAction: handoff.data.nextAction
    });

    return {
      formId: "lead-ai-form",
      loaded: true
    };
  });

  copilotCrm.emitReady({
    capabilities: ["pushJson", "runAction", "clickButton", "openForm"]
  });
</script>
```

HTML 按钮标准：

```html
<button
  data-ai-action="lead.openAiForm"
  data-ai-label="打开 AI 分析录入表单"
>
  打开 AI 分析录入表单
</button>
```

## 17. CrmEventPanel 调试增强

修改文件：

```txt
src/renderer/src/screens/WebOperator/CrmEventPanel.tsx
```

新增本地调试区：

```txt
actionKey
selector
jsonSchema
jsonPayload
targetUrl
```

新增按钮：

```txt
测试点击按钮
测试运行动作
测试推送 JSON
测试跳转并打开 Form
```

### 17.1 测试点击按钮

```ts
window.aiosBrowser.sendCrmCommand({
  commandId: `cmd_click_${Date.now()}`,
  type: "desktop.crm.clickButton",
  target: {
    actionKey,
    selector,
    entityId: lastEvent?.page.entityId,
  },
  payload: {},
  expectAck: true,
  timeoutMs: 8000,
  createdAt: new Date().toISOString(),
});
```

### 17.2 测试推送 JSON

```ts
window.aiosBrowser.sendCrmCommand({
  commandId: `cmd_json_${Date.now()}`,
  type: "desktop.crm.pushJson",
  payload: {
    handoffId: `manual_${Date.now()}`,
    schema: jsonSchema,
    entityType: lastEvent?.page.entityType,
    entityId: lastEvent?.page.entityId,
    data: JSON.parse(jsonPayload),
  },
  expectAck: true,
  timeoutMs: 8000,
  createdAt: new Date().toISOString(),
});
```

### 17.3 测试运行动作

```ts
window.aiosBrowser.sendCrmCommand({
  commandId: `cmd_action_${Date.now()}`,
  type: "desktop.crm.runAction",
  target: {
    actionKey,
    entityId: lastEvent?.page.entityId,
  },
  payload: {
    handoffId: currentManualHandoffId,
  },
  expectAck: true,
  timeoutMs: 12000,
  createdAt: new Date().toISOString(),
});
```

### 17.4 测试跳转并打开 Form

Renderer 不直接调用工具。该按钮只用于显示手工测试 payload，复制给 Hermes 或 BrowserToolServer 调试。

## 18. Hermes Panel 系统提示词补充

修改文件：

```txt
src/renderer/src/components/hermes/constants.ts
```

补充：

```ts
export const DEFAULT_PANEL_SYSTEM_PROMPT =
  "你是 WebOperator 侧栏助手，帮助用户理解并操作当前浏览器中的 Web 页面。" +
  "请用简体中文回答，可使用 Markdown。若上下文中有 web-context 附件，优先阅读附件中的 HTML。" +
  "当用户要求操作 CRM 页面时，优先使用 crm.get_context、crm.click_button、crm.run_action、crm.push_json、crm.open_form_with_json 工具。" +
  "需要跳转到 CRM 页面并打开表单时，必须使用 crm.open_form_with_json。不要让用户手动复制脚本。不要直接输出用于浏览器控制台执行的代码。";
```

该修改不涉及 `HermesTaskPanel.tsx`。

## 19. 安全规则

```txt
1. 不允许 Hermes 执行任意页面 JavaScript。
2. 不允许 Desktop 直接写 CRM 业务数据。
3. CRM 数据落库必须由 CRM Web 系统自己的 JS/API 完成。
4. CRM JSSDK 只接受同 origin 的 postMessage。
5. CRM command result 只接受当前 WebOperator active webContents。
6. handoff 默认 60 秒过期。
7. JSON payload 大小受现有 CRM Bridge payloadMaxBytes 限制。
8. selector 只作为兜底，不作为首选动作协议。
9. actionKey 必须由 CRM 页面注册或通过 data-ai-action 暴露。
10. Form 是新窗口时不使用页面 Map 临时变量，改由 CRM 后端草稿接口或 sessionStorage handoffId 处理。
```

## 20. 验收标准

### 20.1 不影响 HermesTaskPanel

```txt
1. 点击 PageStructurePanel 的 [分析内容]。
2. 首次 pageUrl 仍弹出 HermesTaskStartDialog。
3. 点击确认后 Hermes 仍自动分析 pageContext。
4. 再次分析同 pageUrl 仍加载历史 session。
5. crm.open_form_with_json 执行期间不出现 HermesTaskStartDialog。
6. CRM ready 事件不触发 requestHermesAnalysis。
7. CRM handoff 不写 task_session。
```

### 20.2 CRM preload

```js
window.CopilotDesktopCRM?.isAvailable?.()
```

返回：

```js
true
```

### 20.3 CRM page ready

CRM 页面执行：

```js
CopilotCrm.emitReady({
  capabilities: ["pushJson", "runAction", "clickButton", "openForm"]
});
```

Desktop `CrmEventPanel` 显示：

```txt
crm.page.ready
```

### 20.4 直接推送 JSON

`CrmEventPanel` 点击 `测试推送 JSON`。

预期：

```txt
1. CRM JSSDK 收到 desktop.crm.pushJson。
2. JSON 保存到 handoffStore。
3. CRM 页面触发 copilot-crm-json-ready。
4. Desktop 收到 ok=true ack。
```

### 20.5 直接运行动作

`CrmEventPanel` 点击 `测试运行动作`。

预期：

```txt
1. CRM JSSDK 执行 registerAction(actionKey)。
2. CRM 打开 Form。
3. Form 字段填充。
4. Desktop 收到 ok=true ack。
```

### 20.6 Hermes 跳转并打开 Form

Hermes 输入：

```txt
跳转到这个 CRM URL，把以下 JSON 推送到页面，打开 AI 分析录入表单，并把字段填入 Form。
```

工具调用：

```txt
crm.open_form_with_json
```

预期：

```txt
1. BrowserToolBridge 创建 handoff。
2. WebOperator 跳转到 CRM URL。
3. CRM 页面加载完成并 emitReady。
4. Desktop 自动 pushJson。
5. Desktop 自动 runAction。
6. CRM Form 打开并填充字段。
7. Hermes 收到工具结果。
```

## 21. Cursor 实施任务

### Phase 1：扩展 shared contract

```txt
修改 src/shared/crm-bridge/crm-bridge-contract.ts
- 增加 crm.page.ready
- 增加 clickButton/runAction/pushJson
- 增加 CrmDesktopCommandAck
- 增加 command expectAck/timeoutMs
- 增加 COMMAND_RESULT
- 增加 COMMAND_ACK_TIMEOUT / COMMAND_ACK_INVALID
```

### Phase 2：Command Ack Store

```txt
新增 src/main/crm-bridge/crm-command-result-store.ts
- waitForCrmCommandResult
- resolveCrmCommandResult
- rejectCrmCommandResult
- timeout cleanup
```

### Phase 3：Handoff Store

```txt
新增 src/main/crm-bridge/crm-handoff-store.ts
- createCrmHandoff
- findPendingCrmHandoffByUrl
- markCrmHandoffStatus
- removeCrmHandoff
- normalizeCrmUrl
- expired cleanup
```

### Phase 4：Handoff Orchestrator

```txt
新增 src/main/crm-bridge/crm-handoff-orchestrator.ts
- handleCrmPageReadyForHandoff
- pushJson command
- runAction command
- handoff status update
```

### Phase 5：改造 command dispatcher

```txt
修改 src/main/crm-bridge/crm-command-dispatcher.ts
- normalizeCommand 保留 expectAck/timeoutMs/actionKey
- dispatchCrmCommand 改 async
- legacy command 默认不等待 ack
- clickButton/runAction/pushJson 默认等待 ack
```

### Phase 6：改造 CrmBridgeIPC

```txt
修改 src/main/crm-bridge/crm-bridge-ipc.ts
- send-command await dispatchCrmCommand
- 增加 crm-bridge:command-result
- crm.page.ready 后触发 handoff orchestrator
- unregister 增加 command-result channel
```

### Phase 7：挂载 CRM preload

```txt
修改 src/main/browser/shell-browser-view-adapter.ts
修改 src/main/index.ts
- WebOperator WebContentsView 创建时传入 crm-bridge-preload.js
```

### Phase 8：改造 preload

```txt
修改 src/preload/crm-bridge-preload.ts
- command 转 postMessage
- command result 转 ipcRenderer.invoke
- Desktop ready event
```

### Phase 9：扩展 BrowserToolBridge

```txt
修改 src/main/browser/browser-tool-bridge.ts
修改 src/shared/browser/browser-tool-schema.ts
- crm.get_context
- crm.click_button
- crm.run_action
- crm.push_json
- crm.open_form_with_json
```

### Phase 10：升级 CRM JSSDK

```txt
修改 resources/crm-bridge/hermes-crm-bridge-sdk.js
- init
- emitReady
- registerButton
- registerAction
- onJson
- handoffStore
- command listener
- command ack
```

### Phase 11：CrmEventPanel 调试

```txt
修改 src/renderer/src/screens/WebOperator/CrmEventPanel.tsx
- actionKey/selector/jsonSchema/jsonPayload 输入
- 测试点击按钮
- 测试推送 JSON
- 测试运行动作
- 显示 command result
```

### Phase 12：Hermes system prompt

```txt
修改 src/renderer/src/components/hermes/constants.ts
- 补充 crm.* tools 使用规则
```

### Phase 13：验证

```txt
npm run typecheck
npm run build

验证项：
1. HermesTaskPanel 原分析弹窗逻辑不变
2. CRM 页面 window.CopilotDesktopCRM.isAvailable() 返回 true
3. CRM JSSDK emitReady 被 Desktop 接收
4. CrmEventPanel 可 pushJson
5. CrmEventPanel 可 runAction 打开 Form
6. Hermes 可调用 crm.open_form_with_json
7. 跳转 CRM URL 后 Form 自动填充
```

## 22. 不纳入 v5.7.6

```txt
1. 不做 CRM 后端草稿接口。
2. 不处理新窗口 Form 的跨窗口数据同步。
3. 不让 Hermes 直接执行任意 JS。
4. 不修改 HermesTaskPanel。
5. 不修改 WebOperatorPageContext。
6. 不修改 task_session。
7. 不把 CRM handoff 数据落本地 SQLite。
```
