已确认采用 **方案 B**。

执行边界定义如下：

```txt
MainPage 替代 DesktopShell 成为一级桌面容器；
DesktopSidebar 保留，但降级为 MainPage 内部的二级功能导航；
WorkspaceOutlet / StatusBar / ModalLayer / DrawerLayer 保留；
PageHeader 不再作为全局 Header，只作为页面内 Header 或逐步废弃；
多 WebView Tabs 由 MainPage 顶部统一承载。
```

## 1. 当前改造目标

当前 `Layout.tsx` 直接组合 `DesktopShell + DesktopSidebar + PageHeader + WorkspaceOutlet + StatusBar`。
当前 `DesktopShell` 是固定的 `aside + section + header + main + footer` 结构。

改造后：

```tsx
App
  └─ Layout
      └─ MainPage
          ├─ MainTopBar
          ├─ DesktopSidebar
          ├─ WorkspaceOutlet
          ├─ StatusBar
          ├─ ModalLayer
          └─ DrawerLayer
```

`Layout` 暂时保留，负责组织 navigation hook、profile、update state；`DesktopShell` 从主链路移除。

---

## 2. 目录结构

新增：

```txt
src/renderer/src/screens/MainPage/
  MainPage.tsx
  MainTopBar.tsx
  MainViewTabs.tsx
  MainProfileSwitch.tsx
  MainRuntimeIndicator.tsx
  main-page.css

src/shared/shell/main-page-constants.ts
```

修改：

```txt
src/renderer/src/screens/Layout/Layout.tsx
src/renderer/src/assets/main.css
src/renderer/src/components/layout/DesktopSidebar.tsx
src/renderer/src/components/layout/PageHeader.tsx
```

保留但不再作为主入口：

```txt
src/renderer/src/components/layout/DesktopShell.tsx
```

---

## 3. 新增常量

`src/shared/shell/main-page-constants.ts`

```ts
export const MAIN_TOPBAR_HEIGHT = 40;
export const MAIN_STATUSBAR_HEIGHT = 24;
export const MAIN_RAIL_WIDTH = 56;
export const MAIN_SIDEBAR_WIDTH = 232;
export const MAIN_INSPECTOR_WIDTH = 320;

export const DEFAULT_WINDOW_WIDTH = 1280;
export const DEFAULT_WINDOW_HEIGHT = 800;
export const MINIMUM_WINDOW_WIDTH = 900;
export const MINIMUM_WINDOW_HEIGHT = 600;
```

当前 Electron 主窗口是 `1100 x 750`，后续应同步改成 `1280 x 800`。

---

## 4. MainPage 组件契约

`src/renderer/src/screens/MainPage/MainPage.tsx`

```tsx
import "./main-page.css";

export interface MainPageProps {
  sidebar: React.ReactNode;
  outlet: React.ReactNode;
  statusBar?: React.ReactNode;
  modalLayer?: React.ReactNode;
  drawerLayer?: React.ReactNode;
  activeProfile: string;
  activeView: string;
  onNavigate: (view: string) => void;
}

export function MainPage({
  sidebar,
  outlet,
  statusBar,
  modalLayer,
  drawerLayer,
  activeProfile,
  activeView,
  onNavigate,
}: MainPageProps): React.JSX.Element {
  return (
    <div className="MainPage">
      <MainTopBar
        activeProfile={activeProfile}
        activeView={activeView}
        onNavigate={onNavigate}
      />

      <div className="MainPage__body">
        <aside className="MainPage__sidebar">{sidebar}</aside>
        <main className="MainPage__content">{outlet}</main>
      </div>

      {statusBar ? <footer className="MainPage__status">{statusBar}</footer> : null}
      {modalLayer}
      {drawerLayer}
    </div>
  );
}
```

---

## 5. MainTopBar

`src/renderer/src/screens/MainPage/MainTopBar.tsx`

```tsx
import { Settings, Globe, LayoutDashboard } from "lucide-react";
import { WindowControls } from "../../components/layout/WindowControls";
import { MainViewTabs } from "./MainViewTabs";
import { MainProfileSwitch } from "./MainProfileSwitch";
import { MainRuntimeIndicator } from "./MainRuntimeIndicator";

interface MainTopBarProps {
  activeProfile: string;
  activeView: string;
  onNavigate: (view: string) => void;
}

export function MainTopBar({
  activeProfile,
  activeView,
  onNavigate,
}: MainTopBarProps): React.JSX.Element {
  return (
    <header className="MainTopBar app-drag-region">
      <button
        type="button"
        className="MainTopBar__menu no-drag"
        aria-label="Application menu"
      >
        ⋯
      </button>

      <MainProfileSwitch activeProfile={activeProfile} />

      <MainViewTabs activeView={activeView} onNavigate={onNavigate} />

      <MainRuntimeIndicator />

      <div className="MainTopBar__actions no-drag">
        <button type="button" onClick={() => onNavigate("aios-home")}>
          <LayoutDashboard size={15} />
        </button>
        <button type="button" onClick={() => onNavigate("web-operator")}>
          <Globe size={15} />
        </button>
        <button type="button" onClick={() => onNavigate("settings")}>
          <Settings size={15} />
        </button>
      </div>

      <WindowControls />
    </header>
  );
}
```

当前项目已有 `WindowControls`，可直接复用。

---

## 6. 顶部 Tabs 初版

当前 `package.json` 没有 DnD 依赖，不能直接复制 Mattermost 的 `react-beautiful-dnd` 模式。
因此第一版先做 **可点击 Tabs**，不引入新依赖；DnD 排序作为第二阶段。

`src/renderer/src/screens/MainPage/MainViewTabs.tsx`

```tsx
interface MainViewTabsProps {
  activeView: string;
  onNavigate: (view: string) => void;
}

const WORKSPACE_TABS = [
  { id: "aios-home", label: "Portal" },
  { id: "aios-workspace", label: "Workspace" },
  { id: "web-operator", label: "Web Operator" },
];

export function MainViewTabs({
  activeView,
  onNavigate,
}: MainViewTabsProps): React.JSX.Element {
  return (
    <nav className="MainViewTabs no-drag" aria-label="Workspace tabs">
      {WORKSPACE_TABS.map((tab) => (
        <button
          key={tab.id}
          type="button"
          className={`MainViewTabs__item ${activeView === tab.id ? "active" : ""}`}
          onClick={() => onNavigate(tab.id)}
        >
          <span>{tab.label}</span>
        </button>
      ))}
    </nav>
  );
}
```

后续 profile workspace tabs 再从 `profileEntries` 动态生成。

---

## 7. 修改 Layout.tsx

把原来的：

```tsx
return (
  <DesktopShell
    sidebar={...}
    header={...}
    outlet={...}
    statusBar={...}
    modalLayer={...}
    drawerLayer={...}
  />
);
```

改为：

```tsx
import { MainPage } from "../MainPage/MainPage";

return (
  <MainPage
    activeProfile={navigation.activeProfile}
    activeView={navigation.view}
    onNavigate={(view) => navigation.navigateToView(view as never)}
    sidebar={
      <DesktopSidebar
        view={navigation.view}
        navItems={NAV_ITEMS}
        profileEntries={profileEntries}
        activeProfile={navigation.activeProfile}
        updateState={updateState}
        updateError={updateError}
        updateVersion={updateVersion}
        downloadPercent={downloadPercent}
        onNavigate={navigation.navigateToView}
        onUpdate={handleUpdate}
      />
    }
    outlet={
      <WorkspaceOutlet
        view={navigation.view}
        remoteMode={remoteMode}
        activeProfile={navigation.activeProfile}
        messages={navigation.messages}
        setMessages={navigation.setMessages}
        currentSessionId={navigation.currentSessionId}
        officeVisited={navigation.officeVisited}
        onNewChat={navigation.handleNewChat}
        onResumeSession={navigation.handleResumeSession}
        onSelectProfile={navigation.handleSelectProfile}
        onChatWithProfile={(name) => {
          navigation.handleSelectProfile(name);
          navigation.setView("chat");
        }}
        onNavigate={navigation.navigateToView}
      />
    }
    statusBar={
      <StatusBar
        activeProfile={navigation.activeProfile}
        remoteMode={remoteMode}
        updateState={updateState}
      />
    }
    modalLayer={<ModalLayer />}
    drawerLayer={<DrawerLayer />}
  />
);
```

---

## 8. CSS 主体

`src/renderer/src/screens/MainPage/main-page.css`

```css
.MainPage {
  width: 100%;
  height: 100%;
  min-height: 0;
  display: flex;
  flex-direction: column;
  overflow: hidden;
  background: var(--bg-primary);
  color: var(--text-primary);
}

.MainTopBar {
  height: 40px;
  min-height: 40px;
  display: flex;
  align-items: center;
  border-bottom: 1px solid var(--border);
  background: var(--bg-secondary);
  z-index: 9;
  user-select: none;
  -webkit-app-region: drag;
}

.MainTopBar__menu {
  width: 40px;
  height: 40px;
  border: 0;
  background: transparent;
  color: var(--text-secondary);
  cursor: pointer;
}

.MainTopBar__menu:hover {
  background: var(--bg-tertiary);
  color: var(--text-primary);
}

.MainTopBar__actions {
  display: flex;
  align-items: center;
  height: 40px;
  margin-left: auto;
}

.MainTopBar__actions button {
  width: 36px;
  height: 40px;
  border: 0;
  background: transparent;
  color: var(--text-secondary);
  cursor: pointer;
}

.MainTopBar__actions button:hover {
  background: var(--bg-tertiary);
  color: var(--text-primary);
}

.MainViewTabs {
  flex: 1;
  min-width: 0;
  height: 40px;
  display: flex;
  align-items: center;
  gap: 2px;
  padding: 5px 8px;
  overflow: hidden;
}

.MainViewTabs__item {
  max-width: 220px;
  height: 30px;
  padding: 0 12px;
  border: 0;
  border-radius: 6px;
  background: transparent;
  color: var(--text-secondary);
  font-size: 12px;
  font-weight: 600;
  cursor: pointer;
}

.MainViewTabs__item:hover {
  background: var(--bg-tertiary);
  color: var(--text-primary);
}

.MainViewTabs__item.active {
  background: var(--bg-primary);
  color: var(--text-primary);
}

.MainPage__body {
  flex: 1;
  min-height: 0;
  display: flex;
  overflow: hidden;
}

.MainPage__sidebar {
  width: 232px;
  flex: 0 0 232px;
  min-height: 0;
  display: flex;
  border-right: 1px solid var(--border);
  background: var(--bg-secondary);
}

.MainPage__content {
  flex: 1;
  min-width: 0;
  min-height: 0;
  display: flex;
  flex-direction: column;
  overflow: hidden;
  background: var(--bg-primary);
}

.MainPage__status {
  height: 24px;
  min-height: 24px;
  border-top: 1px solid var(--border);
  background: var(--bg-secondary);
}
```

同时修正 `main.css`：

```css
html,
body,
#root {
  width: 100%;
  height: 100%;
  overflow: hidden;
}

.app {
  width: 100vw;
  height: 100vh;
  height: 100dvh;
  display: flex;
  flex-direction: column;
  background: var(--bg-primary);
}
```

替换当前 `.app { height: 97vh; }`。

---

## 9. Sidebar 降级规则

`DesktopSidebar` 不删除，只调整职责：

```txt
原职责：
  主窗口一级导航

新职责：
  MainPage 内部二级功能导航
```

第一阶段保留 232px 展开态。第二阶段再做：

```txt
expanded: 232px
rail: 56px
hidden: 0px
```

不要首版直接压缩成 56px，否则当前 nav label、profileEntries、update button 都要同步重构，改动过大。当前 `DesktopSidebar` 内部还有 profile entries、update 状态等逻辑。

---

## 10. 本轮不动的部分

```txt
ShellViewManager
WebContentsHost
WorkspaceOutlet
Browser IPC
Profile Runtime IPC
ModalManager
DropdownManager
TrayManager
```

原因：

当前 `WebContentsHost` 已经通过 `ResizeObserver` 同步 DOM bounds 到 `window.shellView.setBounds`，这部分是多 WebView 的正确基础。

本轮只做主窗口壳层替换，避免同时改 IPC、native view 生命周期和样式，降低风险。

---

## 11. 验收标准

```txt
1. npm run typecheck 通过
2. npm run dev 启动后进入 main screen
3. 顶部 MainTopBar 固定 40px
4. WindowControls 在 Windows 下可最小化、最大化、关闭
5. Sidebar 仍可正常切换 Chat / Sessions / Models / Skills / Gateway 等页面
6. 顶部 Portal / Workspace / Web Operator tabs 可切换对应页面
7. AIOSHomeScreen 内 WebContentsHost bounds 不偏移
8. StatusBar 固定底部 24px
9. ModalLayer / DrawerLayer 不被 MainTopBar 遮挡
10. 窗口 resize 后主内容区域不出现滚动条错位
```

## 12. 后续第二阶段

```txt
Phase 2.1：Sidebar 支持 expanded / rail / hidden
Phase 2.2：MainViewTabs 接入 profileEntries，动态生成 profile workspace tabs
Phase 2.3：ShellViewManager 支持 CREATE / LOAD_URL / FOCUS / DESTROY / GET_STATE
Phase 2.4：WebOperatorScreen 从 BrowserViewManager 迁移到 ShellViewManager
Phase 2.5：顶部 Tabs 增加 DnD 排序；如要首版支持，需要引入 @dnd-kit/sortable 或重新加入 react-beautiful-dnd
```

当前执行顺序：**先 MainPage 壳层落地，再统一多 WebView 生命周期。**
