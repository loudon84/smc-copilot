下面是 **方案 B 第二阶段 Cursor 执行稿**。范围限定为：

```txt id="661clp"
Phase 2.1：Sidebar 支持 expanded / rail / hidden
Phase 2.2：MainViewTabs 接入 profileEntries，动态生成 profile workspace tabs
Phase 2.3：ShellViewManager 扩展 CREATE / LOAD_URL / FOCUS / DESTROY / GET_STATE / GET_ALL
Phase 2.4：WebOperatorScreen 从 BrowserViewManager 迁移到 ShellViewManager
```

当前 `ShellViewManager` 已具备 `createView / activateView / destroyView / getView / getAllActiveViews` 等核心能力，第二阶段主要是补 IPC、补 renderer API、补 UI 入口。

---

# 一、第二阶段提交拆分

## Commit 1：MainPage Tabs + Sidebar 三态

目标：

```txt id="utck8n"
1. MainPage 顶部 tabs 支持 profileEntries
2. Sidebar 支持 expanded / rail / hidden
3. Layout.tsx 统一把 profileEntries 传给 MainPage
4. 不改 ShellViewManager
```

当前 `useProfileEntries()` 已从 `window.profileEntry.listProfileEntries()` 获取 profile entry 列表。
`ProfileEntrySummary` 已包含 `profileId / entryType / route / title / icon / enabled / sortOrder`。

## Commit 2：ShellView IPC 扩展

目标：

```txt id="2v7i9t"
1. shared contract 增加 CREATE / LOAD_URL / FOCUS / DESTROY / GET_STATE / GET_ALL
2. preload shellViewApi 增加对应方法
3. main shell-view-ipc.ts 注册对应 handler
4. ShellViewManager 增加 loadUrl / focusView / getViewSnapshot / getAllViewSnapshots
```

当前 IPC contract 只有 `ACTIVATE / SET_BOUNDS / HIDE`。
当前 preload 也只暴露 `activate / setBounds / hide`。

## Commit 3：WebOperator 迁移到 ShellViewManager

目标：

```txt id="ea3jyh"
1. WebOperatorScreen 使用 WebContentsHost layerId="web-operator"
2. BrowserToolbar 的 browser.open 继续保留
3. BrowserController 第一阶段仍用 BrowserViewManager，避免一次性破坏 agent browser tool
4. 后续第三阶段再把 BrowserController 的 viewManager 替换成 ShellViewManager adapter
```

原因：当前 `BrowserController` 强依赖 `BrowserViewManager.getExternalWebContents()`、`isReady()`、`createView()` 等方法。
如果本阶段直接替换，browser tool 链路风险过高。

---

# 二、Commit 1：MainPage Tabs + Sidebar 三态

## 1. 新增类型

`src/renderer/src/screens/MainPage/main-page-types.ts`

```ts id="y5u37c"
import type { ProfileEntrySummary } from "../../../../shared/profile-runtime/profile-runtime-contract";
import type { View } from "../../types/desktop-shell";

export type SidebarMode = "expanded" | "rail" | "hidden";

export interface MainWorkspaceTab {
  id: View;
  title: string;
  closeable: boolean;
  source: "system" | "profile" | "operator";
  profileId?: string;
}

export interface MainPageViewModel {
  activeView: View;
  activeProfile: string;
  sidebarMode: SidebarMode;
  profileEntries: ProfileEntrySummary[];
}
```

## 2. 新增 tabs 解析工具

`src/renderer/src/screens/MainPage/main-page-tabs.ts`

```ts id="2n9uq1"
import type { ProfileEntrySummary } from "../../../../shared/profile-runtime/profile-runtime-contract";
import type { View } from "../../types/desktop-shell";
import type { MainWorkspaceTab } from "./main-page-types";

const STATIC_TABS: MainWorkspaceTab[] = [
  {
    id: "aios-home",
    title: "Portal",
    closeable: false,
    source: "system",
  },
  {
    id: "aios-workspace",
    title: "Workspace",
    closeable: false,
    source: "system",
  },
  {
    id: "web-operator",
    title: "Web Operator",
    closeable: false,
    source: "operator",
  },
];

export function buildMainWorkspaceTabs(
  profileEntries: ProfileEntrySummary[],
): MainWorkspaceTab[] {
  const profileTabs: MainWorkspaceTab[] = profileEntries
    .filter((entry) => entry.enabled)
    .filter((entry) => entry.entryType === "specialist-workspace")
    .sort((a, b) => a.sortOrder - b.sortOrder)
    .map((entry) => ({
      id: `profile-workspace:${entry.profileId}` as View,
      title: entry.title || entry.profileId,
      closeable: false,
      source: "profile",
      profileId: entry.profileId,
    }));

  return [...STATIC_TABS, ...profileTabs];
}

export function isWorkspaceTabView(view: View): boolean {
  return (
    view === "aios-home" ||
    view === "aios-workspace" ||
    view === "web-operator" ||
    (typeof view === "string" && view.startsWith("profile-workspace:"))
  );
}
```

## 3. 修改 MainPage props

`src/renderer/src/screens/MainPage/MainPage.tsx`

```tsx id="mjvtu7"
import type { ProfileEntrySummary } from "../../../../shared/profile-runtime/profile-runtime-contract";
import type { View } from "../../types/desktop-shell";
import type { SidebarMode } from "./main-page-types";
import { MainTopBar } from "./MainTopBar";
import "./main-page.css";

export interface MainPageProps {
  sidebar: React.ReactNode;
  outlet: React.ReactNode;
  statusBar?: React.ReactNode;
  modalLayer?: React.ReactNode;
  drawerLayer?: React.ReactNode;
  activeProfile: string;
  activeView: View;
  profileEntries: ProfileEntrySummary[];
  sidebarMode: SidebarMode;
  onSidebarModeChange: (mode: SidebarMode) => void;
  onNavigate: (view: View) => void;
}

export function MainPage({
  sidebar,
  outlet,
  statusBar,
  modalLayer,
  drawerLayer,
  activeProfile,
  activeView,
  profileEntries,
  sidebarMode,
  onSidebarModeChange,
  onNavigate,
}: MainPageProps): React.JSX.Element {
  return (
    <div className={`MainPage MainPage--sidebar-${sidebarMode}`}>
      <MainTopBar
        activeProfile={activeProfile}
        activeView={activeView}
        profileEntries={profileEntries}
        sidebarMode={sidebarMode}
        onSidebarModeChange={onSidebarModeChange}
        onNavigate={onNavigate}
      />

      <div className="MainPage__body">
        {sidebarMode !== "hidden" ? (
          <aside className="MainPage__sidebar">{sidebar}</aside>
        ) : null}

        <main className="MainPage__content">{outlet}</main>
      </div>

      {statusBar ? <footer className="MainPage__status">{statusBar}</footer> : null}
      {modalLayer}
      {drawerLayer}
    </div>
  );
}
```

## 4. 修改 MainTopBar

`src/renderer/src/screens/MainPage/MainTopBar.tsx`

```tsx id="spmdoj"
import { PanelLeftClose, PanelLeftOpen, Settings, Globe, LayoutDashboard } from "lucide-react";
import type { ProfileEntrySummary } from "../../../../shared/profile-runtime/profile-runtime-contract";
import type { View } from "../../types/desktop-shell";
import { WindowControls } from "../../components/layout/WindowControls";
import type { SidebarMode } from "./main-page-types";
import { MainViewTabs } from "./MainViewTabs";
import { MainProfileSwitch } from "./MainProfileSwitch";
import { MainRuntimeIndicator } from "./MainRuntimeIndicator";

interface MainTopBarProps {
  activeProfile: string;
  activeView: View;
  profileEntries: ProfileEntrySummary[];
  sidebarMode: SidebarMode;
  onSidebarModeChange: (mode: SidebarMode) => void;
  onNavigate: (view: View) => void;
}

function nextSidebarMode(mode: SidebarMode): SidebarMode {
  if (mode === "expanded") return "rail";
  if (mode === "rail") return "hidden";
  return "expanded";
}

export function MainTopBar({
  activeProfile,
  activeView,
  profileEntries,
  sidebarMode,
  onSidebarModeChange,
  onNavigate,
}: MainTopBarProps): React.JSX.Element {
  const SidebarIcon = sidebarMode === "hidden" ? PanelLeftOpen : PanelLeftClose;

  return (
    <header className="MainTopBar app-drag-region">
      <button
        type="button"
        className="MainTopBar__menu no-drag"
        aria-label="Toggle sidebar"
        title={`Sidebar: ${sidebarMode}`}
        onClick={() => onSidebarModeChange(nextSidebarMode(sidebarMode))}
      >
        <SidebarIcon size={16} />
      </button>

      <MainProfileSwitch activeProfile={activeProfile} />

      <MainViewTabs
        activeView={activeView}
        profileEntries={profileEntries}
        onNavigate={onNavigate}
      />

      <MainRuntimeIndicator />

      <div className="MainTopBar__actions no-drag">
        <button type="button" title="Portal" onClick={() => onNavigate("aios-home")}>
          <LayoutDashboard size={15} />
        </button>
        <button type="button" title="Web Operator" onClick={() => onNavigate("web-operator")}>
          <Globe size={15} />
        </button>
        <button type="button" title="Settings" onClick={() => onNavigate("settings")}>
          <Settings size={15} />
        </button>
      </div>

      <WindowControls />
    </header>
  );
}
```

## 5. 修改 MainViewTabs

`src/renderer/src/screens/MainPage/MainViewTabs.tsx`

```tsx id="o4rkxy"
import type { ProfileEntrySummary } from "../../../../shared/profile-runtime/profile-runtime-contract";
import type { View } from "../../types/desktop-shell";
import { buildMainWorkspaceTabs } from "./main-page-tabs";

interface MainViewTabsProps {
  activeView: View;
  profileEntries: ProfileEntrySummary[];
  onNavigate: (view: View) => void;
}

export function MainViewTabs({
  activeView,
  profileEntries,
  onNavigate,
}: MainViewTabsProps): React.JSX.Element {
  const tabs = buildMainWorkspaceTabs(profileEntries);

  return (
    <nav className="MainViewTabs no-drag" aria-label="Workspace tabs">
      {tabs.map((tab) => (
        <button
          key={tab.id}
          type="button"
          className={`MainViewTabs__item ${activeView === tab.id ? "active" : ""}`}
          title={tab.title}
          onClick={() => onNavigate(tab.id)}
        >
          <span>{tab.title}</span>
        </button>
      ))}
    </nav>
  );
}
```

> DnD 不在本阶段做。当前 `package.json` 没有 `@dnd-kit/*` 或 `react-beautiful-dnd` 依赖，直接复制 Mattermost DnD 会引入额外依赖与 React 19 兼容风险。

## 6. 修改 Layout.tsx

增加：

```tsx id="tvk6sm"
import { useState } from "react";
import { MainPage } from "../MainPage/MainPage";
import type { SidebarMode } from "../MainPage/main-page-types";
```

在 `Layout()` 内增加：

```tsx id="vc3fs7"
const [sidebarMode, setSidebarMode] = useState<SidebarMode>("expanded");
```

把原来的 `DesktopShell` 替换为：

```tsx id="xbb8bw"
return (
  <MainPage
    activeProfile={navigation.activeProfile}
    activeView={navigation.view}
    profileEntries={profileEntries}
    sidebarMode={sidebarMode}
    onSidebarModeChange={setSidebarMode}
    onNavigate={navigation.navigateToView}
    sidebar={
      <DesktopSidebar
        mode={sidebarMode}
        view={navigation.view}
        navItems={NAV_ITEMS}
        profileEntries={profileEntries}
        activeProfile={navigation.activeProfile}
        updateState={updateState}
        updateError={updateError}
        updateVersion={updateVersion}
        downloadPercent={downloadPercent}
        onNavigate={navigation.navigateToView}
        onUpdate={handleUpdate}
      />
    }
    outlet={
      <WorkspaceOutlet
        view={navigation.view}
        remoteMode={remoteMode}
        activeProfile={navigation.activeProfile}
        messages={navigation.messages}
        setMessages={navigation.setMessages}
        currentSessionId={navigation.currentSessionId}
        officeVisited={navigation.officeVisited}
        onNewChat={navigation.handleNewChat}
        onResumeSession={navigation.handleResumeSession}
        onSelectProfile={navigation.handleSelectProfile}
        onChatWithProfile={(name) => {
          navigation.handleSelectProfile(name);
          navigation.setView("chat");
        }}
        onNavigate={navigation.navigateToView}
      />
    }
    statusBar={
      <StatusBar
        activeProfile={navigation.activeProfile}
        remoteMode={remoteMode}
        updateState={updateState}
      />
    }
    modalLayer={<ModalLayer />}
    drawerLayer={<DrawerLayer />}
  />
);
```

## 7. 修改 DesktopSidebar props

`src/renderer/src/components/layout/DesktopSidebar.tsx`

增加：

```tsx id="h6dx2o"
import type { SidebarMode } from "../../screens/MainPage/main-page-types";
```

Props 增加：

```ts id="la85vc"
mode?: SidebarMode;
```

组件参数增加：

```ts id="hdmtth"
mode = "expanded",
```

根节点从 fragment 改为：

```tsx id="lg4bay"
return (
  <div className={`desktop-sidebar desktop-sidebar--${mode}`}>
    ...
  </div>
);
```

导航按钮 label 改成：

```tsx id="w8ji1c"
{mode === "expanded" ? t(labelKey) : null}
```

profile entry label 同理：

```tsx id="2r9hcv"
{mode === "expanded" ? entry.title : null}
```

group label 在 rail 模式隐藏：

```tsx id="7j7t1j"
{mode === "expanded" ? (
  <div className="sidebar-nav-group-label">Experts</div>
) : null}
```

---

# 三、Commit 1 CSS

`src/renderer/src/screens/MainPage/main-page.css`

补充：

```css id="gzxz5x"
.MainPage--sidebar-expanded .MainPage__sidebar {
  width: 232px;
  flex-basis: 232px;
}

.MainPage--sidebar-rail .MainPage__sidebar {
  width: 56px;
  flex-basis: 56px;
}

.MainPage--sidebar-hidden .MainPage__content {
  flex: 1;
}

.desktop-sidebar {
  width: 100%;
  height: 100%;
  min-height: 0;
  display: flex;
  flex-direction: column;
  overflow: hidden;
}

.desktop-sidebar--rail .sidebar-nav {
  padding: 6px;
  align-items: center;
}

.desktop-sidebar--rail .sidebar-nav-item {
  width: 40px;
  height: 40px;
  justify-content: center;
  padding: 0;
  gap: 0;
}

.desktop-sidebar--rail .sidebar-nav-divider {
  width: 28px;
  margin: 8px auto;
}

.desktop-sidebar--rail .sidebar-footer {
  padding: 6px;
}

.desktop-sidebar--rail .sidebar-update-btn span,
.desktop-sidebar--rail .sidebar-update-error {
  display: none;
}

.MainViewTabs__item span {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
```

同时保留第一阶段已要求的 `.app height: 100vh` 修正。

---

# 四、Commit 2：ShellView IPC 扩展

## 1. 修改 shared contract

`src/shared/shell/shell-view-contract.ts`

替换为：

```ts id="g8vvyf"
import type {
  ShellViewBounds,
  ShellViewKind,
  ShellViewLayer,
  ShellViewOptions,
  ShellViewState,
} from "./view-contract";

export const ShellViewChannels = {
  CREATE: "shell:view:create",
  ACTIVATE: "shell:view:activate",
  SET_BOUNDS: "shell:view:set-bounds",
  LOAD_URL: "shell:view:load-url",
  FOCUS: "shell:view:focus",
  HIDE: "shell:view:hide",
  DESTROY: "shell:view:destroy",
  GET_STATE: "shell:view:get-state",
  GET_ALL: "shell:view:get-all",
} as const;

export type ShellViewBoundsIPC = ShellViewBounds;

export interface ShellViewCreateRequest {
  layerId: string;
  kind: ShellViewKind;
  url: string;
  options?: Partial<ShellViewOptions>;
}

export interface ShellViewActivateRequest {
  layerId: string;
}

export interface ShellViewSetBoundsRequest {
  layerId: string;
  bounds: ShellViewBoundsIPC;
}

export interface ShellViewLoadUrlRequest {
  layerId: string;
  url: string;
}

export interface ShellViewFocusRequest {
  layerId: string;
}

export interface ShellViewHideRequest {
  layerId: string;
}

export interface ShellViewDestroyRequest {
  layerId: string;
}

export interface ShellViewSnapshot {
  id: string;
  kind: ShellViewKind;
  layer: ShellViewLayer;
  state: ShellViewState;
  bounds: ShellViewBounds;
  url: string;
  active: boolean;
}

export type ShellViewResponse = void;
```

## 2. 修改 ShellViewManager

`src/main/shell/views/shell-view-manager.ts`

增加方法：

```ts id="8df9rv"
async loadUrl(id: string, url: string): Promise<void> {
  const view = this.views.get(id);
  if (!view) {
    throw new Error(`[ShellViewManager] View ${id} not found`);
  }

  await view.load(url);
}

focusView(id: string): void {
  const view = this.views.get(id);
  if (!view) {
    throw new Error(`[ShellViewManager] View ${id} not found`);
  }

  view.focus();
}

getViewSnapshot(id: string) {
  const view = this.views.get(id);
  if (!view) {
    return null;
  }

  const webContents = view.getWebContents();
  return {
    id: view.getId(),
    kind: view.getKind(),
    layer: view.getLayer(),
    state: view.getState(),
    bounds: view.getBounds(),
    url: webContents?.getURL() ?? "",
    active: view.isActive(),
  };
}

getAllViewSnapshots() {
  return Array.from(this.views.keys())
    .map((id) => this.getViewSnapshot(id))
    .filter((item): item is NonNullable<typeof item> => item !== null);
}
```

`ManagedWebContentsView` 已经有 `load / focus / getWebContents / getState / getBounds / isActive`，可以直接调用。

## 3. 修改 preload API

`src/preload/shell-view-api.ts`

```ts id="8lhbao"
import { ipcRenderer } from "electron";
import type {
  ShellViewBoundsIPC,
  ShellViewCreateRequest,
  ShellViewSnapshot,
} from "../shared/shell/shell-view-contract";
import { ShellViewChannels } from "../shared/shell/shell-view-contract";
import type { ShellViewKind, ShellViewOptions } from "../shared/shell/view-contract";

export const shellViewApi = {
  create: (
    layerId: string,
    kind: ShellViewKind,
    url: string,
    options?: Partial<ShellViewOptions>,
  ): Promise<void> =>
    ipcRenderer.invoke(ShellViewChannels.CREATE, {
      layerId,
      kind,
      url,
      options,
    } satisfies ShellViewCreateRequest),

  activate: (layerId: string): Promise<void> =>
    ipcRenderer.invoke(ShellViewChannels.ACTIVATE, layerId),

  setBounds: (layerId: string, bounds: ShellViewBoundsIPC): Promise<void> =>
    ipcRenderer.invoke(ShellViewChannels.SET_BOUNDS, layerId, bounds),

  loadUrl: (layerId: string, url: string): Promise<void> =>
    ipcRenderer.invoke(ShellViewChannels.LOAD_URL, { layerId, url }),

  focus: (layerId: string): Promise<void> =>
    ipcRenderer.invoke(ShellViewChannels.FOCUS, layerId),

  hide: (layerId: string): Promise<void> =>
    ipcRenderer.invoke(ShellViewChannels.HIDE, layerId),

  destroy: (layerId: string): Promise<void> =>
    ipcRenderer.invoke(ShellViewChannels.DESTROY, layerId),

  getState: (layerId: string): Promise<ShellViewSnapshot | null> =>
    ipcRenderer.invoke(ShellViewChannels.GET_STATE, layerId),

  getAll: (): Promise<ShellViewSnapshot[]> =>
    ipcRenderer.invoke(ShellViewChannels.GET_ALL),
};
```

## 4. 修改 shell-view-ipc.ts

`src/main/shell/shell-view-ipc.ts`

新增 imports：

```ts id="dxrw16"
import type {
  ShellViewCreateRequest,
  ShellViewLoadUrlRequest,
} from "../../shared/shell/shell-view-contract";
```

在 `registerShellViewIpc()` 内增加：

```ts id="2b6bfc"
ipcMain.handle(
  ShellViewChannels.CREATE,
  async (_event, request: ShellViewCreateRequest): Promise<void> => {
    if (!request?.layerId || typeof request.layerId !== "string") {
      throw new Error(`Invalid layerId: ${request?.layerId}`);
    }
    if (!request?.kind || typeof request.kind !== "string") {
      throw new Error(`Invalid kind: ${request?.kind}`);
    }
    if (!request?.url || typeof request.url !== "string") {
      throw new Error(`Invalid url: ${request?.url}`);
    }

    await svm.createView(request.layerId, request.kind, request.url, request.options);
  },
);

ipcMain.handle(
  ShellViewChannels.LOAD_URL,
  async (_event, request: ShellViewLoadUrlRequest): Promise<void> => {
    if (!request?.layerId || typeof request.layerId !== "string") {
      throw new Error(`Invalid layerId: ${request?.layerId}`);
    }
    if (!request?.url || typeof request.url !== "string") {
      throw new Error(`Invalid url: ${request?.url}`);
    }

    await ensureKnownView(svm, request.layerId);
    await svm.loadUrl(request.layerId, request.url);
  },
);

ipcMain.handle(
  ShellViewChannels.FOCUS,
  async (_event, layerId: string): Promise<void> => {
    if (!layerId || typeof layerId !== "string") {
      throw new Error(`Invalid layerId: ${layerId}`);
    }

    await ensureKnownView(svm, layerId);
    svm.focusView(layerId);
  },
);

ipcMain.handle(
  ShellViewChannels.DESTROY,
  async (_event, layerId: string): Promise<void> => {
    if (!layerId || typeof layerId !== "string") {
      throw new Error(`Invalid layerId: ${layerId}`);
    }

    svm.destroyView(layerId);
  },
);

ipcMain.handle(
  ShellViewChannels.GET_STATE,
  async (_event, layerId: string) => {
    if (!layerId || typeof layerId !== "string") {
      throw new Error(`Invalid layerId: ${layerId}`);
    }

    return svm.getViewSnapshot(layerId);
  },
);

ipcMain.handle(ShellViewChannels.GET_ALL, async () => {
  return svm.getAllViewSnapshots();
});
```

`view-registry.ts` 已注册 `web-operator / aios-home / profile-page / external-browser`，所以 CREATE 不需要再额外新增 kind。

---

# 五、Commit 3：WebOperator 迁移到 ShellViewManager

## 1. 修改 WebOperatorScreen

当前 `WebOperatorScreen` 使用 `BrowserViewportHost` + `window.aiosBrowser.updateBounds()`。

改为：

```tsx id="sswp0t"
import { useEffect } from "react";
import { WebContentsHost } from "../../components/shell/WebContentsHost";
import { BrowserToolbar } from "./BrowserToolbar";
import { BrowserStatePanel } from "./BrowserStatePanel";
import { ScreenshotPanel } from "./ScreenshotPanel";
import { BrowserActionLog } from "./BrowserActionLog";

const WEB_OPERATOR_HOME_URL = "about:blank";

export function WebOperatorScreen() {
  useEffect(() => {
    void window.shellView
      .create("web-operator", "web-operator", WEB_OPERATOR_HOME_URL, {
        layer: "content",
        sandbox: true,
      })
      .catch(() => {
        // view may already exist
      });
  }, []);

  return (
    <div className="web-operator-layout">
      <div className="web-operator-layout__main">
        <BrowserToolbar />
        <WebContentsHost
          layerId="web-operator"
          className="web-operator-layout__viewport"
        />
      </div>

      <div className="web-operator-layout__side">
        <BrowserStatePanel className="flex-1 overflow-hidden" />
        <div className="web-operator-layout__panel">
          <ScreenshotPanel />
        </div>
        <div className="web-operator-layout__panel web-operator-layout__panel--grow">
          <BrowserActionLog className="flex-1" />
        </div>
      </div>
    </div>
  );
}
```

## 2. 新增 CSS

加入 `main-page.css` 或独立 `web-operator.css`：

```css id="mw048i"
.web-operator-layout {
  flex: 1;
  min-height: 0;
  display: flex;
  overflow: hidden;
  background: var(--bg-primary);
  color: var(--text-primary);
}

.web-operator-layout__main {
  flex: 1;
  min-width: 0;
  min-height: 0;
  display: flex;
  flex-direction: column;
  overflow: hidden;
}

.web-operator-layout__viewport {
  flex: 1;
  min-height: 0;
  width: 100%;
  background: var(--bg-secondary);
}

.web-operator-layout__side {
  width: 320px;
  flex: 0 0 320px;
  min-height: 0;
  display: flex;
  flex-direction: column;
  overflow: hidden;
  border-left: 1px solid var(--border);
  background: var(--bg-secondary);
}

.web-operator-layout__panel {
  border-top: 1px solid var(--border);
}

.web-operator-layout__panel--grow {
  flex: 1;
  min-height: 0;
  overflow: hidden;
}
```

## 3. 暂时保留 BrowserViewManager

不要删除：

```txt id="thg1g2"
src/main/browser/browser-view-manager.ts
src/main/browser/browser-controller.ts
src/main/browser/browser-ipc.ts
```

原因：当前 `BrowserViewManager` 是单 WebContentsView manager，`BrowserController` 全部依赖它。

本阶段只让 UI 的 viewport 走 `ShellViewManager`。Browser tool 的底层迁移放到第三阶段，用 adapter 做兼容：

```ts id="83d387"
class ShellBrowserViewAdapter {
  createView(url)
  navigate(url)
  destroyView()
  updateBounds(bounds)
  getExternalWebContents()
  isReady()
}
```

---

# 六、验收命令

```bash id="d81m4t"
npm run typecheck
npm run lint
npm run dev
```

---

# 七、第二阶段验收标准

```txt id="7qwedg"
1. MainPage 顶部 sidebar toggle 可在 expanded / rail / hidden 三态切换
2. rail 模式下 Sidebar 只显示 icon，不显示 label
3. hidden 模式下 WorkspaceOutlet 占满横向空间
4. 顶部 MainViewTabs 显示：
   - Portal
   - Workspace
   - Web Operator
   - specialist-workspace 类型 profile entry
5. 点击 profile tab 能进入 profile-workspace:{profileId}
6. 点击 Web Operator tab 能进入 WebOperatorScreen
7. WebOperatorScreen 内 WebContentsHost layerId="web-operator" bounds 正常同步
8. window.shellView.create / loadUrl / focus / destroy / getState / getAll 可在 renderer 调用
9. 不破坏 aios-home WebContentsHost
10. 不删除 BrowserViewManager，browser tool 仍可继续工作
```

---

# 八、第三阶段预留

第二阶段完成后，再做：

```txt id="kq11q6"
Phase 3.1：ShellBrowserViewAdapter 替代 BrowserViewManager
Phase 3.2：browser.open 自动激活 MainPage Web Operator tab
Phase 3.3：多 external-browser tab，每个 tab 一个 ShellView layerId
Phase 3.4：Tab close / restore / reload
Phase 3.5：Tab DnD 排序，引入 @dnd-kit/sortable
```

当前第二阶段不要引入 DnD，不要删除 `BrowserViewManager`，不要修改已有 browser IPC channel 字符串。
