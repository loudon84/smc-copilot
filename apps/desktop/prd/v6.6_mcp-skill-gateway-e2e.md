# PRD：Copilot Desktop × nodeskclaw MCP Skill Gateway 集成方案

版本：v6.6_mcp-skill-gateway-e2e
范围：copilot-desktop + nodeskclaw
目标：让 Copilot Desktop 通过本地 MCP Proxy 对接 nodeskclaw MCP Skill Gateway，使本地 Hermes Agent 能发现并调用 nodeskclaw 暴露的 Hermes 实例、Skill、GeneHub 工具能力。

---

## 0. 总体定位

### 0.1 当前事实

nodeskclaw 已经完成：

1. 绑定 Docker Hermes Agent 实例。
2. 读取 Hermes 容器运行状态。
3. 读取 WebUI 健康状态。
4. 管理 Hermes skills 列表。
5. 支持安装内置 skill、上传 ZIP、Git 安装。
6. 已有 GeneHub：Gene manifest 中可包含 `skill.name`、`skill.content`、`tool_allow`、`mcp_servers`、`runtime_config`。

copilot-desktop 已经完成：

1. 登录 nodeskclaw backend。
2. 本地 token vault。
3. MCP Skill Gateway Runtime。
4. 本地 Proxy。
5. 将本地 Proxy 注册到 Hermes profile 的 `mcp_servers.mcp_skill_gateway`。
6. 本地 Hermes profile 与 skills 目录管理。
7. Hermes Skills 页面。
8. MCP Gateway 页面基础状态展示。

### 0.2 本阶段目标

本阶段不是做 GeneHub 市场页面，也不是做 skill 发布平台。

本阶段只做一条核心链路：

```text
Copilot Desktop
  → 本地 MCP Proxy
  → nodeskclaw /api/v1/hermes/mcp
  → tools/list
  → tools/call
  → Hermes Agent 可调用 nodeskclaw tools
```

### 0.3 后续阶段关系

```text
v6.6_mcp-skill-gateway-e2e
  先打通 MCP Gateway 调用链路

v6.6.1_genehub-skill-registration
  再实现 GeneHub skill 注册到 Hermes profile

v6.6.2_genehub-skill-marketplace
  最后做 Desktop 侧 GeneHub UI、安装、发布、审核
```

---

# 子 PRD A：copilot-desktop

## A1. 产品目标

在 copilot-desktop 中完成 nodeskclaw MCP Skill Gateway 的端到端接入：

1. Desktop 能从登录配置中识别 nodeskclaw backend。
2. Desktop 启动本地 MCP Proxy。
3. Desktop 本地 Proxy 能转发 JSON-RPC 到 nodeskclaw `/api/v1/hermes/mcp`。
4. Desktop 可执行 MCP `initialize`、`tools/list`、`tools/call` 测试。
5. Desktop 能将本地 Proxy 注册到 Hermes `default` profile。
6. Hermes Gateway 重启后，可通过 Hermes Agent 调用 nodeskclaw MCP tools。
7. Desktop 页面能展示工具数量、工具列表、注册状态、诊断结果与错误原因。

---

## A2. 功能范围

### A2.1 保留现有模块

继续复用：

```text
src/main/mcp-skill-gateway-runtime/
src/shared/mcp-skill-gateway-runtime/
src/preload/mcp-skill-gateway-runtime-api.ts
src/renderer/src/screens/Hermes/pages/McpGateway/
src/renderer/src/screens/Hermes/hooks/useMcpSkillGatewayRuntime.ts
```

### A2.2 新增能力

新增：

```text
1. MCP Gateway 一键诊断
2. MCP tools/list 结果展示
3. MCP invoke test
4. 注册 profile 后的 Hermes restart 提示
5. nodeskclaw MCP descriptor 检查
6. MCP Gateway 错误分层
7. 工具调用审计记录
```

---

## A3. 页面设计

页面：`HermesMcpGatewayPage`

新增 4 个区域。

### A3.1 Gateway 总览

字段：

```text
登录状态
backendBaseUrl
remoteMcpUrl
localProxyUrl
proxyStatus
gatewayStatus
toolCount
registeredProfileCount
hermesRestartRequired
lastError
```

### A3.2 一键诊断

按钮：

```text
一键检查 MCP Gateway
```

执行顺序：

```text
1. desktopAuth.getState()
2. mcpSkillGatewayRuntime.getStatus()
3. startProxy()
4. testProxy()
5. testRemoteMcp()
6. registerToProfile("default")
7. listProfileRegistrations()
8. tools/list preview
```

诊断结果：

```json
{
  "auth": "ok",
  "backend": "ok",
  "localProxy": "ok",
  "remoteMcp": "connected",
  "toolCount": 4,
  "defaultProfileRegistered": true,
  "hermesRestartRequired": true
}
```

### A3.3 MCP Tools Preview

展示字段：

```text
tool.name
tool.description
tool.inputSchema
tool.source
tool.riskLevel
tool.enabled
lastSyncedAt
```

首批工具：

```text
hermes.instances.list
hermes.instance.status
hermes.skills.list
```

### A3.4 Invoke Test

默认测试：

```text
toolName: hermes.skills.list
input:
{
  "instance_ref": "zhang-zhen"
}
```

输出：

```text
ok / failed
durationMs
result JSON
errorCode
errorMessage
```

---

## A4. Main Process 设计

### A4.1 新增模块

```text
src/main/mcp-skill-gateway-runtime/
  mcp-gateway-diagnostics.ts
  mcp-gateway-tools-cache.ts
  mcp-gateway-invoke-test.ts
```

### A4.2 诊断方法

```ts
runMcpSkillGatewayDiagnostics(): Promise<McpGatewayDiagnosticsResult>
```

返回：

```ts
interface McpGatewayDiagnosticsResult {
  ok: boolean;
  auth: CheckResult;
  backend: CheckResult;
  localProxy: CheckResult;
  remoteMcp: CheckResult;
  hermesRegistration: CheckResult;
  toolCount: number;
  tools: McpGatewayToolPreview[];
  hermesRestartRequired: boolean;
  errors: DiagnosticError[];
}
```

### A4.3 Tools Cache

本地缓存：

```text
~/.hermes/desktop/mcp-skill-gateway-tools.json
```

缓存字段：

```ts
interface McpGatewayToolsCache {
  backendBaseUrl: string;
  remoteMcpUrl: string;
  tools: McpGatewayToolPreview[];
  updatedAt: string;
}
```

缓存 TTL：

```text
60 秒
```

---

## A5. Preload API

扩展：

```ts
window.mcpSkillGatewayRuntime.runDiagnostics()
window.mcpSkillGatewayRuntime.listRemoteTools()
window.mcpSkillGatewayRuntime.invokeRemoteTool(input)
```

IPC：

```text
mcp-skill-gateway-runtime:run-diagnostics
mcp-skill-gateway-runtime:list-remote-tools
mcp-skill-gateway-runtime:invoke-remote-tool
```

强制同步：

```text
src/preload/index.d.ts
src/shared/mcp-skill-gateway-runtime/mcp-skill-gateway-runtime-contract.ts
docs/API_CONTRACTS.md
```

---

## A6. Hermes Profile 注册规则

注册目标：

```yaml
mcp_servers:
  mcp_skill_gateway:
    type: http
    url: http://127.0.0.1:48742/mcp
    enabled: true
```

禁止：

```text
1. 禁止写入 nodeskclaw backend URL。
2. 禁止写入 accessToken。
3. 禁止 Renderer 直接请求 nodeskclaw MCP。
4. 禁止 Hermes Agent 直接访问远端 nodeskclaw MCP。
```

原因：

```text
所有远端认证只能由 Desktop 本地 Proxy 注入。
Hermes config.yaml 只保存本地 Proxy URL。
```

---

## A7. 错误码

新增错误码：

```ts
type McpGatewayDiagnosticsErrorCode =
  | "MCP_DIAG_AUTH_REQUIRED"
  | "MCP_DIAG_BACKEND_UNREACHABLE"
  | "MCP_DIAG_DESCRIPTOR_MISSING"
  | "MCP_DIAG_PROXY_NOT_RUNNING"
  | "MCP_DIAG_REMOTE_INITIALIZE_FAILED"
  | "MCP_DIAG_TOOLS_LIST_FAILED"
  | "MCP_DIAG_PROFILE_NOT_REGISTERED"
  | "MCP_DIAG_HERMES_RESTART_REQUIRED"
  | "MCP_DIAG_TOOL_CALL_FAILED";
```

---

## A8. 安全要求

1. Token 不进入 Renderer。
2. Token 不进入 Hermes config.yaml。
3. Proxy 只监听 `127.0.0.1`。
4. Proxy request body 限制大小。
5. MCP invoke test 默认只允许只读工具。
6. 写操作工具需要用户确认。
7. 工具调用写入 audit log。
8. 错误日志不输出 Authorization header。

---

## A9. 验收标准

### A9.1 本地 Proxy

```text
1. Start Proxy 成功。
2. GET /health 返回 ok=true。
3. 未登录时返回 MCP_GATEWAY_NOT_LOGGED_IN。
4. 登录后返回 backendBaseUrl、remoteMcpUrl、localMcpUrl。
```

### A9.2 远端 MCP

```text
1. testRemoteMcp() 返回 ok=true。
2. tools/list 返回 toolCount > 0。
3. 工具列表至少包含 hermes.instances.list、hermes.instance.status、hermes.skills.list。
```

### A9.3 Hermes 注册

```text
1. registerToProfile("default") 成功。
2. default profile config.yaml 写入 mcp_servers.mcp_skill_gateway。
3. 写入 URL 为 http://127.0.0.1:48742/mcp。
4. config.yaml 不包含 token。
5. 已运行 Gateway 时显示 hermesRestartRequired=true。
```

### A9.4 Hermes Agent 调用

```text
1. 重启 Hermes Gateway。
2. Hermes Agent 能发现 mcp_skill_gateway。
3. Hermes Agent 可调用 hermes.skills.list。
4. nodeskclaw 后端可看到 MCP 调用日志。
```

---

## A10. Cursor 执行文件

```text
src/shared/mcp-skill-gateway-runtime/mcp-skill-gateway-runtime-contract.ts
src/main/mcp-skill-gateway-runtime/mcp-skill-gateway-proxy.ts
src/main/mcp-skill-gateway-runtime/mcp-skill-gateway-config.ts
src/main/mcp-skill-gateway-runtime/mcp-skill-gateway-runtime-ipc.ts
src/preload/mcp-skill-gateway-runtime-api.ts
src/preload/index.d.ts
src/renderer/src/screens/Hermes/hooks/useMcpSkillGatewayRuntime.ts
src/renderer/src/screens/Hermes/pages/McpGateway/HermesMcpGatewayPage.tsx
docs/API_CONTRACTS.md
tests/
```

---

# 子 PRD B：nodeskclaw

## B1. 产品目标

在 nodeskclaw 中提供稳定的 MCP Skill Gateway，使外部 Copilot Desktop 可通过本地 Proxy 调用 nodeskclaw 的 Hermes 管理能力。

第一阶段仅开放只读工具：

```text
hermes.instances.list
hermes.instance.status
hermes.skills.list
```

第二阶段开放受控写操作：

```text
hermes.skills.install_builtin
hermes.skills.install_zip
hermes.skills.install_git
hermes.skills.uninstall
hermes.instance.restart
```

第三阶段开放 GeneHub 注册能力：

```text
genehub.skills.search
genehub.skill.detail
genehub.skill.register_to_hermes
genehub.registration.status
```

---

## B2. API 范围

### B2.1 System Info

接口：

```http
GET /api/v1/system/info
```

必须返回 MCP descriptor：

```json
{
  "service": "nodeskclaw",
  "version": "x.y.z",
  "mcp": {
    "endpoint": "/api/v1/hermes/mcp",
    "transport": "streamable_http",
    "protocolVersion": "2025-06-18",
    "healthEndpoint": "/api/v1/mcp/health"
  }
}
```

### B2.2 MCP Health

接口：

```http
GET /api/v1/mcp/health
```

返回：

```json
{
  "ok": true,
  "service": "nodeskclaw-mcp-skill-gateway",
  "status": "running",
  "tools": {
    "count": 3
  }
}
```

### B2.3 MCP Gateway

接口：

```http
POST /api/v1/hermes/mcp
```

支持 JSON-RPC：

```text
initialize
tools/list
tools/call
```

---

## B3. MCP Protocol 行为

### B3.1 initialize

请求：

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "initialize",
  "params": {
    "protocolVersion": "2025-06-18",
    "capabilities": {},
    "clientInfo": {
      "name": "smc-copilot-desktop",
      "version": "v6.6"
    }
  }
}
```

返回：

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {
    "protocolVersion": "2025-06-18",
    "serverInfo": {
      "name": "nodeskclaw-mcp-skill-gateway",
      "version": "v1"
    },
    "capabilities": {
      "tools": {}
    }
  }
}
```

### B3.2 tools/list

返回：

```json
{
  "jsonrpc": "2.0",
  "id": 2,
  "result": {
    "tools": [
      {
        "name": "hermes.instances.list",
        "description": "List bound Hermes Docker instances",
        "inputSchema": {
          "type": "object",
          "properties": {}
        }
      },
      {
        "name": "hermes.instance.status",
        "description": "Get runtime status of a bound Hermes instance",
        "inputSchema": {
          "type": "object",
          "properties": {
            "instance_ref": { "type": "string" }
          },
          "required": ["instance_ref"]
        }
      },
      {
        "name": "hermes.skills.list",
        "description": "List installed skills of a bound Hermes instance",
        "inputSchema": {
          "type": "object",
          "properties": {
            "instance_ref": { "type": "string" }
          },
          "required": ["instance_ref"]
        }
      }
    ]
  }
}
```

---

## B4. Tool 设计

### B4.1 hermes.instances.list

用途：

```text
返回当前用户 / 当前组织可访问的 Hermes Docker 实例。
```

输入：

```json
{}
```

输出：

```json
{
  "instances": [
    {
      "instance_id": "xxx",
      "profile": "zhang-zhen",
      "container_name": "hermes-zhang-zhen",
      "webui_url": "http://192.168.102.247:8788",
      "docker_status": "running",
      "docker_health": "healthy",
      "webui_health": "healthy",
      "binding_type": "external"
    }
  ]
}
```

### B4.2 hermes.instance.status

输入：

```json
{
  "instance_ref": "zhang-zhen"
}
```

`instance_ref` 解析顺序：

```text
1. instance_id
2. container_name
3. profile
4. alias
```

输出：

```json
{
  "instance": {
    "instance_id": "xxx",
    "profile": "zhang-zhen",
    "container_name": "hermes-zhang-zhen",
    "docker_status": "running",
    "docker_health": "healthy",
    "webui_health": "healthy",
    "webui_url": "http://192.168.102.247:8788"
  }
}
```

### B4.3 hermes.skills.list

输入：

```json
{
  "instance_ref": "zhang-zhen"
}
```

输出：

```json
{
  "skills": [
    {
      "slug": "apple",
      "name": "apple",
      "version": null,
      "status": "installed",
      "source": "manual"
    }
  ]
}
```

---

## B5. 权限模型

工具分级：

```text
read:
  hermes.instances.list
  hermes.instance.status
  hermes.skills.list

write:
  hermes.skills.install_builtin
  hermes.skills.install_zip
  hermes.skills.install_git
  hermes.skills.uninstall

admin:
  hermes.instance.restart
  hermes.instance.rebind
```

本阶段只开放 `read`。

权限检查：

```text
1. accessToken 必须有效。
2. 用户必须属于当前 org。
3. 用户必须有访问该 Hermes 实例权限。
4. tools/call 必须检查 tool permission。
5. write/admin tool 必须支持审批或二次确认。
```

---

## B6. MCP Gateway 审计

每次 MCP 调用写入审计：

```text
mcp_call_logs
```

字段：

```sql
id UUID PRIMARY KEY
org_id UUID
user_id UUID
tool_name TEXT
instance_id TEXT
status TEXT
duration_ms INTEGER
input_summary JSONB
error_code TEXT
error_message TEXT
created_at TIMESTAMPTZ
```

不得记录：

```text
1. Authorization header
2. accessToken
3. password
4. WebUI 访问密码
5. 完整敏感文件内容
```

---

## B7. GeneHub 后续预留

本阶段不实现 GeneHub 注册落盘，但要预留 tools：

```text
genehub.skills.search
genehub.skill.detail
genehub.skill.register_to_hermes
genehub.registration.status
```

其中 `register_to_hermes` 的行为不是直接安装，而是创建 registration job：

```json
{
  "registration_id": "reg_xxx",
  "status": "pending",
  "message": "Registration job created. Copilot Desktop will apply it locally."
}
```

原因：

```text
nodeskclaw 不能直接写用户本地 ~/.hermes/skills。
本地落盘必须由 Copilot Desktop Main Process 执行。
```

---

## B8. GeneHub Skill Registration 后续表设计

后续新增：

```sql
CREATE TABLE hermes_skill_registrations (
  id UUID PRIMARY KEY,
  org_id UUID NOT NULL,
  user_id UUID NOT NULL,
  desktop_id TEXT NOT NULL,
  profile_name TEXT NOT NULL,
  gene_id UUID NOT NULL,
  gene_slug TEXT NOT NULL,
  gene_version TEXT NOT NULL,
  skill_name TEXT NOT NULL,
  manifest_hash TEXT NOT NULL,
  status TEXT NOT NULL,
  requested_at TIMESTAMPTZ NOT NULL,
  claimed_at TIMESTAMPTZ,
  applied_at TIMESTAMPTZ,
  install_path TEXT,
  error_code TEXT,
  error_message TEXT,
  metadata_json JSONB
);
```

状态：

```text
pending
claimed
downloaded
validated
installed
failed
uninstalled
```

---

## B9. 验收标准

### B9.1 system info

```text
1. GET /api/v1/system/info 返回 mcp.endpoint。
2. endpoint 为 /api/v1/hermes/mcp。
3. transport 为 streamable_http。
```

### B9.2 MCP basic

```text
1. POST /api/v1/hermes/mcp initialize 成功。
2. tools/list 返回 tools 数量 >= 3。
3. tools/list 包含 hermes.instances.list、hermes.instance.status、hermes.skills.list。
```

### B9.3 tools/call

```text
1. hermes.instances.list 返回已绑定 Hermes Docker 实例。
2. hermes.instance.status 能返回 zhang-zhen 实例状态。
3. hermes.skills.list 能返回 zhang-zhen skills 清单。
```

### B9.4 权限

```text
1. 未登录返回 401。
2. 无实例权限返回 403。
3. 不存在 instance_ref 返回 404 风格业务错误。
4. 工具调用失败返回 JSON-RPC error，不返回裸异常。
```

---

# 总体验收链路

最终验收流程：

```text
1. 启动 nodeskclaw backend。
2. 确认 zhang-zhen Hermes Docker 实例 running / healthy。
3. 打开 Copilot Desktop。
4. 登录 nodeskclaw。
5. 打开 Hermes > MCP Gateway。
6. 点击 Start Proxy。
7. 点击 Test Remote MCP。
8. Desktop 显示 toolCount >= 3。
9. 点击 Register Default Profile。
10. Desktop 写入 default profile config.yaml。
11. 重启 Hermes Gateway。
12. 在 Hermes Agent 中调用 hermes.skills.list。
13. nodeskclaw 返回 zhang-zhen skills 清单。
14. nodeskclaw 审计日志记录本次调用。
```

---

# 项目切分

## copilot-desktop 任务包

```text
docs/specs/v6.6_mcp-skill-gateway-e2e/copilot-desktop/
  00-overview.md
  01-current-source-analysis.md
  02-contract.md
  03-main-runtime-diagnostics.md
  04-preload-api.md
  05-renderer-mcp-gateway-page.md
  06-hermes-profile-registration.md
  07-tests.md
  08-acceptance.md
```

## nodeskclaw 任务包

```text
docs/specs/v6.6_mcp-skill-gateway-e2e/nodeskclaw/
  00-overview.md
  01-current-source-analysis.md
  02-system-info-mcp-descriptor.md
  03-mcp-jsonrpc-gateway.md
  04-hermes-tools.md
  05-permission-audit.md
  06-genehub-registration-reserved.md
  07-tests.md
  08-acceptance.md
```

---

# Cursor 总执行提示词

你现在执行 v6.6_mcp-skill-gateway-e2e。

目标：
让 Copilot Desktop 通过本地 MCP Skill Gateway Proxy 调用 nodeskclaw /api/v1/hermes/mcp，并将该 Proxy 注册到 Hermes Agent default profile，使 Hermes Agent 可以调用 nodeskclaw 暴露的 Hermes Docker 实例与 skills 工具。

分两个子项目执行：

1. copilot-desktop：

   * 增强 MCP Gateway Runtime。
   * 增加一键诊断。
   * 增加 tools/list 展示。
   * 增加 invoke test。
   * 保证 token 只由本地 Proxy 注入。
   * 注册到 Hermes profile 时只写 127.0.0.1 本地 URL。

2. nodeskclaw：

   * /api/v1/system/info 返回 MCP descriptor。
   * /api/v1/hermes/mcp 支持 initialize、tools/list、tools/call。
   * 第一批 tools：hermes.instances.list、hermes.instance.status、hermes.skills.list。
   * 加权限校验和审计。
   * 暂不做 GeneHub skill 落盘，只预留 registration job 设计。

禁止：

1. Renderer 直接请求 nodeskclaw MCP。
2. token 写入 Hermes config.yaml。
3. Hermes Agent 直接访问 nodeskclaw 远端 MCP。
4. 本阶段做 GeneHub marketplace UI。
5. nodeskclaw 直接写用户本地 ~/.hermes/skills。
6. 未验证 MCP tools/list 就继续做 GeneHub skill 注册。

完成条件：

1. Desktop testRemoteMcp 返回 ok=true。
2. toolCount >= 3。
3. default profile 注册成功。
4. Hermes Gateway 重启后可调用 hermes.skills.list。
5. nodeskclaw 返回 Docker Hermes 实例的 skills 清单。
6. nodeskclaw 有调用审计。
