源码依据先固定如下：当前 `WebOperatorScreen` 已经使用 `WebContentsHost` 承载 `web-operator` 原生 WebContents 层，并带有状态、截图、页面结构、日志等侧栏能力；旧的 `BrowserViewportHost` 已标注废弃，改为 `WebContentsHost + shellView`；`use-browser-actions` 已经把 `window.aiosBrowser` 封装成 open/back/reload/getState/screenshot/click/type/extractTable 等浏览器动作。([GitHub][1])  Main 侧已有 `browser.open/click/type/get_state/screenshot` 等 IPC handler，并且当前 `web-operator` view 是 lazy create，配置为 `sandbox: true`、`contextIsolation: true`、`nodeIntegration: false`。([GitHub][2]) Electron 官方对该方向的基础能力也匹配：`WebContentsView` 用于显示并控制一个 `webContents`，`webContents` 可执行 JS、监听导航并控制页面，`contextBridge + preload` 用于安全暴露能力，IPC 是 Main 与 Renderer/WebContents 间的标准通信方式。([Electron][3])

下面是可直接给 Cursor 建实施计划的 PRD。

# copilot-desktop ver5.7.1：CRM Web 页面主动上报与 Electron 双向控制能力 PRD

## 1. 版本定位

版本号：`ver5.7.1_crm_desktop_bridge`

目标模块：

```text
src/renderer/src/screens/WebOperator/**
src/main/browser/**
src/main/shell/**
src/preload/**
src/shared/**
packages/crm-desktop-jssdk/**
```

本版本在现有 WebOperator 浏览器核心能力上增加 CRM 页面主动上报与 Electron 双向控制能力。

核心原则：

1. CRM 页面不做频繁自动上报。
2. 只有用户在 CRM 页面主动点击指定按钮时，CRM JSSDK 才允许提交当前页面上下文。
3. 默认不引入本地 WebSocket Bridge。
4. CRM JSSDK 单独产物交付给 CRM Web 系统安装，不由 Electron 打包注入。
5. Electron 侧通过 WebContentsView 专用 preload、IPC、session、webFrameMain、MessageChannelMain 的组合能力完成校验、转发、模块打开、页面回控。
6. 业务系统页面与 Electron Main Process 之间禁止直接暴露 Node/Electron 能力。

---

## 2. 当前源码状态分析

### 2.1 WebOperator Renderer 层现状

当前 `WebOperatorScreen.tsx` 已经具备以下结构：

```text
WebOperatorScreen
├─ BrowserToolbar
├─ WebContentsHost
├─ BrowserStatePanel
├─ PageStructurePanel
├─ ScreenshotPanel
├─ BrowserActionLog
└─ WebOperatorSideRail
```

现有职责：

1. 通过 `WebContentsHost` 承载原生 WebContentsView。
2. 通过 `window.aiosBrowser` 执行浏览器动作。
3. 通过侧栏展示页面状态、结构、截图、操作日志。
4. 已支持 `snapshot({ includeFrames: true, includeInteractiveElements: true })`，适合后续承接 CRM 页面上下文提交后的页面快照刷新。

需要保留：

1. WebOperator 页面结构不重构。
2. `BrowserToolbar`、`BrowserStatePanel`、`PageStructurePanel`、`BrowserActionLog` 继续保留。
3. 现有 `window.aiosBrowser` API 不破坏。

需要新增：

1. CRM 事件接收状态。
2. CRM 上报记录展示。
3. CRM 事件触发后的模块路由动作。
4. Electron -> CRM 页面的命令下发能力。

---

### 2.2 Main Process 浏览器核心现状

当前 Main 侧已有浏览器控制链路：

```text
Renderer WebOperator
  ↓ window.aiosBrowser
Preload browser-api.ts
  ↓ ipcRenderer.invoke
Main browser-ipc.ts
  ↓
BrowserController
  ↓
ShellBrowserViewAdapter / ShellViewManager
  ↓
WebContentsView.webContents
```

已存在能力：

1. 打开 URL。
2. 后退、前进、刷新。
3. 获取页面文本、input、button、link。
4. 截图。
5. click/type/table extract。
6. 操作审计。
7. 域名白名单与敏感动作确认。
8. WebOperator 使用独立 `BROWSER_PARTITION`。

不足：

1. 当前 WebOperator 的外部 WebContentsView 没有专用 CRM preload。
2. CRM 页面内的业务 JS 事件没有可信入站通道。
3. 现有 `browser-tool-server` 是本地 HTTP 工具服务，不适合作为 CRM 页面上报入口。
4. 现有 `browser.open/click/type` 偏通用浏览器动作，没有 CRM event router。
5. Electron -> CRM 页面缺少业务命令协议。
6. iframe 内业务区域需要用 `webFrameMain` 做帧级定位与操作补齐。

---

## 3. 目标业务场景

### 3.1 CRM 主动提交当前页面上下文

业务人员在 CRM 页面打开客户、商机、报价、订单等页面。

CRM 页面中已经安装 `crm-desktop-jssdk`。

用户点击 CRM 页面上的按钮：

```text
提交给 Copilot Desktop
同步到 AI 助手
打开桌面客户分析
生成报价辅助材料
```

CRM JSSDK 执行：

```text
collect current CRM context
→ call desktop bridge
→ Electron preload 校验
→ Main Process 二次校验
→ Main Process 根据事件类型打开对应 Electron 模块
```

示例：

```text
用户在 CRM 客户详情页点击「同步到桌面助手」
→ CRM JSSDK 发送 crm.context.submit
→ Electron 打开 WebOperator 侧栏 PageStructurePanel
→ Electron 保存 CRM 上下文
→ Electron 可触发 Hermes / Copilot 工作流
```

---

### 3.2 Electron 根据事件打开模块页面

事件类型与 Electron 动作映射：

```text
crm.context.submit
  → 打开 WebOperator
  → 聚焦 browser-state 或 page-structure
  → 保存 CRM 上下文

crm.customer.open-ai-panel
  → 打开客户 AI 分析模块

crm.quote.create-assist
  → 打开报价辅助模块

crm.order.risk-check
  → 打开订单风控模块

crm.page.snapshot-request
  → 调用 browser.snapshot
  → 刷新 WebOperator 页面结构侧栏
```

---

### 3.3 Electron 回控 CRM 页面

Electron 可以向当前 CRM 页面发送命令：

```text
desktop.crm.highlightField
desktop.crm.fillField
desktop.crm.focusRecord
desktop.crm.showToast
desktop.crm.requestContext
desktop.crm.scrollToSection
```

优先级：

1. 优先走 CRM JSSDK command handler。
2. CRM JSSDK 不支持时，走 BrowserController 通用 click/type/scroll。
3. 涉及 iframe 时，走 webFrameMain 定位 frame 后执行帧级脚本。
4. 涉及敏感动作时，必须进入现有敏感动作确认流程。

---

## 4. 总体架构

```text
CRM Web App
  └─ crm-desktop-jssdk
       ├─ 用户点击按钮后主动 emit
       ├─ 采集 CRM 业务上下文
       └─ 接收 Electron command

Electron WebOperator WebContentsView
  └─ crm-bridge-preload
       ├─ contextBridge 暴露最小 API
       ├─ 监听 window.postMessage
       ├─ 校验 origin / schema / payload / user gesture
       └─ ipcRenderer.invoke("crm-bridge:emit")

Electron Main Process
  ├─ crm-bridge-ipc
  ├─ crm-bridge-security
  ├─ crm-event-router
  ├─ crm-command-dispatcher
  ├─ BrowserController
  ├─ ShellViewManager
  └─ webFrameMain

Renderer Main UI
  ├─ WebOperatorScreen
  ├─ CrmEventPanel
  ├─ BrowserActionLog
  └─ target module page
```

---

## 5. 不使用本地 WebSocket Bridge 的结论

本版本默认不增加本地 WebSocket Bridge。

原因：

1. CRM 页面运行在 Electron WebContentsView 内部，已有 preload + IPC 的可信通道。
2. 上报不是高频流式数据，不需要 socket 长连接。
3. WebSocket Bridge 会引入端口占用、防火墙、CORS、鉴权、局域网暴露等额外安全面。
4. 现有项目已经有 IPC、ShellView、BrowserController、BrowserToolServer，不应再增加一套平行通信栈。

保留扩展条件：

只有在以下场景才允许后续增加 WebSocket Bridge：

1. CRM 必须在系统默认浏览器中打开，而不是 Electron WebOperator。
2. 多个外部浏览器标签页需要同时连接桌面端。
3. 需要本地桌面端作为系统级 URL Protocol / Socket 服务。
4. CRM 无法被 WebContentsView 承载。

---

## 6. 关键设计

### 6.1 WebOperator 专用 CRM preload

新增：

```text
src/preload/crm-bridge-preload.ts
```

职责：

1. 仅用于 WebOperator 外部 CRM WebContentsView。
2. 不复用主窗口 `src/preload/index.ts`。
3. 不暴露 `ipcRenderer`。
4. 不暴露 Node API。
5. 只暴露最小 CRM bridge API。
6. 监听 CRM JSSDK 的 `window.postMessage`。
7. 记录最近一次真实用户点击时间。
8. 对 CRM 上报事件做第一层校验。

preload 需要捕获真实用户手势：

```ts
let lastTrustedClickAt = 0;

window.addEventListener(
  "click",
  (event) => {
    if (event.isTrusted) {
      lastTrustedClickAt = Date.now();
    }
  },
  true,
);
```

只允许在用户点击后的时间窗口内提交：

```ts
const USER_GESTURE_WINDOW_MS = 1500;

function hasRecentTrustedGesture(): boolean {
  return Date.now() - lastTrustedClickAt <= USER_GESTURE_WINDOW_MS;
}
```

---

### 6.2 CRM JSSDK 与 preload 的通信方式

CRM JSSDK 优先调用：

```ts
window.CopilotDesktopCRM?.emit(event)
```

如果 Electron preload 未暴露 API，则退化为：

```ts
window.postMessage(
  {
    source: "copilot-crm-jssdk",
    channel: "crm.desktop.bridge",
    version: "0.1.0",
    event,
  },
  window.location.origin,
);
```

preload 同时支持两种入口：

1. `contextBridge.exposeInMainWorld("CopilotDesktopCRM", api)`
2. `window.addEventListener("message", handler)`

---

### 6.3 Main Process 二次校验

新增：

```text
src/main/crm-bridge/crm-bridge-security.ts
```

校验规则：

1. `event.senderFrame.url` 必须属于 CRM allowlist。
2. 当前发送事件的 WebContents 必须是 `web-operator` layer。
3. event type 必须在 allowlist。
4. payload 大小不能超过配置。
5. event 必须包含 `requestId`。
6. event 必须带 `trigger.type = "user-click"`。
7. 高频重复 requestId 必须去重。
8. 事件类型对应的 action 必须在配置中存在。
9. 敏感动作必须进入确认流程。

---

### 6.4 CRM 事件路由

新增：

```text
src/main/crm-bridge/crm-event-router.ts
```

输入：

```ts
CrmBridgeEvent
```

输出：

```ts
CrmBridgeRouteResult
```

事件路由表：

```ts
export const crmEventRoutes = {
  "crm.context.submit": {
    action: "open-web-operator-panel",
    focusedPanel: "page-structure",
    refreshSnapshot: true,
  },
  "crm.customer.open-ai-panel": {
    action: "open-renderer-route",
    route: "/crm/customer-ai",
  },
  "crm.quote.create-assist": {
    action: "open-renderer-route",
    route: "/crm/quote-assistant",
  },
  "crm.order.risk-check": {
    action: "open-renderer-route",
    route: "/crm/order-risk",
  },
};
```

---

### 6.5 Electron -> CRM command dispatcher

新增：

```text
src/main/crm-bridge/crm-command-dispatcher.ts
```

Main Process 发送命令到 CRM 页面：

```ts
webContents.send("crm-bridge:command", command);
```

CRM preload 接收：

```ts
ipcRenderer.on("crm-bridge:command", (_event, command) => {
  window.postMessage(
    {
      source: "copilot-desktop",
      channel: "crm.desktop.command",
      version: "0.1.0",
      command,
    },
    window.location.origin,
  );
});
```

CRM JSSDK 接收：

```ts
window.CopilotCrmDesktopSDK.onCommand((command) => {
  // CRM Web App 注册业务处理器
});
```

---

### 6.6 iframe 处理

新增或增强：

```text
src/main/browser/browser-frame-service.ts
```

能力：

1. 使用 `webContents.mainFrame.frames` 获取 frame 树。
2. 使用 `webFrameMain.fromId(processId, routingId)` 定位指定 frame。
3. 对同源 iframe 执行业务命令。
4. 对跨源 iframe 仅允许有限只读元信息。
5. 如果 CRM JSSDK 安装在 iframe 内，则 iframe 内 JSSDK 通过 `window.parent.postMessage` 转给主页面，再由主页面 JSSDK 转交 Electron preload。
6. 禁止对未知跨源 iframe 执行写入类动作。

---

## 7. 数据协议

### 7.1 CRM 上报事件 DTO

新增：

```text
src/shared/crm-bridge/crm-bridge-contract.ts
```

```ts
export type CrmBridgeEventType =
  | "crm.context.submit"
  | "crm.customer.open-ai-panel"
  | "crm.quote.create-assist"
  | "crm.order.risk-check"
  | "crm.page.snapshot-request";

export interface CrmBridgeTrigger {
  type: "user-click";
  elementId?: string;
  label?: string;
  timestamp: string;
}

export interface CrmPageContext {
  app: "crm";
  entityType?: "customer" | "contact" | "lead" | "opportunity" | "quote" | "order";
  entityId?: string;
  entityName?: string;
  url: string;
  title?: string;
  selectedIds?: string[];
  fields?: Record<string, string | number | boolean | null>;
  safeText?: string;
}

export interface CrmBridgeEvent {
  source: "crm-web";
  sdkVersion: string;
  requestId: string;
  type: CrmBridgeEventType;
  trigger: CrmBridgeTrigger;
  page: CrmPageContext;
  payload?: Record<string, unknown>;
}

export interface CrmBridgeResult {
  ok: boolean;
  requestId: string;
  action?: string;
  message?: string;
  errorCode?:
    | "CRM_BRIDGE_DISABLED"
    | "ORIGIN_NOT_ALLOWED"
    | "EVENT_TYPE_NOT_ALLOWED"
    | "PAYLOAD_TOO_LARGE"
    | "USER_GESTURE_REQUIRED"
    | "INVALID_SCHEMA"
    | "WEB_OPERATOR_NOT_READY"
    | "ROUTE_NOT_FOUND"
    | "UNKNOWN_ERROR";
}
```

---

### 7.2 Electron 下发 CRM 命令 DTO

```ts
export type CrmDesktopCommandType =
  | "desktop.crm.showToast"
  | "desktop.crm.highlightField"
  | "desktop.crm.focusField"
  | "desktop.crm.fillField"
  | "desktop.crm.scrollToSection"
  | "desktop.crm.requestContext";

export interface CrmDesktopCommand {
  commandId: string;
  type: CrmDesktopCommandType;
  target?: {
    selector?: string;
    fieldName?: string;
    entityId?: string;
    frameId?: string;
  };
  payload?: Record<string, unknown>;
  createdAt: string;
}
```

---

## 8. 配置文件

新增：

```text
resources/crm-bridge/crm-bridge.config.json
```

示例：

```json
{
  "enabled": true,
  "allowedOrigins": [
    "https://crm.company.com",
    "https://*.crm.company.com",
    "http://localhost:9527"
  ],
  "payloadMaxBytes": 524288,
  "trustedGestureWindowMs": 1500,
  "allowedEventTypes": [
    "crm.context.submit",
    "crm.customer.open-ai-panel",
    "crm.quote.create-assist",
    "crm.order.risk-check",
    "crm.page.snapshot-request"
  ],
  "routes": {
    "crm.context.submit": {
      "action": "open-web-operator-panel",
      "focusedPanel": "page-structure",
      "refreshSnapshot": true
    },
    "crm.customer.open-ai-panel": {
      "action": "open-renderer-route",
      "route": "/crm/customer-ai"
    },
    "crm.quote.create-assist": {
      "action": "open-renderer-route",
      "route": "/crm/quote-assistant"
    },
    "crm.order.risk-check": {
      "action": "open-renderer-route",
      "route": "/crm/order-risk"
    }
  }
}
```

---

## 9. 文件级改造清单

### 9.1 新增 shared contract

```text
src/shared/crm-bridge/
├─ crm-bridge-contract.ts
├─ crm-bridge-events.ts
├─ crm-bridge-errors.ts
└─ crm-bridge-schema.ts
```

职责：

1. 定义 DTO。
2. 定义事件类型。
3. 定义错误码。
4. 提供 schema 校验函数。

---

### 9.2 新增 preload

```text
src/preload/crm-bridge-preload.ts
```

职责：

1. 暴露 `window.CopilotDesktopCRM`。
2. 监听 `window.postMessage`。
3. 校验 user gesture。
4. 校验基础 schema。
5. 调用 `ipcRenderer.invoke("crm-bridge:emit", event)`。
6. 接收 `crm-bridge:command` 并转发给 CRM 页面。

---

### 9.3 修改 WebOperator WebContentsView 创建逻辑

修改：

```text
src/main/shell/shell-view-ipc.ts
src/main/browser/shell-browser-view-adapter.ts
src/main/shell/views/managed-webcontents-view.ts
```

目标：

1. `web-operator` layer 创建时带上 CRM bridge preload。
2. 保留 `sandbox: true`。
3. 保留 `contextIsolation: true`。
4. 保留 `nodeIntegration: false`。
5. 继续使用 `BROWSER_PARTITION`。
6. 仅 `kind === "web-operator"` 时启用该 preload。
7. portal/internal view 不加载该 preload。

示例逻辑：

```ts
const preload = resolveCrmBridgePreloadPath();

await svm.createView("web-operator", "web-operator", url, {
  layer: "content",
  partition: BROWSER_PARTITION,
  sandbox: true,
  contextIsolation: true,
  nodeIntegration: false,
  preload,
});
```

---

### 9.4 新增 Main Process CRM bridge

```text
src/main/crm-bridge/
├─ crm-bridge-config.ts
├─ crm-bridge-security.ts
├─ crm-bridge-ipc.ts
├─ crm-event-router.ts
├─ crm-command-dispatcher.ts
├─ crm-event-store.ts
└─ index.ts
```

职责：

1. 注册 IPC handler。
2. 读取配置。
3. 校验 origin、event type、payload size。
4. 保存事件。
5. 调用 event router。
6. 通知 Renderer 更新 UI。
7. 支持 Electron 下发 CRM command。

IPC channels：

```ts
crm-bridge:emit
crm-bridge:get-last-event
crm-bridge:list-events
crm-bridge:send-command
crm-bridge:on-event
crm-bridge:command
```

---

### 9.5 Renderer UI 增强

新增：

```text
src/renderer/src/screens/WebOperator/CrmEventPanel.tsx
src/renderer/src/screens/WebOperator/hooks/use-crm-bridge-events.ts
```

修改：

```text
src/renderer/src/screens/WebOperator/WebOperatorScreen.tsx
src/renderer/src/screens/WebOperator/WebOperatorSideRail.tsx
```

UI 目标：

1. WebOperator 侧栏增加 `CRM Context` 面板。
2. 展示最近一次 CRM 事件。
3. 展示 entityType、entityId、entityName、url、trigger、timestamp。
4. 提供「刷新页面结构」「发送命令到 CRM」「复制上下文」按钮。
5. 事件触发后自动聚焦目标 panel。
6. 不影响现有 BrowserStatePanel、PageStructurePanel、ScreenshotPanel、BrowserActionLog。

---

### 9.6 CRM JSSDK 独立包

新增：

```text
packages/crm-desktop-jssdk/
├─ package.json
├─ tsconfig.json
├─ src/index.ts
├─ src/types.ts
├─ src/dom.ts
├─ dist/crm-desktop-jssdk.iife.js
├─ dist/crm-desktop-jssdk.esm.js
└─ README.md
```

构建目标：

1. IIFE：给传统 CRM `<script>` 标签直接使用。
2. ESM：给现代前端工程安装使用。
3. 不依赖 Electron。
4. 不依赖 Node。
5. 在普通浏览器中运行时自动 no-op。
6. 在 Electron WebOperator 中运行时连接 preload。

---

## 10. CRM JSSDK 参考实现

文件：

```text
packages/crm-desktop-jssdk/src/index.ts
```

```ts
export type CrmDesktopEventType =
  | "crm.context.submit"
  | "crm.customer.open-ai-panel"
  | "crm.quote.create-assist"
  | "crm.order.risk-check"
  | "crm.page.snapshot-request";

export interface CrmDesktopPageContext {
  app: "crm";
  entityType?: string;
  entityId?: string;
  entityName?: string;
  url?: string;
  title?: string;
  selectedIds?: string[];
  fields?: Record<string, string | number | boolean | null>;
  safeText?: string;
}

export interface CrmDesktopEmitOptions {
  requestId?: string;
  triggerElementId?: string;
  triggerLabel?: string;
}

export interface CrmDesktopCommand {
  commandId: string;
  type: string;
  target?: Record<string, unknown>;
  payload?: Record<string, unknown>;
  createdAt: string;
}

declare global {
  interface Window {
    CopilotDesktopCRM?: {
      emit: (event: unknown) => Promise<{ ok: boolean; message?: string }>;
      isAvailable?: () => boolean;
      version?: string;
    };
    CopilotCrmDesktopSDK?: typeof CopilotCrmDesktopSDK;
  }
}

const SDK_VERSION = "0.1.0";
const MESSAGE_CHANNEL = "crm.desktop.bridge";
const COMMAND_CHANNEL = "crm.desktop.command";

function createRequestId(): string {
  return `crm_${Date.now()}_${Math.random().toString(16).slice(2)}`;
}

function normalizePageContext(input: CrmDesktopPageContext): CrmDesktopPageContext {
  return {
    ...input,
    app: "crm",
    url: input.url ?? window.location.href,
    title: input.title ?? document.title,
  };
}

async function emit(
  type: CrmDesktopEventType,
  page: CrmDesktopPageContext,
  payload?: Record<string, unknown>,
  options?: CrmDesktopEmitOptions,
): Promise<{ ok: boolean; message?: string }> {
  const event = {
    source: "crm-web",
    sdkVersion: SDK_VERSION,
    requestId: options?.requestId ?? createRequestId(),
    type,
    trigger: {
      type: "user-click",
      elementId: options?.triggerElementId,
      label: options?.triggerLabel,
      timestamp: new Date().toISOString(),
    },
    page: normalizePageContext(page),
    payload,
  };

  if (window.CopilotDesktopCRM?.emit) {
    return await window.CopilotDesktopCRM.emit(event);
  }

  window.postMessage(
    {
      source: "copilot-crm-jssdk",
      channel: MESSAGE_CHANNEL,
      version: SDK_VERSION,
      event,
    },
    window.location.origin,
  );

  return {
    ok: false,
    message: "Desktop bridge is not available in this browser context",
  };
}

function submitPageContext(
  page: CrmDesktopPageContext,
  payload?: Record<string, unknown>,
  options?: CrmDesktopEmitOptions,
): Promise<{ ok: boolean; message?: string }> {
  return emit("crm.context.submit", page, payload, options);
}

function onCommand(handler: (command: CrmDesktopCommand) => void): () => void {
  const listener = (event: MessageEvent) => {
    if (event.origin !== window.location.origin) return;
    const data = event.data;
    if (!data || data.source !== "copilot-desktop") return;
    if (data.channel !== COMMAND_CHANNEL) return;
    if (!data.command) return;
    handler(data.command as CrmDesktopCommand);
  };

  window.addEventListener("message", listener);
  return () => window.removeEventListener("message", listener);
}

function isDesktopAvailable(): boolean {
  return Boolean(window.CopilotDesktopCRM?.emit);
}

export const CopilotCrmDesktopSDK = {
  version: SDK_VERSION,
  emit,
  submitPageContext,
  onCommand,
  isDesktopAvailable,
};

window.CopilotCrmDesktopSDK = CopilotCrmDesktopSDK;
```

---

## 11. CRM 页面接入示例

CRM 页面安装：

```html
<script src="/static/crm-desktop-jssdk.iife.js"></script>
```

CRM 按钮：

```html
<button id="sync-to-desktop">同步到桌面助手</button>
```

CRM 页面调用：

```js
document.getElementById("sync-to-desktop").addEventListener("click", async () => {
  const result = await window.CopilotCrmDesktopSDK.submitPageContext(
    {
      app: "crm",
      entityType: "customer",
      entityId: window.crmState.customerId,
      entityName: window.crmState.customerName,
      selectedIds: window.crmState.selectedContactIds,
      fields: {
        owner: window.crmState.owner,
        level: window.crmState.level,
        country: window.crmState.country,
      },
      safeText: document.querySelector("#customer-summary")?.innerText?.slice(0, 5000),
    },
    {
      sourceModule: "customer-detail",
    },
    {
      triggerElementId: "sync-to-desktop",
      triggerLabel: "同步到桌面助手",
    },
  );

  console.log(result);
});
```

CRM 接收 Electron 命令：

```js
window.CopilotCrmDesktopSDK.onCommand((command) => {
  if (command.type === "desktop.crm.showToast") {
    window.crmToast(command.payload?.message || "Desktop command received");
  }

  if (command.type === "desktop.crm.highlightField") {
    const selector = command.target?.selector;
    if (selector) {
      document.querySelector(selector)?.classList.add("desktop-highlight");
    }
  }
});
```

---

## 12. Electron preload 参考骨架

文件：

```text
src/preload/crm-bridge-preload.ts
```

```ts
import { contextBridge, ipcRenderer } from "electron";

const SDK_SOURCE = "copilot-crm-jssdk";
const EVENT_CHANNEL = "crm.desktop.bridge";
const COMMAND_CHANNEL = "crm.desktop.command";
const USER_GESTURE_WINDOW_MS = 1500;

let lastTrustedClickAt = 0;

window.addEventListener(
  "click",
  (event) => {
    if (event.isTrusted) {
      lastTrustedClickAt = Date.now();
    }
  },
  true,
);

function hasRecentTrustedGesture(): boolean {
  return Date.now() - lastTrustedClickAt <= USER_GESTURE_WINDOW_MS;
}

function assertTrustedGesture(): void {
  if (!hasRecentTrustedGesture()) {
    throw new Error("USER_GESTURE_REQUIRED");
  }
}

async function emit(event: unknown): Promise<{ ok: boolean; message?: string }> {
  try {
    assertTrustedGesture();
    return await ipcRenderer.invoke("crm-bridge:emit", {
      event,
      location: {
        href: window.location.href,
        origin: window.location.origin,
        title: document.title,
      },
      receivedAt: new Date().toISOString(),
    });
  } catch (error) {
    return {
      ok: false,
      message: error instanceof Error ? error.message : String(error),
    };
  }
}

contextBridge.exposeInMainWorld("CopilotDesktopCRM", {
  version: "0.1.0",
  isAvailable: () => true,
  emit,
});

window.addEventListener("message", (messageEvent) => {
  if (messageEvent.origin !== window.location.origin) return;

  const data = messageEvent.data;
  if (!data || data.source !== SDK_SOURCE) return;
  if (data.channel !== EVENT_CHANNEL) return;
  if (!data.event) return;

  void emit(data.event);
});

ipcRenderer.on("crm-bridge:command", (_event, command) => {
  window.postMessage(
    {
      source: "copilot-desktop",
      channel: COMMAND_CHANNEL,
      version: "0.1.0",
      command,
    },
    window.location.origin,
  );
});
```

---

## 13. Main Process IPC 参考骨架

文件：

```text
src/main/crm-bridge/crm-bridge-ipc.ts
```

```ts
import { ipcMain, WebContents } from "electron";
import { validateCrmBridgeEvent } from "./crm-bridge-security";
import { routeCrmBridgeEvent } from "./crm-event-router";
import { insertCrmBridgeEvent, listCrmBridgeEvents } from "./crm-event-store";
import { dispatchCrmCommand } from "./crm-command-dispatcher";

export function registerCrmBridgeIpc(): void {
  ipcMain.handle("crm-bridge:emit", async (event, input) => {
    const sender = event.sender;

    const validation = validateCrmBridgeEvent({
      sender,
      frame: event.senderFrame,
      input,
    });

    if (!validation.ok) {
      return validation;
    }

    const bridgeEvent = validation.event;

    insertCrmBridgeEvent({
      ...bridgeEvent,
      webContentsId: sender.id,
      createdAt: new Date().toISOString(),
    });

    const routeResult = await routeCrmBridgeEvent({
      sender,
      event: bridgeEvent,
    });

    return {
      ok: routeResult.ok,
      requestId: bridgeEvent.requestId,
      action: routeResult.action,
      message: routeResult.message,
    };
  });

  ipcMain.handle("crm-bridge:list-events", async (_event, limit = 20) => {
    return listCrmBridgeEvents(limit);
  });

  ipcMain.handle("crm-bridge:send-command", async (_event, command) => {
    return dispatchCrmCommand(command);
  });
}
```

---

## 14. Electron 打开模块的动作设计

CRM 事件触发后，Main Process 不直接操作 React Router。

Main Process 应通过已有主窗口事件转发机制通知 Renderer：

```ts
mainWindow.webContents.send("crm-bridge:on-event", {
  event,
  routeAction,
});
```

Renderer 收到后执行：

```ts
window.mainPageState 或现有 navigation store
→ 切换到 WebOperator / CRM 模块
→ 聚焦侧栏 panel
→ 刷新 snapshot
```

如果现有主导航状态不支持外部事件切换，需要新增一个最小 API：

```text
src/preload/main-page-state-api.ts
```

新增方法：

```ts
openModule(moduleId: string, options?: Record<string, unknown>): Promise<void>
```

或复用现有 MainPageState IPC。

---

## 15. 安全边界

### 15.1 禁止项

1. 禁止 CRM 页面直接访问 `ipcRenderer`。
2. 禁止 CRM 页面直接访问 Node API。
3. 禁止 Electron 给任意网站暴露 CRM bridge。
4. 禁止自动定时上报页面全文。
5. 禁止默认开启 WebSocket Bridge。
6. 禁止将 password、token、cookie、Authorization header 放入 CRM payload。
7. 禁止跨源 iframe 写操作。
8. 禁止未确认的敏感 click/type 操作。
9. 禁止 payload 超过配置大小。
10. 禁止 `eval` 执行 CRM 传入脚本。

### 15.2 必须项

1. origin allowlist。
2. event type allowlist。
3. user gesture 校验。
4. schema 校验。
5. payload size 校验。
6. requestId 去重。
7. 操作审计。
8. 错误码标准化。
9. WebOperator layer 校验。
10. preload 最小暴露。

---

## 16. 审计日志

新增事件类型：

```ts
type CrmBridgeAuditAction =
  | "crm.event.received"
  | "crm.event.rejected"
  | "crm.event.routed"
  | "crm.command.sent"
  | "crm.command.failed";
```

审计字段：

```ts
interface CrmBridgeAuditRecord {
  id: string;
  requestId?: string;
  eventType?: string;
  origin?: string;
  url?: string;
  entityType?: string;
  entityId?: string;
  action: CrmBridgeAuditAction;
  status: "success" | "failed" | "blocked";
  errorCode?: string;
  message?: string;
  createdAt: string;
}
```

优先复用现有 BrowserActionLog 展示；CRM 事件可在 WebOperator 新增 panel 中展示。

---

## 17. 验收用例

### 17.1 正常上报

前置：

1. WebOperator 打开 `https://crm.company.com/customer/1001`。
2. CRM 页面安装 JSSDK。
3. CRM 页面存在「同步到桌面助手」按钮。

操作：

1. 用户点击按钮。
2. JSSDK 调用 `submitPageContext`。

预期：

1. preload 接收事件。
2. Main Process 校验通过。
3. WebOperator 侧栏出现 CRM Context。
4. `BrowserActionLog` 出现 `crm.event.received`。
5. Electron 打开配置对应模块或聚焦 WebOperator 指定 panel。

---

### 17.2 非用户点击触发被拒绝

操作：

```js
window.CopilotCrmDesktopSDK.submitPageContext(...)
```

由 console 或 setTimeout 直接执行。

预期：

1. preload 拒绝。
2. 返回 `USER_GESTURE_REQUIRED`。
3. Main Process 不执行 route。
4. 审计日志记录 blocked。

---

### 17.3 非白名单域名被拒绝

前置：

WebOperator 打开 `https://unknown.example.com`。

操作：

页面模拟发送 CRM bridge event。

预期：

1. Main Process 返回 `ORIGIN_NOT_ALLOWED`。
2. 不打开任何模块。
3. 不保存业务 payload。

---

### 17.4 payload 超限被拒绝

操作：

CRM 页面提交大于 `payloadMaxBytes` 的 payload。

预期：

1. Main Process 返回 `PAYLOAD_TOO_LARGE`。
2. Renderer 不更新 CRM Context。
3. 审计日志记录 blocked。

---

### 17.5 Electron 下发 CRM command

操作：

在 WebOperator CRM Context 面板点击「高亮客户等级字段」。

预期：

1. Renderer 调用 `crm-bridge:send-command`。
2. Main Process 向当前 WebOperator WebContents 发送 `crm-bridge:command`。
3. preload 转发 `window.postMessage`。
4. CRM JSSDK 收到 command。
5. CRM 页面对应字段高亮。

---

### 17.6 iframe 页面

场景 A：同源 iframe。

预期：

1. CRM JSSDK 可在 iframe 内通过 parent postMessage 转发。
2. Main Process 可通过 webFrameMain 定位并执行只读或允许写操作。

场景 B：跨源 iframe。

预期：

1. 只允许只读元信息。
2. 不允许写操作。
3. 不允许绕过 origin allowlist。

---

## 18. 测试计划

### 18.1 Unit Test

覆盖：

```text
crm-bridge-schema
crm-bridge-security
crm-event-router
crm-command-dispatcher
crm-desktop-jssdk
```

关键断言：

1. 合法 event 通过。
2. 非法 event type 被拒绝。
3. origin 不匹配被拒绝。
4. payload 超限被拒绝。
5. requestId 重复被拒绝。
6. command DTO 校验通过。
7. SDK 在非 Electron 环境 no-op。

---

### 18.2 Integration Test

使用本地 CRM fixture：

```text
tests/fixtures/crm/customer-detail.html
```

流程：

1. Electron 打开 fixture。
2. 模拟真实 click。
3. 等待 Main Process 收到事件。
4. 断言 WebOperator UI 出现 CRM Context。
5. 发送 command。
6. 断言 fixture DOM 被 JSSDK command handler 修改。

---

### 18.3 Regression Test

必须保证：

1. 原 `browser.open` 正常。
2. 原 `browser.get_state` 正常。
3. 原 `browser.screenshot` 正常。
4. 原 `browser.click/type` 正常。
5. 原 `ShellViewManager` portal view 不受影响。
6. 非 CRM 网站不会出现可用 CRM bridge 行为。

---

## 19. Cursor 实施任务拆分

### Task 1：新增 shared CRM bridge contract

创建：

```text
src/shared/crm-bridge/crm-bridge-contract.ts
src/shared/crm-bridge/crm-bridge-errors.ts
src/shared/crm-bridge/crm-bridge-schema.ts
```

完成 DTO、错误码、schema 校验。

---

### Task 2：新增 CRM preload

创建：

```text
src/preload/crm-bridge-preload.ts
```

实现：

1. contextBridge API。
2. postMessage 监听。
3. user gesture 校验。
4. command 下发监听。

---

### Task 3：WebOperator WebContentsView 加载专用 preload

修改：

```text
src/main/shell/shell-view-ipc.ts
src/main/browser/shell-browser-view-adapter.ts
```

实现：

1. `web-operator` view 创建时加载 `crm-bridge-preload`。
2. 保持 sandbox/contextIsolation/nodeIntegration 原安全配置。
3. 不影响 portal view。

---

### Task 4：新增 Main CRM bridge

创建：

```text
src/main/crm-bridge/**
```

实现：

1. `registerCrmBridgeIpc`。
2. `validateCrmBridgeEvent`。
3. `routeCrmBridgeEvent`。
4. `dispatchCrmCommand`。
5. `crm-event-store`。

---

### Task 5：Renderer WebOperator 增加 CRM Context 面板

创建：

```text
src/renderer/src/screens/WebOperator/CrmEventPanel.tsx
src/renderer/src/screens/WebOperator/hooks/use-crm-bridge-events.ts
```

修改：

```text
src/renderer/src/screens/WebOperator/WebOperatorScreen.tsx
src/renderer/src/screens/WebOperator/WebOperatorSideRail.tsx
```

实现：

1. 展示最近 CRM event。
2. 支持刷新 snapshot。
3. 支持发送 command。
4. 支持复制上下文。

---

### Task 6：新增 CRM JSSDK 包

创建：

```text
packages/crm-desktop-jssdk/**
```

实现：

1. IIFE build。
2. ESM build。
3. README。
4. CRM 接入示例。
5. 非 Electron 环境 no-op。

---

### Task 7：补充测试

创建：

```text
tests/crm-bridge/**
tests/fixtures/crm/**
```

覆盖：

1. bridge validation。
2. preload event。
3. event router。
4. command dispatcher。
5. SDK browser runtime。

---

## 20. Definition of Done

1. CRM 页面安装 JSSDK 后，用户点击按钮可主动提交页面上下文。
2. 非用户点击触发的提交被拒绝。
3. 非白名单域名被拒绝。
4. Electron Main Process 能根据 CRM event type 打开对应模块或聚焦 WebOperator panel。
5. Electron 能向 CRM 页面下发 command。
6. CRM 页面 JSSDK 能接收 command 并执行业务 handler。
7. 不引入本地 WebSocket Bridge。
8. 不破坏现有 WebOperator 浏览器动作。
9. 不破坏 portal/internal view。
10. 所有新增通道具备审计日志。
11. 所有新增 API 有 DTO 类型。
12. 所有新增能力有测试覆盖。

[1]: https://raw.githubusercontent.com/loudon84/ai-os-desktop/main/src/renderer/src/screens/WebOperator/WebOperatorScreen.tsx "raw.githubusercontent.com"
[2]: https://raw.githubusercontent.com/loudon84/ai-os-desktop/main/src/main/browser/browser-ipc.ts "raw.githubusercontent.com"
[3]: https://www.electronjs.org/docs/latest/api/web-contents-view "WebContentsView | Electron"
