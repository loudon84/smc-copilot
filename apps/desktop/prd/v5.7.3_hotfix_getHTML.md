# v5.7.2 getFrameHtml srcdoc fallback 实施方案

目标：修复 `iframe sandbox="" srcdoc="..."` 场景下 `Get HTML` 返回 `FRAME_SCRIPT_BLOCKED` 的问题。

当前失败点在 `BrowserV57Core.getFrameHtml()`：已能通过 `frameId` 找到 frame，但随后直接对子 frame 执行 `frame.executeJavaScript(GET_FRAME_HTML_SCRIPT(...))`，sandbox srcdoc frame 会阻断脚本执行。
`BrowserFrameInspector` 当前已提供 `frame.path` 与 `resolveFrameByPath()`，可以通过 `path` 找到父 frame，再从父 frame DOM 读取 iframe 元素的 `srcdoc`。

---

## 1. 修改范围

```text
src/shared/browser/browser-frame-contract.ts
src/main/browser/browser-v57-core.ts
src/renderer/src/screens/WebOperator/panels/PageFrameHtmlInspector.tsx
```

不修改：

```text
BrowserIPC
BrowserController
preload/browser-api.ts
PageStructurePanel
```

原因：`browser:get-frame-html` IPC 链路已存在，本次只补充 main 侧 fallback 与前端展示修复。

---

## 2. shared contract 增加来源字段

文件：

```text
src/shared/browser/browser-frame-contract.ts
```

修改 `BrowserFrameHtmlResult`：

```ts
export interface BrowserFrameHtmlResult {
  ok: boolean;
  frameId?: string;
  url?: string;
  title?: string;
  selector?: string;
  html?: string;
  text?: string;
  capturedAt: string;
  truncated?: boolean;
  source?: "frame-document" | "parent-srcdoc";
  error?: {
    code:
      | "WEB_CONTENTS_NOT_READY"
      | "FRAME_NOT_FOUND"
      | "FRAME_SCRIPT_BLOCKED"
      | "ELEMENT_NOT_FOUND"
      | "UNKNOWN_BROWSER_ERROR";
    message: string;
  };
}
```

---

## 3. BrowserV57Core 增加 srcdoc fallback 脚本

文件：

```text
src/main/browser/browser-v57-core.ts
```

在 `GET_FRAME_HTML_SCRIPT` 后新增：

```ts
const GET_SRCDOC_HTML_FROM_PARENT_SCRIPT = (
  childIndex: number,
  selector: string | undefined,
  outer: boolean,
  maxLength: number,
) => `
(function() {
  var frames = Array.from(document.querySelectorAll("iframe, frame"));
  var iframe = frames[${childIndex}];

  if (!iframe) {
    return {
      ok: false,
      code: "FRAME_NOT_FOUND",
      message: "iframe element not found in parent frame"
    };
  }

  var srcdoc =
    iframe.getAttribute("srcdoc") ||
    iframe.srcdoc ||
    "";

  if (!srcdoc) {
    return {
      ok: false,
      code: "ELEMENT_NOT_FOUND",
      message: "iframe srcdoc is empty"
    };
  }

  var html = srcdoc;
  var text = "";

  try {
    var doc = new DOMParser().parseFromString(srcdoc, "text/html");

    if (${selector ? "true" : "false"}) {
      var node = doc.querySelector(${JSON.stringify(selector ?? "")});

      if (!node) {
        return {
          ok: false,
          code: "ELEMENT_NOT_FOUND",
          message: "Element not found in iframe srcdoc"
        };
      }

      html = ${outer ? "node.outerHTML" : "node.innerHTML"};
      text = node.textContent || "";
    } else {
      text = doc.body ? doc.body.innerText : "";
    }
  } catch (error) {
    text = "";
  }

  var truncated = html.length > ${maxLength};

  return {
    ok: true,
    url: iframe.getAttribute("src") || "about:srcdoc",
    title: iframe.getAttribute("title") || "",
    html: html.slice(0, ${maxLength}),
    text: text.slice(0, ${Math.min(maxLength, 20000)}),
    truncated: truncated
  };
})()
`;
```

---

## 4. BrowserV57Core 增加私有方法

在 `BrowserV57Core` class 内新增：

```ts
private isSrcdocFrame(frameMeta: BrowserFrameSnapshot): boolean {
  return frameMeta.url === "about:srcdoc";
}

private async getFrameHtmlFromSrcdocParent(
  wc: WebContents,
  frameMeta: BrowserFrameSnapshot,
  target: BrowserFrameHtmlRequest,
): Promise<BrowserFrameHtmlResult | null> {
  if (!this.isSrcdocFrame(frameMeta)) return null;
  if (frameMeta.path.length === 0) return null;

  const parentPath = frameMeta.path.slice(0, -1);
  const childIndex = frameMeta.path[frameMeta.path.length - 1];

  if (typeof childIndex !== "number") return null;

  const parentFrame = this.frameInspector.resolveFrameByPath(wc, parentPath);
  if (!parentFrame) {
    return {
      ok: false,
      frameId: frameMeta.frameId,
      url: frameMeta.url,
      title: frameMeta.title,
      selector: target.selector,
      capturedAt: new Date().toISOString(),
      source: "parent-srcdoc",
      error: {
        code: "FRAME_NOT_FOUND",
        message: "Parent frame not found for srcdoc iframe",
      },
    };
  }

  const maxLength = target.maxLength ?? 100_000;

  try {
    const raw = (await parentFrame.executeJavaScript(
      GET_SRCDOC_HTML_FROM_PARENT_SCRIPT(
        childIndex,
        target.selector,
        target.outer !== false,
        maxLength,
      ),
    )) as {
      ok: boolean;
      url?: string;
      title?: string;
      html?: string;
      text?: string;
      truncated?: boolean;
      code?: BrowserActionErrorCode;
      message?: string;
    };

    if (!raw.ok) {
      const code =
        raw.code === "FRAME_NOT_FOUND" || raw.code === "ELEMENT_NOT_FOUND"
          ? raw.code
          : "UNKNOWN_BROWSER_ERROR";

      return {
        ok: false,
        frameId: frameMeta.frameId,
        url: frameMeta.url,
        title: frameMeta.title,
        selector: target.selector,
        capturedAt: new Date().toISOString(),
        source: "parent-srcdoc",
        error: {
          code,
          message: raw.message ?? "Read iframe srcdoc failed",
        },
      };
    }

    return {
      ok: true,
      frameId: frameMeta.frameId,
      url: raw.url ?? frameMeta.url,
      title: raw.title ?? frameMeta.title,
      selector: target.selector,
      html: raw.html ?? "",
      text: raw.text,
      truncated: raw.truncated,
      capturedAt: new Date().toISOString(),
      source: "parent-srcdoc",
    };
  } catch (err) {
    return {
      ok: false,
      frameId: frameMeta.frameId,
      url: frameMeta.url,
      title: frameMeta.title,
      selector: target.selector,
      capturedAt: new Date().toISOString(),
      source: "parent-srcdoc",
      error: {
        code: "FRAME_SCRIPT_BLOCKED",
        message: (err as Error).message,
      },
    };
  }
}
```

---

## 5. 修改 getFrameHtml 执行顺序

当前 `getFrameHtml()` 已完成 frame 查找、frame 解析、正常执行、catch 处理。

在获取到 `frameMeta` 之后、进入 `frame.executeJavaScript()` 之前，增加 srcdoc 优先读取。

在这段代码之后：

```ts
const frame = this.frameInspector.resolveFrameByPath(wc, frameMeta.path);
if (!frame) {
  ...
}
```

新增：

```ts
const srcdocResult = await this.getFrameHtmlFromSrcdocParent(
  wc,
  frameMeta,
  target,
);

if (srcdocResult?.ok) {
  this.log(
    action,
    target,
    wrapResult(action, startedAt, {
      ok: true,
      frameId: frameMeta.frameId,
      selector: target.selector,
      url: srcdocResult.url,
      title: srcdocResult.title,
    }),
  );

  return srcdocResult;
}
```

---

## 6. 修改正常 frame document 成功返回

在正常 `frame.executeJavaScript(GET_FRAME_HTML_SCRIPT...)` 成功返回处，给 result 增加来源：

```ts
const result: BrowserFrameHtmlResult = {
  ok: true,
  frameId: frameMeta.frameId,
  url: raw.url ?? frameMeta.url,
  title: raw.title ?? frameMeta.title,
  selector: target.selector,
  html: raw.html ?? "",
  text: raw.text,
  truncated: raw.truncated,
  capturedAt: new Date().toISOString(),
  source: "frame-document",
};
```

---

## 7. 修改 catch 分支

当前 catch 直接返回 `FRAME_SCRIPT_BLOCKED`。

替换 catch 为：

```ts
} catch (err) {
  const srcdocResult = await this.getFrameHtmlFromSrcdocParent(
    wc,
    frameMeta,
    target,
  );

  if (srcdocResult?.ok) {
    this.log(
      action,
      target,
      wrapResult(action, startedAt, {
        ok: true,
        frameId: frameMeta.frameId,
        selector: target.selector,
        url: srcdocResult.url,
        title: srcdocResult.title,
      }),
    );

    return srcdocResult;
  }

  const message =
    srcdocResult?.error?.message ??
    (err as Error).message;

  const error = actionError("FRAME_SCRIPT_BLOCKED", message);

  this.log(
    action,
    target,
    wrapResult(action, startedAt, {
      ok: false,
      frameId: frameMeta.frameId,
      selector: target.selector,
      url: frameMeta.url,
      title: frameMeta.title,
      error,
    }),
  );

  return {
    ok: false,
    frameId: frameMeta.frameId,
    url: frameMeta.url,
    title: frameMeta.title,
    selector: target.selector,
    capturedAt: new Date().toISOString(),
    source: srcdocResult?.source,
    error: {
      code: "FRAME_SCRIPT_BLOCKED",
      message,
    },
  };
}
```

说明：即使未来不是 `about:srcdoc`，也保留原有 `FRAME_SCRIPT_BLOCKED` 行为。

---

## 8. 修复 PageFrameHtmlInspector 的 body 提取 bug

文件：

```text
src/renderer/src/screens/WebOperator/panels/PageFrameHtmlInspector.tsx
```

当前函数存在提前 `return trimmed`，后续 `DOMParser` 永远不会执行。

替换为：

```ts
function extractBodyInnerHtml(html: string): string {
  const trimmed = html.trim();

  if (!trimmed || !/<body\b/i.test(trimmed)) {
    return html;
  }

  try {
    const doc = new DOMParser().parseFromString(trimmed, "text/html");
    if (doc.querySelector("parsererror")) {
      return html;
    }
    return doc.body?.innerHTML ?? html;
  } catch {
    const match = trimmed.match(/<body\b[^>]*>([\s\S]*?)<\/body>/i);
    return match?.[1] ?? html;
  }
}
```

---

## 9. PageFrameHtmlInspector 展示 source

在结果头部增加 source 信息。

将：

```tsx
{result.ok ? "OK" : "ERROR"} · {result.capturedAt}
{result.truncated ? " · truncated" : ""}
```

改为：

```tsx
{result.ok ? "OK" : "ERROR"} · {result.capturedAt}
{result.source ? ` · ${result.source}` : ""}
{result.truncated ? " · truncated" : ""}
```

---

## 10. 验收用例

### 用例 1：当前邮箱 iframe

页面：

```text
/zh/email
```

iframe：

```text
d1 about:srcdoc
```

操作：

```text
1. Page Structure 点击 Refresh Snapshot
2. 选择 d1 about:srcdoc
3. Frame HTML 点击 Get HTML
```

预期：

```json
{
  "ok": true,
  "frameId": "5",
  "url": "about:srcdoc",
  "source": "parent-srcdoc",
  "html": "<!DOCTYPE html><html>...",
  "truncated": false
}
```

---

### 用例 2：srcdoc 内 selector

输入：

```text
body
```

预期：

```json
{
  "ok": true,
  "source": "parent-srcdoc",
  "selector": "body",
  "html": "<body ...>...</body>"
}
```

输入：

```text
table
```

预期：

```json
{
  "ok": true,
  "source": "parent-srcdoc",
  "selector": "table",
  "html": "<table ...>...</table>"
}
```

---

### 用例 3：不存在 selector

输入：

```text
#not-exists
```

预期：

```json
{
  "ok": false,
  "source": "parent-srcdoc",
  "error": {
    "code": "ELEMENT_NOT_FOUND",
    "message": "Element not found in iframe srcdoc"
  }
}
```

---

### 用例 4：普通非 sandbox frame

选择普通 frame，点击 `Get HTML`。

预期：

```json
{
  "ok": true,
  "source": "frame-document"
}
```

---

## 11. Cursor 执行任务清单

```text
Task 1
修改 src/shared/browser/browser-frame-contract.ts
- BrowserFrameHtmlResult 增加 source?: "frame-document" | "parent-srcdoc"

Task 2
修改 src/main/browser/browser-v57-core.ts
- 新增 GET_SRCDOC_HTML_FROM_PARENT_SCRIPT
- 新增 isSrcdocFrame()
- 新增 getFrameHtmlFromSrcdocParent()
- getFrameHtml() 在 frame 解析后优先执行 srcdoc fallback
- 正常 getFrameHtml 成功结果增加 source: "frame-document"
- catch 分支增加 srcdoc fallback 二次兜底

Task 3
修改 src/renderer/src/screens/WebOperator/panels/PageFrameHtmlInspector.tsx
- 修复 extractBodyInnerHtml 提前 return bug
- 结果头部显示 result.source

Task 4
执行验证
- npm run typecheck
- npm run lint
- npm run build
- 打开 /zh/email
- 选择 about:srcdoc frame
- Get HTML 返回 source=parent-srcdoc
```

---

## 12. 禁止改动项

```text
1. 不修改 iframe sandbox 属性
2. 不给 srcdoc iframe 增加 allow-scripts
3. 不在子 iframe 内强制执行脚本
4. 不改 CRM 页面代码
5. 不改 BrowserIPC / preload 通道名称
```

该方案只通过父 frame 读取 iframe 元素的 `srcdoc` 属性，保持当前页面安全模型不变。
