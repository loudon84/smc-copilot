name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: .
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: npm
      - run: npm ci
      - run: npm run check:workspaces-no-tailwind
      - run: npm run check:no-reference-imports
      - run: npm run check:chat-boundaries
      - run: npm run typecheck:chat
      - run: npm run typecheck
      - run: npm test
      - run: npm run build
      - run: npm run test:e2e:electron:required
        env:
          E2E_ELECTRON_REQUIRED: "1"
          E2E_ELECTRON_WINDOW: "1"
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-electron-artifacts
          path: |
            playwright-report/
            test-results/e2e-electron/
          if-no-files-found: ignore
