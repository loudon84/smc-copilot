#!/usr/bin/env node
/** Scaffold: ban opening ~/.hermes/**/state.db from non-legacy Main (Phase 8). */
console.log("[check:no-hermes-home-access] scaffold ok (not enforced until Phase 8)");
process.exit(0);
