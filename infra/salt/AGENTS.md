# DISABLED — agent ignore

`infra/salt/` is **agent-disabled**.

Do not search, read, edit, test, or generate diffs here unless the user explicitly re-enables Salt in that message.

Active Endpoint Control Plane agent work: `infra/opsi/` + `services/opsi-control/` (ADR-031).
Historical SOT: ADR-026. Root routing: ../../AGENTS.md.
