[project]
name = "smc-salt-extensions"
version = "0.1.0"
description = "SMC Hermes Salt extensions (Endpoint Control Plane, PRD v2.0)"
requires-python = ">=3.12,<3.13"
dependencies = [
  "pyyaml>=6,<7",
]

[project.optional-dependencies]
dev = [
  "pytest>=8,<9",
  "ruff>=0.6,<1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["extensions", "mock_backend"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = [".", "extensions", "extensions/_utils", "mock_backend"]

[tool.ruff]
line-length = 120
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]
ignore = ["E501"]
