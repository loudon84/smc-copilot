# Production Salt Master extras for SMC Endpoint Control Plane (PRD v2.1).
# Lab fixture remains in infra/salt/lab/master.d/smc.conf (auto_accept allowed there only).

log_level: info
file_recv: False
timeout: 30
gather_job_timeout: 30
