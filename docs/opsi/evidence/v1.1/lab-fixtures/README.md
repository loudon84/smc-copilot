# Lab fixtures (operator)

Place redacted OPSI 4.3 exports here after Lab capture. CI uses `services/opsi-control/tests/fixtures/opsi-4.3` (`source=mock_verified`). Live `proven` requires operator files plus STATUS signoff.
