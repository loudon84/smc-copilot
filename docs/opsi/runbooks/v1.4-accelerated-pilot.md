# v1.4 Accelerated Pilot

Policy `accelerated-v1.4` (immutable digest bound to Campaign/Approval/Evidence):

- targets 3–5
- Canary 2 / 4h
- follow-on max 3 / 1h
- final 24h

`legacy-v1.2` remains explicit and cannot satisfy the v1.4 Gate.

## Live drill (operator)

1. Freeze a 3–5 endpoint snapshot.
2. Canary 2 endpoints, observe 4h; remaining ≤3, observe 1h; all 24h.
3. OPSI/Control offline: healthy Gateway/Work stay available (Direct Hermes).
4. target / batch / campaign rollback drill.
5. secret leak, owner conflict, false success, and final unknown must be 0.

Production Campaign start, stable promotion, and Ring advancement stay frozen until `v1.5-production-reentry` GO is persisted. Cursor must not write `proven/GO`.
