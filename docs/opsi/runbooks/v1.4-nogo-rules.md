# v1.4 NO-GO rules

Keep `docs/opsi/evidence/v1.1`, `v1.2`, and `v1.3` STATUS files at `not_proven / NO-GO`. Do not rewrite history to `proven`.

v1.4 tokens:

| Token | Who may set it |
| --- | --- |
| `implemented` | Cursor / CI after automated gates |
| `verified` | CI + contract/fixture gates |
| `proven` / `GO` | Operator signoff only |

Automatic NO-GO if any of:

- Lab assembled Fake RPC or Memory/SQLite
- Seeded facts used as inventory truth
- PATH `Get-Command hermes` success
- Signature length-only / missing Ed25519
- `clientId=local` continuation
- Uninstall task name mismatch
- Smoke rewrite of release keys or `.opsi` suffix
- Production start / stable / ring without `v1.5-production-reentry` GO

Cursor and fixtures must not write Operator `proven` or `GO` in lab or production.
