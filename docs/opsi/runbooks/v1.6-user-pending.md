# v1.6 User pending

Machine transactions may stop at `user_pending`. Resume is SID-scoped:

- inbox command binds request/client/SID/desired digest/deadline
- outbox writes observed digest
- status relays unacked records
- Control ack token archives outbox

Wrong SID, tamper, or expiry goes to quarantine. SYSTEM must not run Gateway/config CLI.
