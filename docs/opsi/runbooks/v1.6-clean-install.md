# v1.6 Clean install

1. Confirm ABSENT baseline: no owner, no Controller, no runtime slot, no SID tasks.
2. Dispatch setup with client-specific `request_id`, `client_id`, `managed_user_sid/account`, exact `hermes_version`.
3. Machine stage installs Controller under ProgramData and Hermes into `runtime/versions/<version>-<digest>`.
4. If the user is not logged on, parent result is `USER_CONTEXT_PENDING` (exit 10).
5. After logon, User Controller writes config/Gateway, health, then owner commit.

Do not treat Product `installed` as HEALTHY.
