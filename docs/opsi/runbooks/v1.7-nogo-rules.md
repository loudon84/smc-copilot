# v1.7 Evidence capture and NO-GO

Engineering may set `implemented`/`verified`. Cursor/CI must not write `proven` or `GO`.

`v1.7-client-deployment-release` GO requires Release Owner, Endpoint Ops, and Security Owner signatures over the same Evidence manifest. Smoke/fixture/`test://` refs cannot import GO.

Until GO, v1.5 Production start/stable/next Ring stay 412 (also requiring v1.5 re-entry and v1.6 controller Gates). v1.7 GO does not authorize ≤100 rollout.
