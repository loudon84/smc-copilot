# v1.4 Windows 10 Clean Endpoint

Operator-attended. Cursor must not mark this proven.

## Scope

1. One Windows 10 endpoint with no managed Hermes, no `owner=opsi`, and no SID tasks. Windows 11 is not part of the v1.4 signoff matrix.
2. Real Lab OPSI 4.3 Config Server/Depot.
3. Real signed `.opsi` from the Linux builder.

## Sequence

1. Refresh inventory and write operator Binding (SID/account). Do not put `actor` in the body.
2. Preflight must classify `ABSENT`.
3. `setup` → machine stage (`owner=pending`) → user logon → continuation outbox with the real OPSI client id → status poll relay → `owner=opsi` only after Gateway health.
4. Run status, apply-config, restart-gateway, diagnose, repair. All success paths use `Resolve-SmcHermesCli` (absolute `hermes.exe`).
5. update, failed rollback, successful rollback, uninstall. Confirm SID tasks are removed by name, user `.hermes` data is retained, and previous owner is restored on failure.
6. Tamper artifact/signature/health and confirm fail-closed (no version/owner write).

Archive evidence under `docs/opsi/evidence/v1.4/`. Only Operator signoff may set `win10-clean-endpoint=proven/GO`.
