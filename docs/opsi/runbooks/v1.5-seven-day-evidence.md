# v1.5 7-Day Observation and Evidence

After Ring 100, keep the fleet under observation for 7 days. Evidence Manifest v3 is recomputed from PostgreSQL events, verifications, rings, attestations, and freezes.

Service verification tops out at `verified`. Decision defaults to `NO-GO`. Operator signed Go/No-Go is stored separately from the service manifest.

Live GO only authorizes 21–50 / 1–2 Depot on Windows 10 evidence. 51–500 and 3–8 Depot remain NO-GO.
