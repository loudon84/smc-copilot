# v1.7 Lab Depot publish / read-back

Operator only:

```bash
opsi-package-manager -i smc-hermes-agent_1.7.0-1.opsi
```

Record actor, time, depot, and `.opsi` SHA256. Read back with `productOnDepot_getObjects` for exact `productId=smc-hermes-agent`, `productVersion=1.7.0`, `packageVersion=1`. Persist the read-back digest on the Product Release record. Smoke/fixture packages must keep `liveEligible=false`.
