# v1.3 Production nomination

1. Confirm v1.2 Evidence is operator-signed `proven / GO`. Do not start if STATUS.md is `NO-GO`.
2. Freeze client→depot mapping and ring digest for 21–500 endpoints across 1–8 Depots.
3. Complete OPSI-native package distribution. Do not ask `opsi-control` to copy files.
4. Record a signed Depot Artifact Attestation per Depot (exact Product/package + digest).
5. Promote `pilot → stable` only after Release Owner + Security Owner approval bound to that digest.
6. Open a `mode=production` Campaign. Triple-approve start (Release, Endpoint Ops, Security). Site Owner approves each Depot lane.

Cursor must not seed live gates in `SMC_OPSI_ENV=production`.
