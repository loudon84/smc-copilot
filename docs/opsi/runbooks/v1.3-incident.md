# v1.3 Incident

1. Freeze production (`POST /api/v1/opsi/release-freezes`).
2. Pause affected Depot lanes. Do not delete failed Targets or rewrite snapshots.
3. Collect Evidence Manifest v2 (restart-stable digest). Redact secrets/usernames.
4. If mutation occurred, run scoped rollback with frozen baseline.
5. Restore OPSI/Control/DB; wait for authoritative reconcile or `UNKNOWN_BLOCKED`.
6. Clear freeze only after dual approval and fresh preflight.
