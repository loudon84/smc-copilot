# v1.6 Config transport

Policy keys are encoded as bounded base64url `config_payload` plus `config_digest`.

`custom.opsiscript` must read both properties and pass them to the thin bootstrap. Endpoint decode recomputes the digest. Secret key names fail closed. Do not concatenate raw JSON into a PowerShell expression.
