# v1.6 Crash recovery

Controller startup and `reconcile-controller` scan open journals.

- Verified checkpoint → resume
- Unprovable checkpoint → deterministic rollback or `MANUAL_BLOCKED`
- Same request + digest is idempotent; different digest conflicts
- Open mutation blocks a new mutation
