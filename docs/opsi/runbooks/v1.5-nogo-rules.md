# v1.5 NO-GO rules

Keep v1.1–v1.4 Evidence STATUS files at `not_proven / NO-GO`. Do not rewrite history.

Automatic NO-GO if any of:

- production Rollout HEALTHY from `self.facts`
- observation started before all current-ring Targets are HEALTHY
- next Ring without predecessor PASSED / deadline / freshness / triple approval
- Depot attestation signature-length-only or failed Ed25519
- production seed / body GO / env flag used to unfreeze
- engineering-v1.3 500/8 treated as Live GO
- Cursor/CI wrote `proven` or `GO`

Human evidence uses Windows 10 only. Existing Windows 11 compatibility logic is unchanged.
