# v1.6 NO-GO rules

Keep v1.1–v1.5 Evidence STATUS files at `not_proven / NO-GO`.

Automatic NO-GO if any of:

- `/clients/{id}/state` maps Product `installed` to HEALTHY
- Endpoint status hardcodes `owner=opsi`
- SYSTEM runs user Gateway/config CLI
- journal does not resume after crash
- uninstall leaves `hermes=opsi`
- Cursor/CI wrote `proven` or `GO` for `v1.6-endpoint-controller`

v1.5 Production start/stable/next Ring stay 412 until both `v1.5-production-reentry` and `v1.6-endpoint-controller` are operator GO.
