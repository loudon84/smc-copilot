# v1.6 Update / rollback

Controller-only and runtime-only updates are separate transactions. Compatibility is checked before activation.

Failure restores exact previous Controller, runtime slot, config, task, and owner. Rollback failure is `MANUAL_BLOCKED` and freezes Production mutation.
