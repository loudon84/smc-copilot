# Salt v2.3 → v2.4 Go / No-Go

## Repo status (`implemented`)

v2.3 delivered production composition (PostgreSQL/OIDC/live adapters), salt-api adapter + eAuth policy, adoption tooling, atomic handover modules, and first-endpoint evidence templates under `docs/salt/evidence/v2.3/first-endpoint/2026-08-12/`.

## Live status (`not_proven` / Manual Gate)

| Item | Status |
| --- | --- |
| Master `salt ITBJB0676 test.ping` | NOT PROVEN |
| Trusted master fingerprint locked | NOT PROVEN |
| `ep_*` adoption + highstate | NOT PROVEN |
| Hermes/Gateway handover on hardware | NOT PROVEN |
| 24h metrics | NOT PROVEN |
| Second Master + failover drill | **MISSING** |

## Decision

**v2.4 Ring 0: NO-GO**

Reasons:

1. Single Master (`192.168.102.104`) only — plan requires second Master before Ring 0.
2. First-endpoint Manual Gates remain unsigned / not proven.
3. Runtime Endpoint Control Plane must stay available (not decommissioned).

Revisit after operator-filed `proven` evidence and dual-Master failover drill.
