# v2.4 Ring 0 — Go / No-Go (from v2.3.1)

Status: **NO-GO** until Manual Gates below are `proven`.

## Implemented in-repo (code / CI)

- [x] Shared AsyncSession eliminated for JobWorker / Observer (independent sessions)
- [x] Job claim / lease / heartbeat / reclaim
- [x] Redis wake + DB poll converge on claim
- [x] JID conflict does not mutate ownership (`SALT_JID_CONFLICT`)
- [x] Secret scope idempotent upsert (no plaintext in scopes)
- [x] Job API + Endpoint status + Observer + Rollout approval
- [x] Handover / Rollback / Remigrate orchestration hooks
- [x] Live Canary workflow + apps/work enterprise salt tests

## Manual Gates (must stay not_proven until operator attestation)

- [ ] First real Endpoint migrate / rollback / remigrate proven
- [ ] 24h observation thresholds proven
- [ ] Master backup/restore drill proven
- [ ] Single-Master risk acceptance signed
- [ ] Runtime fallback proven on live endpoint
- [ ] Live Canary with `confirm_live=LIVE` proven

## Decision

**Current: NO-GO for Ring 0.**

Re-evaluate only after Phase 5–6 Manual Gates are signed under `docs/salt/evidence/v2.3.1/first-endpoint/`.
