---
description: Apply minimal, root-cause implementation rules when a Cursor .plan.md is referenced.
globs: .cursor/plans/*.plan.md
alwaysApply: false
---

# Plan → code: lazy senior

本规则只在引用 `.cursor/plans/*.plan.md` 时附加。`.plan.md` 是结果、边界和当前实施切片，不是施工清单；文件链接是按需探索上限，不是必须读取、修改或新建的下限。

## 先收敛上下文

1. 只读取计划「立即读取」中的材料和当前 todo 的主锚点。
2. 「按触发读取」仅在触发条件真实发生后加载；不得预读整个目录、历史 PRD/evidence 或无关 ADR/子项目。
3. 从主锚点追踪真实调用链：读取被改符号及其直接调用方。发现共享根因后在那里修一次，不在各调用方重复补丁。
4. 计划列出多个 todo 时，只实现当前选中的 todo；不得预实现后续 todo。

## 最小方案梯子

理解真实调用链后，停在第一级能满足当前停止条件的方案：

1. 无需新增：现有行为已满足或需求属于推测性扩展时跳过。
2. 复用仓库现有 helper、schema、type、fixture 或共享路径。
3. 使用标准库。
4. 使用平台或框架原生能力。
5. 使用已安装依赖；禁止为少量逻辑增加新包。
6. 在现有函数、字段或 mapping 中完成。
7. 仅当前述都不成立时，编写满足停止条件的最小新增实现。

两个方案都可行时选择梯子中更靠前、边界条件正确的方案。最短工作 diff 胜出，但必须是根因位置上的正确修复。

## 执行变更预算

- 计划预算是上限，不是配额；能少碰文件就少碰。
- 默认不新增生产文件、依赖、公共接口、抽象层或测试文件。
- 超预算前，必须确认现有能力不能满足当前停止条件，并在交付说明中写一行具体理由。
- 生成产物通过既有生成入口产生，不手写、不逐项扩展计划中的产物列表。

只要更短的正确补丁已经满足停止条件，禁止：

- 单实现的 Protocol / ABC / Factory。
- 新建 `*_codec.py`、`*_mapper.py`、`*_adapter.py`、`*_invocation.py`。
- 为未来预留配置对象、重试包装、兼容层或扩展点。
- 为每个 todo 创建平行 test harness。
- 在多个调用方重复同一 guard，而不修共享函数。

## 最小验证与停止

- 非平凡逻辑保留一个最小可运行回归检查；优先在现有测试中增加断言。
- 先运行当前 todo 的直接测试；只有实际触发跨项目、contract、生成物或发布边界时才运行对应门禁。
- 当前 todo 的客观停止条件成立后立即停止，不顺手重构、补文档体系或实施未来 Phase。
- Manual、Live Evidence、签署和观察窗口不得由 Cursor 自动完成或标记 `proven`。

## 不准懒

不得精简：信任边界校验、防止数据丢失的错误处理、安全与 Secret 保护、可访问性、明确要求的行为、实机校准，以及计划停止条件要求的验证。修根因，不只修票据点名的症状。

交付最多说明三件事：做了什么；跳过什么；什么可观察条件出现时再增加。若超出计划预算，再补一行必要性理由。
